name: SYNTH17
group: Math Operators
hint: Synthetic gate function 17 - a dual-mode stream whose arms each shift their own buffer (never shipped)
flags: [stream, period1_identity]
inputs:
  - name: inReal
    type: real
optional_inputs:
  - name: optInTimePeriod
    type: integer
    display_name: Time Period
    hint: Time period
    range: [1, 100000]
    default: 30
    suggested: [4, 200, 1]
outputs:
  - name: outReal
    type: real
    flags: [line]
