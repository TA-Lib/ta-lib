name: SYNTH18
group: Math Operators
hint: Synthetic gate function 18 - initialized locals read before a top-level overwrite (never shipped)
# `path_dependent`: `cap` is set from the first bar of the requested window, so
# a sub-range call answers differently from the full-range one.
flags: [stream, path_dependent]
inputs:
  - name: inReal
    type: real
optional_inputs:
  - name: optInTimePeriod
    type: integer
    display_name: Time Period
    hint: Time period
    range: [2, 100000]
    default: 30
    suggested: [4, 200, 1]
outputs:
  - name: outReal
    type: real
    flags: [line]
