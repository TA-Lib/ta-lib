name: SYNTH19
group: Math Operators
hint: Synthetic gate function 19 - memset into allocated buffers (never shipped)
# `path_dependent`: the seed comes from the first output's window, so a
# later startIdx computes a different one.
flags: [stream, path_dependent]
inputs:
  - name: inReal
    type: real
optional_inputs:
  - name: optInTimePeriod
    type: integer
    display_name: Time Period
    hint: Time period
    range: [2, 100000]
    default: 30
    suggested: [4, 200, 1]
outputs:
  - name: outInteger
    type: integer
    flags: [line]
