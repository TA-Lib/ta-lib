//! GENERATED by ta_codegen -- DO NOT EDIT.
//!
//! Negative-space coverage for the Rust batch API: array lengths chosen so that
//! any access the contract forbids is a panic rather than a comment.
//!
//! The value gates (`ta_regtest`, `--xlang-hash`, `--fuzz-064`) can only see
//! work that reaches an output. Work a function does and then discards -- a read
//! past `endIdx`, a write past the count it reported, a leg it touches on a
//! range where it promised to touch nothing -- leaves no trace in any output, so
//! nothing there can see it.
//!
//! # What each sweep can and cannot see
//!
//! **`sub_*` (sub-lookback).** A range strictly shorter than the lookback, with
//! a zero-length slice for every input and every output. That call is a
//! documented success with no values and must touch nothing; with no elements,
//! every index is out of bounds. This sweep is fully non-circular: on such a
//! range the body's `assert!` preamble short-circuits (`_assertStart > endIdx`
//! is true), so the zero-length slices reach the body itself and the only thing
//! that can fire is a real `inReal[i]` or `outReal[i]` access. It is the sweep
//! that caught APO, PPO and PVO computing their fast MA over the whole requested
//! range before discovering the range was too short for their own.
//!
//! Each vector also gets a control arm first: the same call one bar longer
//! produces exactly one value, so it must index an array, so with zero-length
//! slices it must panic. That turns "this core was silent" into "this core was
//! silent *and* the detector was working on it".
//!
//! **`legs_*` (unread legs).** One declared input at a time given a zero-length
//! slice while the rest stay correctly sized, over a range that does produce
//! values. Here the panic can come from two places, and the difference is the
//! whole point:
//!
//! * an **assertion failure** is the body's `assert!` preamble, whose leg set is
//!   `backends::common::indexed_input_names` -- the same computation that feeds
//!   Java's and C#'s argument checks. Asserting *that* against itself would be
//!   circular, so it is reported, not asserted on.
//! * an **index-out-of-bounds** is the body indexing a leg the preamble does not
//!   bound. Under `#![forbid(unsafe_code)]` that access is checked whatever the
//!   preamble says, which is what makes this reading the body rather than the
//!   declaration. It is a violation: in Rust it is a panic in shipped code, and
//!   in the C the same input generates, an unbounded read.
//!
//! So this sweep pins one direction of `indexed_input_names` -- that it covers
//! every leg the body actually indexes. The other direction (a leg listed but
//! never read) is not reachable from outside the crate: the preamble runs first
//! and pre-empts the observation. That is a false rejection, not a memory
//! error, and Java's suite does not distinguish it either.
//!
//! # Measured against the Java suite
//!
//! Run together on the same corpus, the two agree where they can:
//!
//! * **sub-lookback: zero delta.** 144 cores probed at the defaults vector, 30
//!   skipped for having no sub-lookback range (lookback 0), 174 detector
//!   controls fired -- the same three numbers in both, so Rust reaches every
//!   core Java reaches.
//! * **unread legs: Rust sees 7, Java sees 40, and the 7 are a subset.** The 33
//!   extra are legs Rust's preamble bounds but the body does not read at the
//!   default candle settings; the preamble pre-empts the observation, which is
//!   the direction described above as unreachable from outside the crate. Zero
//!   legs are unread here and read there, which is the direction that would be
//!   a real disagreement.
//!
//! # What is deliberately not here
//!
//! `exactExtentSweep` and `openAndFillSweep` from the Java suite are not ported.
//! The first passes the public guard by construction; the second is streaming,
//! which #236 does not touch.
//!
//! And this covers the shared C under `ta_codegen/input/` as the Rust emitter
//! renders it. It is not a substitute for the Java and C# suites: an emitter bug
//! that changes what ONE backend touches without changing what it produces is
//! invisible to `--xlang-hash` by construction, so each backend needs its own.

#![allow(non_snake_case)]
// SAREXT takes eight optional parameters, so its vector tuple is eight wide.
// Naming a type per arity would be eight aliases used once each.
#![allow(clippy::type_complexity)]

use ta_lib::Core;
use ta_lib::MAType;

/// What one call did to the arrays it was given.
#[derive(Debug, PartialEq, Eq)]
enum Touch {
    /// Returned without touching an out-of-bounds element.
    Quiet(bool, usize),
    /// The body's `assert!` preamble fired: bounded by the declaration.
    Bounded,
    /// The body indexed out of bounds: it read or wrote the array itself.
    Indexed,
    /// A panic that is neither -- reported rather than classified away.
    Other(String),
}

/// Run one call, classifying a panic by what produced it.
fn run(f: impl FnOnce() -> Result<ta_lib::OutRange, ta_lib::RetCode>) -> Touch {
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(f)) {
        Ok(Ok(range)) => Touch::Quiet(true, range.count),
        Ok(Err(_)) => Touch::Quiet(false, 0),
        Err(e) => {
            let msg = e
                .downcast_ref::<String>()
                .map(String::as_str)
                .or_else(|| e.downcast_ref::<&str>().copied())
                .unwrap_or("<non-string panic>")
                .to_string();
            if msg.contains("index out of bounds") || msg.contains("out of range for slice") {
                Touch::Indexed
            } else if msg.contains("assertion failed") {
                Touch::Bounded
            } else {
                Touch::Other(msg)
            }
        }
    }
}

/// `len` bars of `leg`, mirroring `NoPhantomIoTest.bar` so the two suites agree
/// on the fixture and a leg cannot read as unread here but read there.
fn series<T: FromF64>(leg: &str, len: usize) -> Vec<T> {
    (0..len).map(|i| T::from_f64(bar(leg, i))).collect()
}

trait FromF64 {
    fn from_f64(v: f64) -> Self;
}
impl FromF64 for f64 {
    fn from_f64(v: f64) -> Self { v }
}
impl FromF64 for i32 {
    fn from_f64(v: f64) -> Self { v as i32 }
}

fn bar(leg: &str, i: usize) -> f64 {
    let x = i as f64;
    let base = 100.0 + 10.0 * (x / 7.0).sin() + 3.0 * (x / 3.0).cos();
    match leg {
        "open" => base - 0.5,
        "high" => base + 2.0,
        "low" => base - 2.0,
        "close" => base + 0.5,
        "volume" => 1000.0 + x,
        // MAVP's per-bar period: the one leg whose value sets how far back the
        // function reads.
        "inPeriods" => if i % 3 == 0 { 1.0e5 } else { 5.0 + (i % 7) as f64 },
        _ => base,
    }
}

/// Counters and failures for both sweeps.
struct Report {
    name: &'static str,
    failures: Vec<String>,
    probed: usize,
    no_quiet: usize,
    controls_live: std::collections::BTreeSet<&'static str>,
    quiet_cores: std::collections::BTreeSet<&'static str>,
    quiet_calls: usize,
    /// Per function: the legs whose empty slice stopped the call, and how.
    bounded: std::collections::BTreeMap<&'static str, Vec<String>>,
    unread: Vec<String>,
    legs_seen: usize,
    funcs_with_legs: usize,
}

impl Report {
    fn new() -> Self {
        Report {
            name: "",
            failures: Vec::new(),
            probed: 0,
            no_quiet: 0,
            controls_live: std::collections::BTreeSet::new(),
            quiet_cores: std::collections::BTreeSet::new(),
            quiet_calls: 0,
            bounded: std::collections::BTreeMap::new(),
            unread: Vec::new(),
            legs_seen: 0,
            funcs_with_legs: 0,
        }
    }

    fn fail(&mut self, what: String) {
        self.failures.push(what);
    }

    /// The per-vector control arm: a call that produces a value must index an
    /// array, so with zero-length slices it must panic.
    fn control(&mut self, name: &'static str, label: &str, t: Touch) {
        match t {
            Touch::Quiet(ok, n) => self.fail(format!(
                "{name}[{label}] at endIdx == lookback returned {} ({n} value(s)) without \
                 touching an array; a call that produces a value must index one, so this \
                 sweep could not detect I/O for it",
                if ok { "Ok" } else { "Err" }
            )),
            Touch::Other(msg) => {
                self.fail(format!("{name}[{label}] at endIdx == lookback panicked with {msg}"));
            }
            _ => {
                self.controls_live.insert(name);
            }
        }
    }

    fn no_quiet_range(&mut self, name: &'static str, label: &str) {
        if label == "defaults" {
            self.no_quiet += 1;
        }
        let _ = name;
    }

    /// The sweep proper: a sub-lookback range must be a success with no values
    /// and must touch neither array.
    fn quiet(&mut self, name: &'static str, label: &str, lb: usize, t: Touch) {
        if label == "defaults" {
            self.probed += 1;
        }
        self.quiet_calls += 1;
        match t {
            Touch::Quiet(true, 0) => {
                self.quiet_cores.insert(name);
            }
            Touch::Quiet(true, n) => self.fail(format!(
                "{name}[{label}] (lookback {lb}, endIdx {}) reported {n} value(s) on a \
                 sub-lookback range",
                lb - 1
            )),
            Touch::Quiet(false, _) => self.fail(format!(
                "{name}[{label}] (lookback {lb}, endIdx {}) returned Err, expected a \
                 success with no values",
                lb - 1
            )),
            Touch::Bounded => self.fail(format!(
                "{name}[{label}] (lookback {lb}, endIdx {}) tripped its own bounds assert on a \
                 range it promised to touch nothing on",
                lb - 1
            )),
            Touch::Indexed => self.fail(format!(
                "{name}[{label}] (lookback {lb}, endIdx {}) INDEXED an array on a sub-lookback \
                 range: it read an input or wrote an output the contract says it must not touch",
                lb - 1
            )),
            Touch::Other(msg) => self.fail(format!("{name}[{label}] panicked with {msg}")),
        }
    }

    fn no_legs(&mut self, name: &'static str) {
        let _ = name;
    }

    /// One leg given a zero-length slice while the rest stay sized.
    fn leg(&mut self, name: &'static str, leg: &str, _index: usize, t: Touch) {
        self.legs_seen += 1;
        match t {
            Touch::Bounded => self.bounded.entry(name).or_default().push(leg.to_string()),
            Touch::Indexed => self.fail(format!(
                "{name}.{leg} was indexed with a zero-length slice, but the body's bounds-assert \
                 preamble does not cover it: the body reads a leg the declaration says it does \
                 not, so nothing bounds that read"
            )),
            Touch::Quiet(_, _) => self.unread.push(format!("{name}.{leg}")),
            Touch::Other(msg) => self.fail(format!("{name}.{leg} panicked with {msg}")),
        }
    }

    fn legs_done(&mut self, name: &'static str, declared: usize) {
        self.funcs_with_legs += 1;
        if !self.bounded.contains_key(name) {
            self.fail(format!(
                "{name} reads none of its {declared} declared leg(s): a function that reads no \
                 input has stopped being an indicator"
            ));
        }
    }

    fn finish_sub_lookback(&mut self) {
        // Every core is accounted for at the defaults vector: probed, or
        // explicitly counted as having no sub-lookback range.
        if self.probed + self.no_quiet != PROBES.len() {
            self.fail(format!(
                "sub-lookback: every core is probed or counted ({} + {} vs {})",
                self.probed,
                self.no_quiet,
                PROBES.len()
            ));
        }
        if self.probed == 0 || self.no_quiet == 0 {
            self.fail(format!(
                "sub-lookback: both outcomes must occur, so neither branch is dead ({} probed, \
                 {} with no sub-lookback range)",
                self.probed, self.no_quiet
            ));
        }
        // The detector is proved live per core, not once on SMA.
        if self.controls_live.len() != PROBES.len() {
            let missing: Vec<&str> = PROBES
                .iter()
                .map(|p| p.0)
                .filter(|n| !self.controls_live.contains(n))
                .collect();
            self.fail(format!(
                "sub-lookback: the detector is proved live for every core ({} of {}; not proved \
                 {missing:?})",
                self.controls_live.len(),
                PROBES.len()
            ));
        }
    }

    fn finish_legs(&mut self) {
        if self.funcs_with_legs == 0 || self.legs_seen == 0 {
            self.fail("unread legs: the sweep ran no leg at all".to_string());
        }
    }

    fn print(&self) {
        println!(
            "  sub-lookback: {} core(s) probed at defaults, {} skipped (lookback 0, no \
             sub-lookback range exists), {} quiet call(s) across all vectors; {} detector \
             control(s) fired",
            self.probed,
            self.no_quiet,
            self.quiet_calls,
            self.controls_live.len()
        );
        let bounded_legs: usize = self.bounded.values().map(Vec::len).sum();
        println!(
            "  unread legs: {} leg(s) across {} function(s); {} bounded by the assert preamble, \
             {} declared but never indexed{}",
            self.legs_seen,
            self.funcs_with_legs,
            bounded_legs,
            self.unread.len(),
            if self.unread.is_empty() { String::new() } else { format!(" -> {:?}", self.unread) }
        );
        for f in &self.failures {
            println!("  VIOLATION: {f}");
        }
    }
}

const V_AC: &[(&str, i32, i32, i32)] = &[
    ("defaults", i32::MIN, i32::MIN, i32::MIN),
    ("minimums", 2i32, 2i32, 2i32),
];

fn sub_AC(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastPeriod, optInSlowPeriod, optInSignalPeriod) in V_AC {
        let lb = core.AC_Lookback(optInFastPeriod, optInSlowPeriod, optInSignalPeriod);
        if lb == usize::MAX { continue; }
        r.control("AC", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AC(0, lb, &inHigh, &inLow, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("AC", label); continue; }
        r.quiet("AC", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AC(0, lb - 1, &inHigh, &inLow, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outReal)
        }));
    }
}

fn legs_AC(r: &mut Report) {
    let core = Core::new();
    let optInFastPeriod = i32::MIN;
    let optInSlowPeriod = i32::MIN;
    let optInSignalPeriod = i32::MIN;
    let lb = core.AC_Lookback(optInFastPeriod, optInSlowPeriod, optInSignalPeriod);
    if lb == usize::MAX { r.no_legs("AC"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AC", "inHigh", 0, run(|| core.AC(startIdx, endIdx, &inHigh, &inLow, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AC", "inLow", 1, run(|| core.AC(startIdx, endIdx, &inHigh, &inLow, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outReal)));
    }
    r.legs_done("AC", 2);
}

const V_ACCBANDS: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_ACCBANDS(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_ACCBANDS {
        let lb = core.ACCBANDS_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("ACCBANDS", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outRealUpperBand: Vec<f64> = Vec::with_capacity(1);
            let mut outRealMiddleBand: Vec<f64> = Vec::with_capacity(1);
            let mut outRealLowerBand: Vec<f64> = Vec::with_capacity(1);
            core.ACCBANDS(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outRealUpperBand, &mut outRealMiddleBand, &mut outRealLowerBand)
        }));
        if lb < 1 { r.no_quiet_range("ACCBANDS", label); continue; }
        r.quiet("ACCBANDS", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outRealUpperBand: Vec<f64> = Vec::with_capacity(1);
            let mut outRealMiddleBand: Vec<f64> = Vec::with_capacity(1);
            let mut outRealLowerBand: Vec<f64> = Vec::with_capacity(1);
            core.ACCBANDS(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outRealUpperBand, &mut outRealMiddleBand, &mut outRealLowerBand)
        }));
    }
}

fn legs_ACCBANDS(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.ACCBANDS_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("ACCBANDS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outRealUpperBand: Vec<f64> = vec![Default::default(); 5];
        let mut outRealMiddleBand: Vec<f64> = vec![Default::default(); 5];
        let mut outRealLowerBand: Vec<f64> = vec![Default::default(); 5];
        r.leg("ACCBANDS", "inHigh", 0, run(|| core.ACCBANDS(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outRealUpperBand, &mut outRealMiddleBand, &mut outRealLowerBand)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outRealUpperBand: Vec<f64> = vec![Default::default(); 5];
        let mut outRealMiddleBand: Vec<f64> = vec![Default::default(); 5];
        let mut outRealLowerBand: Vec<f64> = vec![Default::default(); 5];
        r.leg("ACCBANDS", "inLow", 1, run(|| core.ACCBANDS(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outRealUpperBand, &mut outRealMiddleBand, &mut outRealLowerBand)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outRealUpperBand: Vec<f64> = vec![Default::default(); 5];
        let mut outRealMiddleBand: Vec<f64> = vec![Default::default(); 5];
        let mut outRealLowerBand: Vec<f64> = vec![Default::default(); 5];
        r.leg("ACCBANDS", "inClose", 2, run(|| core.ACCBANDS(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outRealUpperBand, &mut outRealMiddleBand, &mut outRealLowerBand)));
    }
    r.legs_done("ACCBANDS", 3);
}

const V_ACOS: &[&str] = &[
    "defaults",
];

fn sub_ACOS(r: &mut Report) {
    let core = Core::new();
    for &label in V_ACOS {
        let lb = core.ACOS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("ACOS", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ACOS(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ACOS", label); continue; }
        r.quiet("ACOS", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ACOS(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_ACOS(r: &mut Report) {
    let core = Core::new();
    let lb = core.ACOS_Lookback();
    if lb == usize::MAX { r.no_legs("ACOS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ACOS", "inReal", 0, run(|| core.ACOS(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("ACOS", 1);
}

const V_AD: &[&str] = &[
    "defaults",
];

fn sub_AD(r: &mut Report) {
    let core = Core::new();
    for &label in V_AD {
        let lb = core.AD_Lookback();
        if lb == usize::MAX { continue; }
        r.control("AD", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AD(0, lb, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("AD", label); continue; }
        r.quiet("AD", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AD(0, lb - 1, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)
        }));
    }
}

fn legs_AD(r: &mut Report) {
    let core = Core::new();
    let lb = core.AD_Lookback();
    if lb == usize::MAX { r.no_legs("AD"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AD", "inHigh", 0, run(|| core.AD(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AD", "inLow", 1, run(|| core.AD(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AD", "inClose", 2, run(|| core.AD(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AD", "inVolume", 3, run(|| core.AD(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)));
    }
    r.legs_done("AD", 4);
}

const V_ADD: &[&str] = &[
    "defaults",
];

fn sub_ADD(r: &mut Report) {
    let core = Core::new();
    for &label in V_ADD {
        let lb = core.ADD_Lookback();
        if lb == usize::MAX { continue; }
        r.control("ADD", label, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ADD(0, lb, &inReal0, &inReal1, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ADD", label); continue; }
        r.quiet("ADD", label, lb, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ADD(0, lb - 1, &inReal0, &inReal1, &mut outReal)
        }));
    }
}

fn legs_ADD(r: &mut Report) {
    let core = Core::new();
    let lb = core.ADD_Lookback();
    if lb == usize::MAX { r.no_legs("ADD"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal0: Vec<f64> = Vec::with_capacity(1);
        let inReal1: Vec<f64> = series("real", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADD", "inReal0", 0, run(|| core.ADD(startIdx, endIdx, &inReal0, &inReal1, &mut outReal)));
    }
    {
        let inReal0: Vec<f64> = series("real", endIdx + 1);
        let inReal1: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADD", "inReal1", 1, run(|| core.ADD(startIdx, endIdx, &inReal0, &inReal1, &mut outReal)));
    }
    r.legs_done("ADD", 2);
}

const V_ADOSC: &[(&str, i32, i32)] = &[
    ("defaults", i32::MIN, i32::MIN),
    ("minimums", 2i32, 2i32),
];

fn sub_ADOSC(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastPeriod, optInSlowPeriod) in V_ADOSC {
        let lb = core.ADOSC_Lookback(optInFastPeriod, optInSlowPeriod);
        if lb == usize::MAX { continue; }
        r.control("ADOSC", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ADOSC(0, lb, &inHigh, &inLow, &inClose, &inVolume, optInFastPeriod, optInSlowPeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ADOSC", label); continue; }
        r.quiet("ADOSC", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ADOSC(0, lb - 1, &inHigh, &inLow, &inClose, &inVolume, optInFastPeriod, optInSlowPeriod, &mut outReal)
        }));
    }
}

fn legs_ADOSC(r: &mut Report) {
    let core = Core::new();
    let optInFastPeriod = i32::MIN;
    let optInSlowPeriod = i32::MIN;
    let lb = core.ADOSC_Lookback(optInFastPeriod, optInSlowPeriod);
    if lb == usize::MAX { r.no_legs("ADOSC"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADOSC", "inHigh", 0, run(|| core.ADOSC(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInFastPeriod, optInSlowPeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADOSC", "inLow", 1, run(|| core.ADOSC(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInFastPeriod, optInSlowPeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADOSC", "inClose", 2, run(|| core.ADOSC(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInFastPeriod, optInSlowPeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADOSC", "inVolume", 3, run(|| core.ADOSC(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInFastPeriod, optInSlowPeriod, &mut outReal)));
    }
    r.legs_done("ADOSC", 4);
}

const V_ADX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_ADX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_ADX {
        let lb = core.ADX_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("ADX", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ADX(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ADX", label); continue; }
        r.quiet("ADX", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ADX(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_ADX(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.ADX_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("ADX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADX", "inHigh", 0, run(|| core.ADX(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADX", "inLow", 1, run(|| core.ADX(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADX", "inClose", 2, run(|| core.ADX(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("ADX", 3);
}

const V_ADXR: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_ADXR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_ADXR {
        let lb = core.ADXR_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("ADXR", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ADXR(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ADXR", label); continue; }
        r.quiet("ADXR", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ADXR(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_ADXR(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.ADXR_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("ADXR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADXR", "inHigh", 0, run(|| core.ADXR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADXR", "inLow", 1, run(|| core.ADXR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ADXR", "inClose", 2, run(|| core.ADXR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("ADXR", 3);
}

const V_AO: &[(&str, i32, i32)] = &[
    ("defaults", i32::MIN, i32::MIN),
    ("minimums", 2i32, 2i32),
];

fn sub_AO(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastPeriod, optInSlowPeriod) in V_AO {
        let lb = core.AO_Lookback(optInFastPeriod, optInSlowPeriod);
        if lb == usize::MAX { continue; }
        r.control("AO", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AO(0, lb, &inHigh, &inLow, optInFastPeriod, optInSlowPeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("AO", label); continue; }
        r.quiet("AO", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AO(0, lb - 1, &inHigh, &inLow, optInFastPeriod, optInSlowPeriod, &mut outReal)
        }));
    }
}

fn legs_AO(r: &mut Report) {
    let core = Core::new();
    let optInFastPeriod = i32::MIN;
    let optInSlowPeriod = i32::MIN;
    let lb = core.AO_Lookback(optInFastPeriod, optInSlowPeriod);
    if lb == usize::MAX { r.no_legs("AO"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AO", "inHigh", 0, run(|| core.AO(startIdx, endIdx, &inHigh, &inLow, optInFastPeriod, optInSlowPeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AO", "inLow", 1, run(|| core.AO(startIdx, endIdx, &inHigh, &inLow, optInFastPeriod, optInSlowPeriod, &mut outReal)));
    }
    r.legs_done("AO", 2);
}

const V_APO: &[(&str, i32, i32, MAType)] = &[
    ("defaults", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("minimums", 2i32, 2i32, MAType::DEFAULT),
    ("optInMAType=SMA", i32::MIN, i32::MIN, MAType::SMA),
    ("optInMAType=SMA, periods doubled", 24i32, 52i32, MAType::SMA),
    ("optInMAType=EMA", i32::MIN, i32::MIN, MAType::EMA),
    ("optInMAType=EMA, periods doubled", 24i32, 52i32, MAType::EMA),
    ("optInMAType=WMA", i32::MIN, i32::MIN, MAType::WMA),
    ("optInMAType=WMA, periods doubled", 24i32, 52i32, MAType::WMA),
    ("optInMAType=DEMA", i32::MIN, i32::MIN, MAType::DEMA),
    ("optInMAType=DEMA, periods doubled", 24i32, 52i32, MAType::DEMA),
    ("optInMAType=TEMA", i32::MIN, i32::MIN, MAType::TEMA),
    ("optInMAType=TEMA, periods doubled", 24i32, 52i32, MAType::TEMA),
    ("optInMAType=TRIMA", i32::MIN, i32::MIN, MAType::TRIMA),
    ("optInMAType=TRIMA, periods doubled", 24i32, 52i32, MAType::TRIMA),
    ("optInMAType=KAMA", i32::MIN, i32::MIN, MAType::KAMA),
    ("optInMAType=KAMA, periods doubled", 24i32, 52i32, MAType::KAMA),
    ("optInMAType=MAMA", i32::MIN, i32::MIN, MAType::MAMA),
    ("optInMAType=MAMA, periods doubled", 24i32, 52i32, MAType::MAMA),
    ("optInMAType=T3", i32::MIN, i32::MIN, MAType::T3),
    ("optInMAType=T3, periods doubled", 24i32, 52i32, MAType::T3),
    ("optInMAType=HMA", i32::MIN, i32::MIN, MAType::HMA),
    ("optInMAType=HMA, periods doubled", 24i32, 52i32, MAType::HMA),
    ("optInMAType=DISABLED", i32::MIN, i32::MIN, MAType::DISABLED),
    ("optInMAType=DISABLED, periods doubled", 24i32, 52i32, MAType::DISABLED),
    ("optInMAType=DEFAULT", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("optInMAType=DEFAULT, periods doubled", 24i32, 52i32, MAType::DEFAULT),
];

fn sub_APO(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastPeriod, optInSlowPeriod, optInMAType) in V_APO {
        let lb = core.APO_Lookback(optInFastPeriod, optInSlowPeriod, optInMAType);
        if lb == usize::MAX { continue; }
        r.control("APO", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.APO(0, lb, &inReal, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("APO", label); continue; }
        r.quiet("APO", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.APO(0, lb - 1, &inReal, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)
        }));
    }
}

fn legs_APO(r: &mut Report) {
    let core = Core::new();
    let optInFastPeriod = i32::MIN;
    let optInSlowPeriod = i32::MIN;
    let optInMAType = MAType::DEFAULT;
    let lb = core.APO_Lookback(optInFastPeriod, optInSlowPeriod, optInMAType);
    if lb == usize::MAX { r.no_legs("APO"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("APO", "inReal", 0, run(|| core.APO(startIdx, endIdx, &inReal, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)));
    }
    r.legs_done("APO", 1);
}

const V_AROON: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_AROON(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_AROON {
        let lb = core.AROON_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("AROON", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outAroonDown: Vec<f64> = Vec::with_capacity(1);
            let mut outAroonUp: Vec<f64> = Vec::with_capacity(1);
            core.AROON(0, lb, &inHigh, &inLow, optInTimePeriod, &mut outAroonDown, &mut outAroonUp)
        }));
        if lb < 1 { r.no_quiet_range("AROON", label); continue; }
        r.quiet("AROON", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outAroonDown: Vec<f64> = Vec::with_capacity(1);
            let mut outAroonUp: Vec<f64> = Vec::with_capacity(1);
            core.AROON(0, lb - 1, &inHigh, &inLow, optInTimePeriod, &mut outAroonDown, &mut outAroonUp)
        }));
    }
}

fn legs_AROON(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.AROON_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("AROON"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outAroonDown: Vec<f64> = vec![Default::default(); 5];
        let mut outAroonUp: Vec<f64> = vec![Default::default(); 5];
        r.leg("AROON", "inHigh", 0, run(|| core.AROON(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outAroonDown, &mut outAroonUp)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outAroonDown: Vec<f64> = vec![Default::default(); 5];
        let mut outAroonUp: Vec<f64> = vec![Default::default(); 5];
        r.leg("AROON", "inLow", 1, run(|| core.AROON(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outAroonDown, &mut outAroonUp)));
    }
    r.legs_done("AROON", 2);
}

const V_AROONOSC: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_AROONOSC(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_AROONOSC {
        let lb = core.AROONOSC_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("AROONOSC", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AROONOSC(0, lb, &inHigh, &inLow, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("AROONOSC", label); continue; }
        r.quiet("AROONOSC", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AROONOSC(0, lb - 1, &inHigh, &inLow, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_AROONOSC(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.AROONOSC_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("AROONOSC"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AROONOSC", "inHigh", 0, run(|| core.AROONOSC(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AROONOSC", "inLow", 1, run(|| core.AROONOSC(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("AROONOSC", 2);
}

const V_ASIN: &[&str] = &[
    "defaults",
];

fn sub_ASIN(r: &mut Report) {
    let core = Core::new();
    for &label in V_ASIN {
        let lb = core.ASIN_Lookback();
        if lb == usize::MAX { continue; }
        r.control("ASIN", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ASIN(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ASIN", label); continue; }
        r.quiet("ASIN", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ASIN(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_ASIN(r: &mut Report) {
    let core = Core::new();
    let lb = core.ASIN_Lookback();
    if lb == usize::MAX { r.no_legs("ASIN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ASIN", "inReal", 0, run(|| core.ASIN(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("ASIN", 1);
}

const V_ATAN: &[&str] = &[
    "defaults",
];

fn sub_ATAN(r: &mut Report) {
    let core = Core::new();
    for &label in V_ATAN {
        let lb = core.ATAN_Lookback();
        if lb == usize::MAX { continue; }
        r.control("ATAN", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ATAN(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ATAN", label); continue; }
        r.quiet("ATAN", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ATAN(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_ATAN(r: &mut Report) {
    let core = Core::new();
    let lb = core.ATAN_Lookback();
    if lb == usize::MAX { r.no_legs("ATAN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ATAN", "inReal", 0, run(|| core.ATAN(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("ATAN", 1);
}

const V_ATR: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_ATR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_ATR {
        let lb = core.ATR_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("ATR", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ATR(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ATR", label); continue; }
        r.quiet("ATR", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ATR(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_ATR(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.ATR_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("ATR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ATR", "inHigh", 0, run(|| core.ATR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ATR", "inLow", 1, run(|| core.ATR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ATR", "inClose", 2, run(|| core.ATR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("ATR", 3);
}

const V_AVGDEV: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_AVGDEV(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_AVGDEV {
        let lb = core.AVGDEV_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("AVGDEV", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AVGDEV(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("AVGDEV", label); continue; }
        r.quiet("AVGDEV", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AVGDEV(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_AVGDEV(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.AVGDEV_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("AVGDEV"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AVGDEV", "inReal", 0, run(|| core.AVGDEV(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("AVGDEV", 1);
}

const V_AVGPRICE: &[&str] = &[
    "defaults",
];

fn sub_AVGPRICE(r: &mut Report) {
    let core = Core::new();
    for &label in V_AVGPRICE {
        let lb = core.AVGPRICE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("AVGPRICE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AVGPRICE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("AVGPRICE", label); continue; }
        r.quiet("AVGPRICE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.AVGPRICE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)
        }));
    }
}

fn legs_AVGPRICE(r: &mut Report) {
    let core = Core::new();
    let lb = core.AVGPRICE_Lookback();
    if lb == usize::MAX { r.no_legs("AVGPRICE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AVGPRICE", "inOpen", 0, run(|| core.AVGPRICE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AVGPRICE", "inHigh", 1, run(|| core.AVGPRICE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AVGPRICE", "inLow", 2, run(|| core.AVGPRICE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("AVGPRICE", "inClose", 3, run(|| core.AVGPRICE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    r.legs_done("AVGPRICE", 4);
}

const V_BBANDS: &[(&str, i32, f64, f64, MAType)] = &[
    ("defaults", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::DEFAULT),
    ("minimums", 2i32, -3e37f64, -3e37f64, MAType::DEFAULT),
    ("optInMAType=SMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::SMA),
    ("optInMAType=SMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::SMA),
    ("optInMAType=EMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::EMA),
    ("optInMAType=EMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::EMA),
    ("optInMAType=WMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::WMA),
    ("optInMAType=WMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::WMA),
    ("optInMAType=DEMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::DEMA),
    ("optInMAType=DEMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::DEMA),
    ("optInMAType=TEMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::TEMA),
    ("optInMAType=TEMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::TEMA),
    ("optInMAType=TRIMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::TRIMA),
    ("optInMAType=TRIMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::TRIMA),
    ("optInMAType=KAMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::KAMA),
    ("optInMAType=KAMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::KAMA),
    ("optInMAType=MAMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::MAMA),
    ("optInMAType=MAMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::MAMA),
    ("optInMAType=T3", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::T3),
    ("optInMAType=T3, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::T3),
    ("optInMAType=HMA", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::HMA),
    ("optInMAType=HMA, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::HMA),
    ("optInMAType=DISABLED", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::DISABLED),
    ("optInMAType=DISABLED, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::DISABLED),
    ("optInMAType=DEFAULT", i32::MIN, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::DEFAULT),
    ("optInMAType=DEFAULT, periods doubled", 40i32, Core::REAL_DEFAULT, Core::REAL_DEFAULT, MAType::DEFAULT),
];

fn sub_BBANDS(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod, optInNbDevUp, optInNbDevDn, optInMAType) in V_BBANDS {
        let lb = core.BBANDS_Lookback(optInTimePeriod, optInNbDevUp, optInNbDevDn, optInMAType);
        if lb == usize::MAX { continue; }
        r.control("BBANDS", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outRealUpperBand: Vec<f64> = Vec::with_capacity(1);
            let mut outRealMiddleBand: Vec<f64> = Vec::with_capacity(1);
            let mut outRealLowerBand: Vec<f64> = Vec::with_capacity(1);
            core.BBANDS(0, lb, &inReal, optInTimePeriod, optInNbDevUp, optInNbDevDn, optInMAType, &mut outRealUpperBand, &mut outRealMiddleBand, &mut outRealLowerBand)
        }));
        if lb < 1 { r.no_quiet_range("BBANDS", label); continue; }
        r.quiet("BBANDS", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outRealUpperBand: Vec<f64> = Vec::with_capacity(1);
            let mut outRealMiddleBand: Vec<f64> = Vec::with_capacity(1);
            let mut outRealLowerBand: Vec<f64> = Vec::with_capacity(1);
            core.BBANDS(0, lb - 1, &inReal, optInTimePeriod, optInNbDevUp, optInNbDevDn, optInMAType, &mut outRealUpperBand, &mut outRealMiddleBand, &mut outRealLowerBand)
        }));
    }
}

fn legs_BBANDS(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let optInNbDevUp = Core::REAL_DEFAULT;
    let optInNbDevDn = Core::REAL_DEFAULT;
    let optInMAType = MAType::DEFAULT;
    let lb = core.BBANDS_Lookback(optInTimePeriod, optInNbDevUp, optInNbDevDn, optInMAType);
    if lb == usize::MAX { r.no_legs("BBANDS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outRealUpperBand: Vec<f64> = vec![Default::default(); 5];
        let mut outRealMiddleBand: Vec<f64> = vec![Default::default(); 5];
        let mut outRealLowerBand: Vec<f64> = vec![Default::default(); 5];
        r.leg("BBANDS", "inReal", 0, run(|| core.BBANDS(startIdx, endIdx, &inReal, optInTimePeriod, optInNbDevUp, optInNbDevDn, optInMAType, &mut outRealUpperBand, &mut outRealMiddleBand, &mut outRealLowerBand)));
    }
    r.legs_done("BBANDS", 1);
}

const V_BETA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_BETA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_BETA {
        let lb = core.BETA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("BETA", label, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.BETA(0, lb, &inReal0, &inReal1, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("BETA", label); continue; }
        r.quiet("BETA", label, lb, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.BETA(0, lb - 1, &inReal0, &inReal1, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_BETA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.BETA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("BETA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal0: Vec<f64> = Vec::with_capacity(1);
        let inReal1: Vec<f64> = series("real", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("BETA", "inReal0", 0, run(|| core.BETA(startIdx, endIdx, &inReal0, &inReal1, optInTimePeriod, &mut outReal)));
    }
    {
        let inReal0: Vec<f64> = series("real", endIdx + 1);
        let inReal1: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("BETA", "inReal1", 1, run(|| core.BETA(startIdx, endIdx, &inReal0, &inReal1, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("BETA", 2);
}

const V_BOP: &[&str] = &[
    "defaults",
];

fn sub_BOP(r: &mut Report) {
    let core = Core::new();
    for &label in V_BOP {
        let lb = core.BOP_Lookback();
        if lb == usize::MAX { continue; }
        r.control("BOP", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.BOP(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("BOP", label); continue; }
        r.quiet("BOP", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.BOP(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)
        }));
    }
}

fn legs_BOP(r: &mut Report) {
    let core = Core::new();
    let lb = core.BOP_Lookback();
    if lb == usize::MAX { r.no_legs("BOP"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("BOP", "inOpen", 0, run(|| core.BOP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("BOP", "inHigh", 1, run(|| core.BOP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("BOP", "inLow", 2, run(|| core.BOP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("BOP", "inClose", 3, run(|| core.BOP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    r.legs_done("BOP", 4);
}

const V_CCI: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_CCI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_CCI {
        let lb = core.CCI_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("CCI", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CCI(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("CCI", label); continue; }
        r.quiet("CCI", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CCI(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_CCI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.CCI_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("CCI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CCI", "inHigh", 0, run(|| core.CCI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CCI", "inLow", 1, run(|| core.CCI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CCI", "inClose", 2, run(|| core.CCI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("CCI", 3);
}

const V_CDL2CROWS: &[&str] = &[
    "defaults",
];

fn sub_CDL2CROWS(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDL2CROWS {
        let lb = core.CDL2CROWS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDL2CROWS", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL2CROWS(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDL2CROWS", label); continue; }
        r.quiet("CDL2CROWS", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL2CROWS(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDL2CROWS(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDL2CROWS_Lookback();
    if lb == usize::MAX { r.no_legs("CDL2CROWS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL2CROWS", "inOpen", 0, run(|| core.CDL2CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL2CROWS", "inHigh", 1, run(|| core.CDL2CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL2CROWS", "inLow", 2, run(|| core.CDL2CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL2CROWS", "inClose", 3, run(|| core.CDL2CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDL2CROWS", 4);
}

const V_CDL3BLACKCROWS: &[&str] = &[
    "defaults",
];

fn sub_CDL3BLACKCROWS(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDL3BLACKCROWS {
        let lb = core.CDL3BLACKCROWS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDL3BLACKCROWS", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3BLACKCROWS(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDL3BLACKCROWS", label); continue; }
        r.quiet("CDL3BLACKCROWS", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3BLACKCROWS(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDL3BLACKCROWS(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDL3BLACKCROWS_Lookback();
    if lb == usize::MAX { r.no_legs("CDL3BLACKCROWS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3BLACKCROWS", "inOpen", 0, run(|| core.CDL3BLACKCROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3BLACKCROWS", "inHigh", 1, run(|| core.CDL3BLACKCROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3BLACKCROWS", "inLow", 2, run(|| core.CDL3BLACKCROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3BLACKCROWS", "inClose", 3, run(|| core.CDL3BLACKCROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDL3BLACKCROWS", 4);
}

const V_CDL3INSIDE: &[&str] = &[
    "defaults",
];

fn sub_CDL3INSIDE(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDL3INSIDE {
        let lb = core.CDL3INSIDE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDL3INSIDE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3INSIDE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDL3INSIDE", label); continue; }
        r.quiet("CDL3INSIDE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3INSIDE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDL3INSIDE(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDL3INSIDE_Lookback();
    if lb == usize::MAX { r.no_legs("CDL3INSIDE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3INSIDE", "inOpen", 0, run(|| core.CDL3INSIDE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3INSIDE", "inHigh", 1, run(|| core.CDL3INSIDE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3INSIDE", "inLow", 2, run(|| core.CDL3INSIDE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3INSIDE", "inClose", 3, run(|| core.CDL3INSIDE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDL3INSIDE", 4);
}

const V_CDL3LINESTRIKE: &[&str] = &[
    "defaults",
];

fn sub_CDL3LINESTRIKE(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDL3LINESTRIKE {
        let lb = core.CDL3LINESTRIKE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDL3LINESTRIKE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3LINESTRIKE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDL3LINESTRIKE", label); continue; }
        r.quiet("CDL3LINESTRIKE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3LINESTRIKE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDL3LINESTRIKE(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDL3LINESTRIKE_Lookback();
    if lb == usize::MAX { r.no_legs("CDL3LINESTRIKE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3LINESTRIKE", "inOpen", 0, run(|| core.CDL3LINESTRIKE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3LINESTRIKE", "inHigh", 1, run(|| core.CDL3LINESTRIKE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3LINESTRIKE", "inLow", 2, run(|| core.CDL3LINESTRIKE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3LINESTRIKE", "inClose", 3, run(|| core.CDL3LINESTRIKE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDL3LINESTRIKE", 4);
}

const V_CDL3OUTSIDE: &[&str] = &[
    "defaults",
];

fn sub_CDL3OUTSIDE(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDL3OUTSIDE {
        let lb = core.CDL3OUTSIDE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDL3OUTSIDE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3OUTSIDE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDL3OUTSIDE", label); continue; }
        r.quiet("CDL3OUTSIDE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3OUTSIDE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDL3OUTSIDE(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDL3OUTSIDE_Lookback();
    if lb == usize::MAX { r.no_legs("CDL3OUTSIDE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3OUTSIDE", "inOpen", 0, run(|| core.CDL3OUTSIDE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3OUTSIDE", "inHigh", 1, run(|| core.CDL3OUTSIDE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3OUTSIDE", "inLow", 2, run(|| core.CDL3OUTSIDE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3OUTSIDE", "inClose", 3, run(|| core.CDL3OUTSIDE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDL3OUTSIDE", 4);
}

const V_CDL3STARSINSOUTH: &[&str] = &[
    "defaults",
];

fn sub_CDL3STARSINSOUTH(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDL3STARSINSOUTH {
        let lb = core.CDL3STARSINSOUTH_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDL3STARSINSOUTH", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3STARSINSOUTH(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDL3STARSINSOUTH", label); continue; }
        r.quiet("CDL3STARSINSOUTH", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3STARSINSOUTH(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDL3STARSINSOUTH(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDL3STARSINSOUTH_Lookback();
    if lb == usize::MAX { r.no_legs("CDL3STARSINSOUTH"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3STARSINSOUTH", "inOpen", 0, run(|| core.CDL3STARSINSOUTH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3STARSINSOUTH", "inHigh", 1, run(|| core.CDL3STARSINSOUTH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3STARSINSOUTH", "inLow", 2, run(|| core.CDL3STARSINSOUTH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3STARSINSOUTH", "inClose", 3, run(|| core.CDL3STARSINSOUTH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDL3STARSINSOUTH", 4);
}

const V_CDL3WHITESOLDIERS: &[&str] = &[
    "defaults",
];

fn sub_CDL3WHITESOLDIERS(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDL3WHITESOLDIERS {
        let lb = core.CDL3WHITESOLDIERS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDL3WHITESOLDIERS", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3WHITESOLDIERS(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDL3WHITESOLDIERS", label); continue; }
        r.quiet("CDL3WHITESOLDIERS", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDL3WHITESOLDIERS(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDL3WHITESOLDIERS(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDL3WHITESOLDIERS_Lookback();
    if lb == usize::MAX { r.no_legs("CDL3WHITESOLDIERS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3WHITESOLDIERS", "inOpen", 0, run(|| core.CDL3WHITESOLDIERS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3WHITESOLDIERS", "inHigh", 1, run(|| core.CDL3WHITESOLDIERS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3WHITESOLDIERS", "inLow", 2, run(|| core.CDL3WHITESOLDIERS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDL3WHITESOLDIERS", "inClose", 3, run(|| core.CDL3WHITESOLDIERS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDL3WHITESOLDIERS", 4);
}

const V_CDLABANDONEDBABY: &[(&str, f64)] = &[
    ("defaults", Core::REAL_DEFAULT),
    ("minimums", 0.0f64),
];

fn sub_CDLABANDONEDBABY(r: &mut Report) {
    let core = Core::new();
    for &(label, optInPenetration) in V_CDLABANDONEDBABY {
        let lb = core.CDLABANDONEDBABY_Lookback(optInPenetration);
        if lb == usize::MAX { continue; }
        r.control("CDLABANDONEDBABY", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLABANDONEDBABY(0, lb, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLABANDONEDBABY", label); continue; }
        r.quiet("CDLABANDONEDBABY", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLABANDONEDBABY(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
    }
}

fn legs_CDLABANDONEDBABY(r: &mut Report) {
    let core = Core::new();
    let optInPenetration = Core::REAL_DEFAULT;
    let lb = core.CDLABANDONEDBABY_Lookback(optInPenetration);
    if lb == usize::MAX { r.no_legs("CDLABANDONEDBABY"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLABANDONEDBABY", "inOpen", 0, run(|| core.CDLABANDONEDBABY(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLABANDONEDBABY", "inHigh", 1, run(|| core.CDLABANDONEDBABY(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLABANDONEDBABY", "inLow", 2, run(|| core.CDLABANDONEDBABY(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLABANDONEDBABY", "inClose", 3, run(|| core.CDLABANDONEDBABY(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    r.legs_done("CDLABANDONEDBABY", 4);
}

const V_CDLADVANCEBLOCK: &[&str] = &[
    "defaults",
];

fn sub_CDLADVANCEBLOCK(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLADVANCEBLOCK {
        let lb = core.CDLADVANCEBLOCK_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLADVANCEBLOCK", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLADVANCEBLOCK(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLADVANCEBLOCK", label); continue; }
        r.quiet("CDLADVANCEBLOCK", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLADVANCEBLOCK(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLADVANCEBLOCK(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLADVANCEBLOCK_Lookback();
    if lb == usize::MAX { r.no_legs("CDLADVANCEBLOCK"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLADVANCEBLOCK", "inOpen", 0, run(|| core.CDLADVANCEBLOCK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLADVANCEBLOCK", "inHigh", 1, run(|| core.CDLADVANCEBLOCK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLADVANCEBLOCK", "inLow", 2, run(|| core.CDLADVANCEBLOCK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLADVANCEBLOCK", "inClose", 3, run(|| core.CDLADVANCEBLOCK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLADVANCEBLOCK", 4);
}

const V_CDLBELTHOLD: &[&str] = &[
    "defaults",
];

fn sub_CDLBELTHOLD(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLBELTHOLD {
        let lb = core.CDLBELTHOLD_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLBELTHOLD", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLBELTHOLD(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLBELTHOLD", label); continue; }
        r.quiet("CDLBELTHOLD", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLBELTHOLD(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLBELTHOLD(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLBELTHOLD_Lookback();
    if lb == usize::MAX { r.no_legs("CDLBELTHOLD"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLBELTHOLD", "inOpen", 0, run(|| core.CDLBELTHOLD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLBELTHOLD", "inHigh", 1, run(|| core.CDLBELTHOLD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLBELTHOLD", "inLow", 2, run(|| core.CDLBELTHOLD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLBELTHOLD", "inClose", 3, run(|| core.CDLBELTHOLD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLBELTHOLD", 4);
}

const V_CDLBREAKAWAY: &[&str] = &[
    "defaults",
];

fn sub_CDLBREAKAWAY(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLBREAKAWAY {
        let lb = core.CDLBREAKAWAY_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLBREAKAWAY", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLBREAKAWAY(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLBREAKAWAY", label); continue; }
        r.quiet("CDLBREAKAWAY", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLBREAKAWAY(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLBREAKAWAY(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLBREAKAWAY_Lookback();
    if lb == usize::MAX { r.no_legs("CDLBREAKAWAY"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLBREAKAWAY", "inOpen", 0, run(|| core.CDLBREAKAWAY(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLBREAKAWAY", "inHigh", 1, run(|| core.CDLBREAKAWAY(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLBREAKAWAY", "inLow", 2, run(|| core.CDLBREAKAWAY(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLBREAKAWAY", "inClose", 3, run(|| core.CDLBREAKAWAY(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLBREAKAWAY", 4);
}

const V_CDLCLOSINGMARUBOZU: &[&str] = &[
    "defaults",
];

fn sub_CDLCLOSINGMARUBOZU(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLCLOSINGMARUBOZU {
        let lb = core.CDLCLOSINGMARUBOZU_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLCLOSINGMARUBOZU", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLCLOSINGMARUBOZU(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLCLOSINGMARUBOZU", label); continue; }
        r.quiet("CDLCLOSINGMARUBOZU", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLCLOSINGMARUBOZU(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLCLOSINGMARUBOZU(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLCLOSINGMARUBOZU_Lookback();
    if lb == usize::MAX { r.no_legs("CDLCLOSINGMARUBOZU"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCLOSINGMARUBOZU", "inOpen", 0, run(|| core.CDLCLOSINGMARUBOZU(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCLOSINGMARUBOZU", "inHigh", 1, run(|| core.CDLCLOSINGMARUBOZU(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCLOSINGMARUBOZU", "inLow", 2, run(|| core.CDLCLOSINGMARUBOZU(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCLOSINGMARUBOZU", "inClose", 3, run(|| core.CDLCLOSINGMARUBOZU(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLCLOSINGMARUBOZU", 4);
}

const V_CDLCONCEALBABYSWALL: &[&str] = &[
    "defaults",
];

fn sub_CDLCONCEALBABYSWALL(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLCONCEALBABYSWALL {
        let lb = core.CDLCONCEALBABYSWALL_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLCONCEALBABYSWALL", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLCONCEALBABYSWALL(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLCONCEALBABYSWALL", label); continue; }
        r.quiet("CDLCONCEALBABYSWALL", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLCONCEALBABYSWALL(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLCONCEALBABYSWALL(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLCONCEALBABYSWALL_Lookback();
    if lb == usize::MAX { r.no_legs("CDLCONCEALBABYSWALL"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCONCEALBABYSWALL", "inOpen", 0, run(|| core.CDLCONCEALBABYSWALL(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCONCEALBABYSWALL", "inHigh", 1, run(|| core.CDLCONCEALBABYSWALL(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCONCEALBABYSWALL", "inLow", 2, run(|| core.CDLCONCEALBABYSWALL(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCONCEALBABYSWALL", "inClose", 3, run(|| core.CDLCONCEALBABYSWALL(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLCONCEALBABYSWALL", 4);
}

const V_CDLCOUNTERATTACK: &[&str] = &[
    "defaults",
];

fn sub_CDLCOUNTERATTACK(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLCOUNTERATTACK {
        let lb = core.CDLCOUNTERATTACK_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLCOUNTERATTACK", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLCOUNTERATTACK(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLCOUNTERATTACK", label); continue; }
        r.quiet("CDLCOUNTERATTACK", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLCOUNTERATTACK(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLCOUNTERATTACK(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLCOUNTERATTACK_Lookback();
    if lb == usize::MAX { r.no_legs("CDLCOUNTERATTACK"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCOUNTERATTACK", "inOpen", 0, run(|| core.CDLCOUNTERATTACK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCOUNTERATTACK", "inHigh", 1, run(|| core.CDLCOUNTERATTACK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCOUNTERATTACK", "inLow", 2, run(|| core.CDLCOUNTERATTACK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLCOUNTERATTACK", "inClose", 3, run(|| core.CDLCOUNTERATTACK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLCOUNTERATTACK", 4);
}

const V_CDLDARKCLOUDCOVER: &[(&str, f64)] = &[
    ("defaults", Core::REAL_DEFAULT),
    ("minimums", 0.0f64),
];

fn sub_CDLDARKCLOUDCOVER(r: &mut Report) {
    let core = Core::new();
    for &(label, optInPenetration) in V_CDLDARKCLOUDCOVER {
        let lb = core.CDLDARKCLOUDCOVER_Lookback(optInPenetration);
        if lb == usize::MAX { continue; }
        r.control("CDLDARKCLOUDCOVER", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLDARKCLOUDCOVER(0, lb, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLDARKCLOUDCOVER", label); continue; }
        r.quiet("CDLDARKCLOUDCOVER", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLDARKCLOUDCOVER(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
    }
}

fn legs_CDLDARKCLOUDCOVER(r: &mut Report) {
    let core = Core::new();
    let optInPenetration = Core::REAL_DEFAULT;
    let lb = core.CDLDARKCLOUDCOVER_Lookback(optInPenetration);
    if lb == usize::MAX { r.no_legs("CDLDARKCLOUDCOVER"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDARKCLOUDCOVER", "inOpen", 0, run(|| core.CDLDARKCLOUDCOVER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDARKCLOUDCOVER", "inHigh", 1, run(|| core.CDLDARKCLOUDCOVER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDARKCLOUDCOVER", "inLow", 2, run(|| core.CDLDARKCLOUDCOVER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDARKCLOUDCOVER", "inClose", 3, run(|| core.CDLDARKCLOUDCOVER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    r.legs_done("CDLDARKCLOUDCOVER", 4);
}

const V_CDLDOJI: &[&str] = &[
    "defaults",
];

fn sub_CDLDOJI(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLDOJI {
        let lb = core.CDLDOJI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLDOJI", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLDOJI(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLDOJI", label); continue; }
        r.quiet("CDLDOJI", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLDOJI(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLDOJI(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLDOJI_Lookback();
    if lb == usize::MAX { r.no_legs("CDLDOJI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDOJI", "inOpen", 0, run(|| core.CDLDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDOJI", "inHigh", 1, run(|| core.CDLDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDOJI", "inLow", 2, run(|| core.CDLDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDOJI", "inClose", 3, run(|| core.CDLDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLDOJI", 4);
}

const V_CDLDOJISTAR: &[&str] = &[
    "defaults",
];

fn sub_CDLDOJISTAR(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLDOJISTAR {
        let lb = core.CDLDOJISTAR_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLDOJISTAR", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLDOJISTAR(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLDOJISTAR", label); continue; }
        r.quiet("CDLDOJISTAR", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLDOJISTAR(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLDOJISTAR(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLDOJISTAR_Lookback();
    if lb == usize::MAX { r.no_legs("CDLDOJISTAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDOJISTAR", "inOpen", 0, run(|| core.CDLDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDOJISTAR", "inHigh", 1, run(|| core.CDLDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDOJISTAR", "inLow", 2, run(|| core.CDLDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDOJISTAR", "inClose", 3, run(|| core.CDLDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLDOJISTAR", 4);
}

const V_CDLDRAGONFLYDOJI: &[&str] = &[
    "defaults",
];

fn sub_CDLDRAGONFLYDOJI(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLDRAGONFLYDOJI {
        let lb = core.CDLDRAGONFLYDOJI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLDRAGONFLYDOJI", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLDRAGONFLYDOJI(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLDRAGONFLYDOJI", label); continue; }
        r.quiet("CDLDRAGONFLYDOJI", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLDRAGONFLYDOJI(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLDRAGONFLYDOJI(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLDRAGONFLYDOJI_Lookback();
    if lb == usize::MAX { r.no_legs("CDLDRAGONFLYDOJI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDRAGONFLYDOJI", "inOpen", 0, run(|| core.CDLDRAGONFLYDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDRAGONFLYDOJI", "inHigh", 1, run(|| core.CDLDRAGONFLYDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDRAGONFLYDOJI", "inLow", 2, run(|| core.CDLDRAGONFLYDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLDRAGONFLYDOJI", "inClose", 3, run(|| core.CDLDRAGONFLYDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLDRAGONFLYDOJI", 4);
}

const V_CDLENGULFING: &[&str] = &[
    "defaults",
];

fn sub_CDLENGULFING(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLENGULFING {
        let lb = core.CDLENGULFING_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLENGULFING", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLENGULFING(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLENGULFING", label); continue; }
        r.quiet("CDLENGULFING", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLENGULFING(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLENGULFING(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLENGULFING_Lookback();
    if lb == usize::MAX { r.no_legs("CDLENGULFING"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLENGULFING", "inOpen", 0, run(|| core.CDLENGULFING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLENGULFING", "inHigh", 1, run(|| core.CDLENGULFING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLENGULFING", "inLow", 2, run(|| core.CDLENGULFING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLENGULFING", "inClose", 3, run(|| core.CDLENGULFING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLENGULFING", 4);
}

const V_CDLEVENINGDOJISTAR: &[(&str, f64)] = &[
    ("defaults", Core::REAL_DEFAULT),
    ("minimums", 0.0f64),
];

fn sub_CDLEVENINGDOJISTAR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInPenetration) in V_CDLEVENINGDOJISTAR {
        let lb = core.CDLEVENINGDOJISTAR_Lookback(optInPenetration);
        if lb == usize::MAX { continue; }
        r.control("CDLEVENINGDOJISTAR", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLEVENINGDOJISTAR(0, lb, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLEVENINGDOJISTAR", label); continue; }
        r.quiet("CDLEVENINGDOJISTAR", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLEVENINGDOJISTAR(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
    }
}

fn legs_CDLEVENINGDOJISTAR(r: &mut Report) {
    let core = Core::new();
    let optInPenetration = Core::REAL_DEFAULT;
    let lb = core.CDLEVENINGDOJISTAR_Lookback(optInPenetration);
    if lb == usize::MAX { r.no_legs("CDLEVENINGDOJISTAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLEVENINGDOJISTAR", "inOpen", 0, run(|| core.CDLEVENINGDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLEVENINGDOJISTAR", "inHigh", 1, run(|| core.CDLEVENINGDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLEVENINGDOJISTAR", "inLow", 2, run(|| core.CDLEVENINGDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLEVENINGDOJISTAR", "inClose", 3, run(|| core.CDLEVENINGDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    r.legs_done("CDLEVENINGDOJISTAR", 4);
}

const V_CDLEVENINGSTAR: &[(&str, f64)] = &[
    ("defaults", Core::REAL_DEFAULT),
    ("minimums", 0.0f64),
];

fn sub_CDLEVENINGSTAR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInPenetration) in V_CDLEVENINGSTAR {
        let lb = core.CDLEVENINGSTAR_Lookback(optInPenetration);
        if lb == usize::MAX { continue; }
        r.control("CDLEVENINGSTAR", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLEVENINGSTAR(0, lb, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLEVENINGSTAR", label); continue; }
        r.quiet("CDLEVENINGSTAR", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLEVENINGSTAR(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
    }
}

fn legs_CDLEVENINGSTAR(r: &mut Report) {
    let core = Core::new();
    let optInPenetration = Core::REAL_DEFAULT;
    let lb = core.CDLEVENINGSTAR_Lookback(optInPenetration);
    if lb == usize::MAX { r.no_legs("CDLEVENINGSTAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLEVENINGSTAR", "inOpen", 0, run(|| core.CDLEVENINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLEVENINGSTAR", "inHigh", 1, run(|| core.CDLEVENINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLEVENINGSTAR", "inLow", 2, run(|| core.CDLEVENINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLEVENINGSTAR", "inClose", 3, run(|| core.CDLEVENINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    r.legs_done("CDLEVENINGSTAR", 4);
}

const V_CDLGAPSIDESIDEWHITE: &[&str] = &[
    "defaults",
];

fn sub_CDLGAPSIDESIDEWHITE(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLGAPSIDESIDEWHITE {
        let lb = core.CDLGAPSIDESIDEWHITE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLGAPSIDESIDEWHITE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLGAPSIDESIDEWHITE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLGAPSIDESIDEWHITE", label); continue; }
        r.quiet("CDLGAPSIDESIDEWHITE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLGAPSIDESIDEWHITE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLGAPSIDESIDEWHITE(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLGAPSIDESIDEWHITE_Lookback();
    if lb == usize::MAX { r.no_legs("CDLGAPSIDESIDEWHITE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLGAPSIDESIDEWHITE", "inOpen", 0, run(|| core.CDLGAPSIDESIDEWHITE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLGAPSIDESIDEWHITE", "inHigh", 1, run(|| core.CDLGAPSIDESIDEWHITE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLGAPSIDESIDEWHITE", "inLow", 2, run(|| core.CDLGAPSIDESIDEWHITE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLGAPSIDESIDEWHITE", "inClose", 3, run(|| core.CDLGAPSIDESIDEWHITE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLGAPSIDESIDEWHITE", 4);
}

const V_CDLGRAVESTONEDOJI: &[&str] = &[
    "defaults",
];

fn sub_CDLGRAVESTONEDOJI(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLGRAVESTONEDOJI {
        let lb = core.CDLGRAVESTONEDOJI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLGRAVESTONEDOJI", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLGRAVESTONEDOJI(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLGRAVESTONEDOJI", label); continue; }
        r.quiet("CDLGRAVESTONEDOJI", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLGRAVESTONEDOJI(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLGRAVESTONEDOJI(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLGRAVESTONEDOJI_Lookback();
    if lb == usize::MAX { r.no_legs("CDLGRAVESTONEDOJI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLGRAVESTONEDOJI", "inOpen", 0, run(|| core.CDLGRAVESTONEDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLGRAVESTONEDOJI", "inHigh", 1, run(|| core.CDLGRAVESTONEDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLGRAVESTONEDOJI", "inLow", 2, run(|| core.CDLGRAVESTONEDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLGRAVESTONEDOJI", "inClose", 3, run(|| core.CDLGRAVESTONEDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLGRAVESTONEDOJI", 4);
}

const V_CDLHAMMER: &[&str] = &[
    "defaults",
];

fn sub_CDLHAMMER(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLHAMMER {
        let lb = core.CDLHAMMER_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLHAMMER", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHAMMER(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLHAMMER", label); continue; }
        r.quiet("CDLHAMMER", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHAMMER(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLHAMMER(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLHAMMER_Lookback();
    if lb == usize::MAX { r.no_legs("CDLHAMMER"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHAMMER", "inOpen", 0, run(|| core.CDLHAMMER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHAMMER", "inHigh", 1, run(|| core.CDLHAMMER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHAMMER", "inLow", 2, run(|| core.CDLHAMMER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHAMMER", "inClose", 3, run(|| core.CDLHAMMER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLHAMMER", 4);
}

const V_CDLHANGINGMAN: &[&str] = &[
    "defaults",
];

fn sub_CDLHANGINGMAN(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLHANGINGMAN {
        let lb = core.CDLHANGINGMAN_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLHANGINGMAN", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHANGINGMAN(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLHANGINGMAN", label); continue; }
        r.quiet("CDLHANGINGMAN", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHANGINGMAN(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLHANGINGMAN(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLHANGINGMAN_Lookback();
    if lb == usize::MAX { r.no_legs("CDLHANGINGMAN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHANGINGMAN", "inOpen", 0, run(|| core.CDLHANGINGMAN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHANGINGMAN", "inHigh", 1, run(|| core.CDLHANGINGMAN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHANGINGMAN", "inLow", 2, run(|| core.CDLHANGINGMAN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHANGINGMAN", "inClose", 3, run(|| core.CDLHANGINGMAN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLHANGINGMAN", 4);
}

const V_CDLHARAMI: &[&str] = &[
    "defaults",
];

fn sub_CDLHARAMI(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLHARAMI {
        let lb = core.CDLHARAMI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLHARAMI", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHARAMI(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLHARAMI", label); continue; }
        r.quiet("CDLHARAMI", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHARAMI(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLHARAMI(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLHARAMI_Lookback();
    if lb == usize::MAX { r.no_legs("CDLHARAMI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHARAMI", "inOpen", 0, run(|| core.CDLHARAMI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHARAMI", "inHigh", 1, run(|| core.CDLHARAMI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHARAMI", "inLow", 2, run(|| core.CDLHARAMI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHARAMI", "inClose", 3, run(|| core.CDLHARAMI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLHARAMI", 4);
}

const V_CDLHARAMICROSS: &[&str] = &[
    "defaults",
];

fn sub_CDLHARAMICROSS(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLHARAMICROSS {
        let lb = core.CDLHARAMICROSS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLHARAMICROSS", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHARAMICROSS(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLHARAMICROSS", label); continue; }
        r.quiet("CDLHARAMICROSS", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHARAMICROSS(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLHARAMICROSS(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLHARAMICROSS_Lookback();
    if lb == usize::MAX { r.no_legs("CDLHARAMICROSS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHARAMICROSS", "inOpen", 0, run(|| core.CDLHARAMICROSS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHARAMICROSS", "inHigh", 1, run(|| core.CDLHARAMICROSS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHARAMICROSS", "inLow", 2, run(|| core.CDLHARAMICROSS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHARAMICROSS", "inClose", 3, run(|| core.CDLHARAMICROSS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLHARAMICROSS", 4);
}

const V_CDLHIGHWAVE: &[&str] = &[
    "defaults",
];

fn sub_CDLHIGHWAVE(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLHIGHWAVE {
        let lb = core.CDLHIGHWAVE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLHIGHWAVE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHIGHWAVE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLHIGHWAVE", label); continue; }
        r.quiet("CDLHIGHWAVE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHIGHWAVE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLHIGHWAVE(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLHIGHWAVE_Lookback();
    if lb == usize::MAX { r.no_legs("CDLHIGHWAVE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIGHWAVE", "inOpen", 0, run(|| core.CDLHIGHWAVE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIGHWAVE", "inHigh", 1, run(|| core.CDLHIGHWAVE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIGHWAVE", "inLow", 2, run(|| core.CDLHIGHWAVE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIGHWAVE", "inClose", 3, run(|| core.CDLHIGHWAVE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLHIGHWAVE", 4);
}

const V_CDLHIKKAKE: &[&str] = &[
    "defaults",
];

fn sub_CDLHIKKAKE(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLHIKKAKE {
        let lb = core.CDLHIKKAKE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLHIKKAKE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHIKKAKE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLHIKKAKE", label); continue; }
        r.quiet("CDLHIKKAKE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHIKKAKE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLHIKKAKE(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLHIKKAKE_Lookback();
    if lb == usize::MAX { r.no_legs("CDLHIKKAKE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIKKAKE", "inOpen", 0, run(|| core.CDLHIKKAKE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIKKAKE", "inHigh", 1, run(|| core.CDLHIKKAKE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIKKAKE", "inLow", 2, run(|| core.CDLHIKKAKE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIKKAKE", "inClose", 3, run(|| core.CDLHIKKAKE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLHIKKAKE", 4);
}

const V_CDLHIKKAKEMOD: &[&str] = &[
    "defaults",
];

fn sub_CDLHIKKAKEMOD(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLHIKKAKEMOD {
        let lb = core.CDLHIKKAKEMOD_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLHIKKAKEMOD", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHIKKAKEMOD(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLHIKKAKEMOD", label); continue; }
        r.quiet("CDLHIKKAKEMOD", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHIKKAKEMOD(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLHIKKAKEMOD(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLHIKKAKEMOD_Lookback();
    if lb == usize::MAX { r.no_legs("CDLHIKKAKEMOD"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIKKAKEMOD", "inOpen", 0, run(|| core.CDLHIKKAKEMOD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIKKAKEMOD", "inHigh", 1, run(|| core.CDLHIKKAKEMOD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIKKAKEMOD", "inLow", 2, run(|| core.CDLHIKKAKEMOD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHIKKAKEMOD", "inClose", 3, run(|| core.CDLHIKKAKEMOD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLHIKKAKEMOD", 4);
}

const V_CDLHOMINGPIGEON: &[&str] = &[
    "defaults",
];

fn sub_CDLHOMINGPIGEON(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLHOMINGPIGEON {
        let lb = core.CDLHOMINGPIGEON_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLHOMINGPIGEON", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHOMINGPIGEON(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLHOMINGPIGEON", label); continue; }
        r.quiet("CDLHOMINGPIGEON", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLHOMINGPIGEON(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLHOMINGPIGEON(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLHOMINGPIGEON_Lookback();
    if lb == usize::MAX { r.no_legs("CDLHOMINGPIGEON"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHOMINGPIGEON", "inOpen", 0, run(|| core.CDLHOMINGPIGEON(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHOMINGPIGEON", "inHigh", 1, run(|| core.CDLHOMINGPIGEON(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHOMINGPIGEON", "inLow", 2, run(|| core.CDLHOMINGPIGEON(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLHOMINGPIGEON", "inClose", 3, run(|| core.CDLHOMINGPIGEON(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLHOMINGPIGEON", 4);
}

const V_CDLIDENTICAL3CROWS: &[&str] = &[
    "defaults",
];

fn sub_CDLIDENTICAL3CROWS(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLIDENTICAL3CROWS {
        let lb = core.CDLIDENTICAL3CROWS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLIDENTICAL3CROWS", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLIDENTICAL3CROWS(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLIDENTICAL3CROWS", label); continue; }
        r.quiet("CDLIDENTICAL3CROWS", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLIDENTICAL3CROWS(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLIDENTICAL3CROWS(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLIDENTICAL3CROWS_Lookback();
    if lb == usize::MAX { r.no_legs("CDLIDENTICAL3CROWS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLIDENTICAL3CROWS", "inOpen", 0, run(|| core.CDLIDENTICAL3CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLIDENTICAL3CROWS", "inHigh", 1, run(|| core.CDLIDENTICAL3CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLIDENTICAL3CROWS", "inLow", 2, run(|| core.CDLIDENTICAL3CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLIDENTICAL3CROWS", "inClose", 3, run(|| core.CDLIDENTICAL3CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLIDENTICAL3CROWS", 4);
}

const V_CDLINNECK: &[&str] = &[
    "defaults",
];

fn sub_CDLINNECK(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLINNECK {
        let lb = core.CDLINNECK_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLINNECK", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLINNECK(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLINNECK", label); continue; }
        r.quiet("CDLINNECK", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLINNECK(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLINNECK(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLINNECK_Lookback();
    if lb == usize::MAX { r.no_legs("CDLINNECK"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLINNECK", "inOpen", 0, run(|| core.CDLINNECK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLINNECK", "inHigh", 1, run(|| core.CDLINNECK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLINNECK", "inLow", 2, run(|| core.CDLINNECK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLINNECK", "inClose", 3, run(|| core.CDLINNECK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLINNECK", 4);
}

const V_CDLINVERTEDHAMMER: &[&str] = &[
    "defaults",
];

fn sub_CDLINVERTEDHAMMER(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLINVERTEDHAMMER {
        let lb = core.CDLINVERTEDHAMMER_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLINVERTEDHAMMER", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLINVERTEDHAMMER(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLINVERTEDHAMMER", label); continue; }
        r.quiet("CDLINVERTEDHAMMER", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLINVERTEDHAMMER(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLINVERTEDHAMMER(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLINVERTEDHAMMER_Lookback();
    if lb == usize::MAX { r.no_legs("CDLINVERTEDHAMMER"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLINVERTEDHAMMER", "inOpen", 0, run(|| core.CDLINVERTEDHAMMER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLINVERTEDHAMMER", "inHigh", 1, run(|| core.CDLINVERTEDHAMMER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLINVERTEDHAMMER", "inLow", 2, run(|| core.CDLINVERTEDHAMMER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLINVERTEDHAMMER", "inClose", 3, run(|| core.CDLINVERTEDHAMMER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLINVERTEDHAMMER", 4);
}

const V_CDLKICKING: &[&str] = &[
    "defaults",
];

fn sub_CDLKICKING(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLKICKING {
        let lb = core.CDLKICKING_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLKICKING", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLKICKING(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLKICKING", label); continue; }
        r.quiet("CDLKICKING", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLKICKING(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLKICKING(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLKICKING_Lookback();
    if lb == usize::MAX { r.no_legs("CDLKICKING"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLKICKING", "inOpen", 0, run(|| core.CDLKICKING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLKICKING", "inHigh", 1, run(|| core.CDLKICKING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLKICKING", "inLow", 2, run(|| core.CDLKICKING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLKICKING", "inClose", 3, run(|| core.CDLKICKING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLKICKING", 4);
}

const V_CDLKICKINGBYLENGTH: &[&str] = &[
    "defaults",
];

fn sub_CDLKICKINGBYLENGTH(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLKICKINGBYLENGTH {
        let lb = core.CDLKICKINGBYLENGTH_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLKICKINGBYLENGTH", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLKICKINGBYLENGTH(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLKICKINGBYLENGTH", label); continue; }
        r.quiet("CDLKICKINGBYLENGTH", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLKICKINGBYLENGTH(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLKICKINGBYLENGTH(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLKICKINGBYLENGTH_Lookback();
    if lb == usize::MAX { r.no_legs("CDLKICKINGBYLENGTH"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLKICKINGBYLENGTH", "inOpen", 0, run(|| core.CDLKICKINGBYLENGTH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLKICKINGBYLENGTH", "inHigh", 1, run(|| core.CDLKICKINGBYLENGTH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLKICKINGBYLENGTH", "inLow", 2, run(|| core.CDLKICKINGBYLENGTH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLKICKINGBYLENGTH", "inClose", 3, run(|| core.CDLKICKINGBYLENGTH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLKICKINGBYLENGTH", 4);
}

const V_CDLLADDERBOTTOM: &[&str] = &[
    "defaults",
];

fn sub_CDLLADDERBOTTOM(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLLADDERBOTTOM {
        let lb = core.CDLLADDERBOTTOM_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLLADDERBOTTOM", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLLADDERBOTTOM(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLLADDERBOTTOM", label); continue; }
        r.quiet("CDLLADDERBOTTOM", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLLADDERBOTTOM(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLLADDERBOTTOM(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLLADDERBOTTOM_Lookback();
    if lb == usize::MAX { r.no_legs("CDLLADDERBOTTOM"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLADDERBOTTOM", "inOpen", 0, run(|| core.CDLLADDERBOTTOM(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLADDERBOTTOM", "inHigh", 1, run(|| core.CDLLADDERBOTTOM(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLADDERBOTTOM", "inLow", 2, run(|| core.CDLLADDERBOTTOM(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLADDERBOTTOM", "inClose", 3, run(|| core.CDLLADDERBOTTOM(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLLADDERBOTTOM", 4);
}

const V_CDLLONGLEGGEDDOJI: &[&str] = &[
    "defaults",
];

fn sub_CDLLONGLEGGEDDOJI(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLLONGLEGGEDDOJI {
        let lb = core.CDLLONGLEGGEDDOJI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLLONGLEGGEDDOJI", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLLONGLEGGEDDOJI(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLLONGLEGGEDDOJI", label); continue; }
        r.quiet("CDLLONGLEGGEDDOJI", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLLONGLEGGEDDOJI(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLLONGLEGGEDDOJI(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLLONGLEGGEDDOJI_Lookback();
    if lb == usize::MAX { r.no_legs("CDLLONGLEGGEDDOJI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLONGLEGGEDDOJI", "inOpen", 0, run(|| core.CDLLONGLEGGEDDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLONGLEGGEDDOJI", "inHigh", 1, run(|| core.CDLLONGLEGGEDDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLONGLEGGEDDOJI", "inLow", 2, run(|| core.CDLLONGLEGGEDDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLONGLEGGEDDOJI", "inClose", 3, run(|| core.CDLLONGLEGGEDDOJI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLLONGLEGGEDDOJI", 4);
}

const V_CDLLONGLINE: &[&str] = &[
    "defaults",
];

fn sub_CDLLONGLINE(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLLONGLINE {
        let lb = core.CDLLONGLINE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLLONGLINE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLLONGLINE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLLONGLINE", label); continue; }
        r.quiet("CDLLONGLINE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLLONGLINE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLLONGLINE(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLLONGLINE_Lookback();
    if lb == usize::MAX { r.no_legs("CDLLONGLINE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLONGLINE", "inOpen", 0, run(|| core.CDLLONGLINE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLONGLINE", "inHigh", 1, run(|| core.CDLLONGLINE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLONGLINE", "inLow", 2, run(|| core.CDLLONGLINE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLLONGLINE", "inClose", 3, run(|| core.CDLLONGLINE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLLONGLINE", 4);
}

const V_CDLMARUBOZU: &[&str] = &[
    "defaults",
];

fn sub_CDLMARUBOZU(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLMARUBOZU {
        let lb = core.CDLMARUBOZU_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLMARUBOZU", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMARUBOZU(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLMARUBOZU", label); continue; }
        r.quiet("CDLMARUBOZU", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMARUBOZU(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLMARUBOZU(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLMARUBOZU_Lookback();
    if lb == usize::MAX { r.no_legs("CDLMARUBOZU"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMARUBOZU", "inOpen", 0, run(|| core.CDLMARUBOZU(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMARUBOZU", "inHigh", 1, run(|| core.CDLMARUBOZU(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMARUBOZU", "inLow", 2, run(|| core.CDLMARUBOZU(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMARUBOZU", "inClose", 3, run(|| core.CDLMARUBOZU(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLMARUBOZU", 4);
}

const V_CDLMATCHINGLOW: &[&str] = &[
    "defaults",
];

fn sub_CDLMATCHINGLOW(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLMATCHINGLOW {
        let lb = core.CDLMATCHINGLOW_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLMATCHINGLOW", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMATCHINGLOW(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLMATCHINGLOW", label); continue; }
        r.quiet("CDLMATCHINGLOW", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMATCHINGLOW(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLMATCHINGLOW(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLMATCHINGLOW_Lookback();
    if lb == usize::MAX { r.no_legs("CDLMATCHINGLOW"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMATCHINGLOW", "inOpen", 0, run(|| core.CDLMATCHINGLOW(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMATCHINGLOW", "inHigh", 1, run(|| core.CDLMATCHINGLOW(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMATCHINGLOW", "inLow", 2, run(|| core.CDLMATCHINGLOW(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMATCHINGLOW", "inClose", 3, run(|| core.CDLMATCHINGLOW(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLMATCHINGLOW", 4);
}

const V_CDLMATHOLD: &[(&str, f64)] = &[
    ("defaults", Core::REAL_DEFAULT),
    ("minimums", 0.0f64),
];

fn sub_CDLMATHOLD(r: &mut Report) {
    let core = Core::new();
    for &(label, optInPenetration) in V_CDLMATHOLD {
        let lb = core.CDLMATHOLD_Lookback(optInPenetration);
        if lb == usize::MAX { continue; }
        r.control("CDLMATHOLD", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMATHOLD(0, lb, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLMATHOLD", label); continue; }
        r.quiet("CDLMATHOLD", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMATHOLD(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
    }
}

fn legs_CDLMATHOLD(r: &mut Report) {
    let core = Core::new();
    let optInPenetration = Core::REAL_DEFAULT;
    let lb = core.CDLMATHOLD_Lookback(optInPenetration);
    if lb == usize::MAX { r.no_legs("CDLMATHOLD"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMATHOLD", "inOpen", 0, run(|| core.CDLMATHOLD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMATHOLD", "inHigh", 1, run(|| core.CDLMATHOLD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMATHOLD", "inLow", 2, run(|| core.CDLMATHOLD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMATHOLD", "inClose", 3, run(|| core.CDLMATHOLD(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    r.legs_done("CDLMATHOLD", 4);
}

const V_CDLMORNINGDOJISTAR: &[(&str, f64)] = &[
    ("defaults", Core::REAL_DEFAULT),
    ("minimums", 0.0f64),
];

fn sub_CDLMORNINGDOJISTAR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInPenetration) in V_CDLMORNINGDOJISTAR {
        let lb = core.CDLMORNINGDOJISTAR_Lookback(optInPenetration);
        if lb == usize::MAX { continue; }
        r.control("CDLMORNINGDOJISTAR", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMORNINGDOJISTAR(0, lb, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLMORNINGDOJISTAR", label); continue; }
        r.quiet("CDLMORNINGDOJISTAR", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMORNINGDOJISTAR(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
    }
}

fn legs_CDLMORNINGDOJISTAR(r: &mut Report) {
    let core = Core::new();
    let optInPenetration = Core::REAL_DEFAULT;
    let lb = core.CDLMORNINGDOJISTAR_Lookback(optInPenetration);
    if lb == usize::MAX { r.no_legs("CDLMORNINGDOJISTAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMORNINGDOJISTAR", "inOpen", 0, run(|| core.CDLMORNINGDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMORNINGDOJISTAR", "inHigh", 1, run(|| core.CDLMORNINGDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMORNINGDOJISTAR", "inLow", 2, run(|| core.CDLMORNINGDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMORNINGDOJISTAR", "inClose", 3, run(|| core.CDLMORNINGDOJISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    r.legs_done("CDLMORNINGDOJISTAR", 4);
}

const V_CDLMORNINGSTAR: &[(&str, f64)] = &[
    ("defaults", Core::REAL_DEFAULT),
    ("minimums", 0.0f64),
];

fn sub_CDLMORNINGSTAR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInPenetration) in V_CDLMORNINGSTAR {
        let lb = core.CDLMORNINGSTAR_Lookback(optInPenetration);
        if lb == usize::MAX { continue; }
        r.control("CDLMORNINGSTAR", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMORNINGSTAR(0, lb, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLMORNINGSTAR", label); continue; }
        r.quiet("CDLMORNINGSTAR", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLMORNINGSTAR(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)
        }));
    }
}

fn legs_CDLMORNINGSTAR(r: &mut Report) {
    let core = Core::new();
    let optInPenetration = Core::REAL_DEFAULT;
    let lb = core.CDLMORNINGSTAR_Lookback(optInPenetration);
    if lb == usize::MAX { r.no_legs("CDLMORNINGSTAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMORNINGSTAR", "inOpen", 0, run(|| core.CDLMORNINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMORNINGSTAR", "inHigh", 1, run(|| core.CDLMORNINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMORNINGSTAR", "inLow", 2, run(|| core.CDLMORNINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLMORNINGSTAR", "inClose", 3, run(|| core.CDLMORNINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, optInPenetration, &mut outInteger)));
    }
    r.legs_done("CDLMORNINGSTAR", 4);
}

const V_CDLONNECK: &[&str] = &[
    "defaults",
];

fn sub_CDLONNECK(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLONNECK {
        let lb = core.CDLONNECK_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLONNECK", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLONNECK(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLONNECK", label); continue; }
        r.quiet("CDLONNECK", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLONNECK(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLONNECK(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLONNECK_Lookback();
    if lb == usize::MAX { r.no_legs("CDLONNECK"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLONNECK", "inOpen", 0, run(|| core.CDLONNECK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLONNECK", "inHigh", 1, run(|| core.CDLONNECK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLONNECK", "inLow", 2, run(|| core.CDLONNECK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLONNECK", "inClose", 3, run(|| core.CDLONNECK(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLONNECK", 4);
}

const V_CDLPIERCING: &[&str] = &[
    "defaults",
];

fn sub_CDLPIERCING(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLPIERCING {
        let lb = core.CDLPIERCING_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLPIERCING", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLPIERCING(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLPIERCING", label); continue; }
        r.quiet("CDLPIERCING", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLPIERCING(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLPIERCING(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLPIERCING_Lookback();
    if lb == usize::MAX { r.no_legs("CDLPIERCING"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLPIERCING", "inOpen", 0, run(|| core.CDLPIERCING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLPIERCING", "inHigh", 1, run(|| core.CDLPIERCING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLPIERCING", "inLow", 2, run(|| core.CDLPIERCING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLPIERCING", "inClose", 3, run(|| core.CDLPIERCING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLPIERCING", 4);
}

const V_CDLRICKSHAWMAN: &[&str] = &[
    "defaults",
];

fn sub_CDLRICKSHAWMAN(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLRICKSHAWMAN {
        let lb = core.CDLRICKSHAWMAN_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLRICKSHAWMAN", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLRICKSHAWMAN(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLRICKSHAWMAN", label); continue; }
        r.quiet("CDLRICKSHAWMAN", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLRICKSHAWMAN(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLRICKSHAWMAN(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLRICKSHAWMAN_Lookback();
    if lb == usize::MAX { r.no_legs("CDLRICKSHAWMAN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLRICKSHAWMAN", "inOpen", 0, run(|| core.CDLRICKSHAWMAN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLRICKSHAWMAN", "inHigh", 1, run(|| core.CDLRICKSHAWMAN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLRICKSHAWMAN", "inLow", 2, run(|| core.CDLRICKSHAWMAN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLRICKSHAWMAN", "inClose", 3, run(|| core.CDLRICKSHAWMAN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLRICKSHAWMAN", 4);
}

const V_CDLRISEFALL3METHODS: &[&str] = &[
    "defaults",
];

fn sub_CDLRISEFALL3METHODS(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLRISEFALL3METHODS {
        let lb = core.CDLRISEFALL3METHODS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLRISEFALL3METHODS", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLRISEFALL3METHODS(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLRISEFALL3METHODS", label); continue; }
        r.quiet("CDLRISEFALL3METHODS", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLRISEFALL3METHODS(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLRISEFALL3METHODS(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLRISEFALL3METHODS_Lookback();
    if lb == usize::MAX { r.no_legs("CDLRISEFALL3METHODS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLRISEFALL3METHODS", "inOpen", 0, run(|| core.CDLRISEFALL3METHODS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLRISEFALL3METHODS", "inHigh", 1, run(|| core.CDLRISEFALL3METHODS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLRISEFALL3METHODS", "inLow", 2, run(|| core.CDLRISEFALL3METHODS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLRISEFALL3METHODS", "inClose", 3, run(|| core.CDLRISEFALL3METHODS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLRISEFALL3METHODS", 4);
}

const V_CDLSEPARATINGLINES: &[&str] = &[
    "defaults",
];

fn sub_CDLSEPARATINGLINES(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLSEPARATINGLINES {
        let lb = core.CDLSEPARATINGLINES_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLSEPARATINGLINES", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSEPARATINGLINES(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLSEPARATINGLINES", label); continue; }
        r.quiet("CDLSEPARATINGLINES", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSEPARATINGLINES(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLSEPARATINGLINES(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLSEPARATINGLINES_Lookback();
    if lb == usize::MAX { r.no_legs("CDLSEPARATINGLINES"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSEPARATINGLINES", "inOpen", 0, run(|| core.CDLSEPARATINGLINES(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSEPARATINGLINES", "inHigh", 1, run(|| core.CDLSEPARATINGLINES(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSEPARATINGLINES", "inLow", 2, run(|| core.CDLSEPARATINGLINES(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSEPARATINGLINES", "inClose", 3, run(|| core.CDLSEPARATINGLINES(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLSEPARATINGLINES", 4);
}

const V_CDLSHOOTINGSTAR: &[&str] = &[
    "defaults",
];

fn sub_CDLSHOOTINGSTAR(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLSHOOTINGSTAR {
        let lb = core.CDLSHOOTINGSTAR_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLSHOOTINGSTAR", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSHOOTINGSTAR(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLSHOOTINGSTAR", label); continue; }
        r.quiet("CDLSHOOTINGSTAR", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSHOOTINGSTAR(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLSHOOTINGSTAR(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLSHOOTINGSTAR_Lookback();
    if lb == usize::MAX { r.no_legs("CDLSHOOTINGSTAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSHOOTINGSTAR", "inOpen", 0, run(|| core.CDLSHOOTINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSHOOTINGSTAR", "inHigh", 1, run(|| core.CDLSHOOTINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSHOOTINGSTAR", "inLow", 2, run(|| core.CDLSHOOTINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSHOOTINGSTAR", "inClose", 3, run(|| core.CDLSHOOTINGSTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLSHOOTINGSTAR", 4);
}

const V_CDLSHORTLINE: &[&str] = &[
    "defaults",
];

fn sub_CDLSHORTLINE(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLSHORTLINE {
        let lb = core.CDLSHORTLINE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLSHORTLINE", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSHORTLINE(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLSHORTLINE", label); continue; }
        r.quiet("CDLSHORTLINE", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSHORTLINE(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLSHORTLINE(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLSHORTLINE_Lookback();
    if lb == usize::MAX { r.no_legs("CDLSHORTLINE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSHORTLINE", "inOpen", 0, run(|| core.CDLSHORTLINE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSHORTLINE", "inHigh", 1, run(|| core.CDLSHORTLINE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSHORTLINE", "inLow", 2, run(|| core.CDLSHORTLINE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSHORTLINE", "inClose", 3, run(|| core.CDLSHORTLINE(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLSHORTLINE", 4);
}

const V_CDLSPINNINGTOP: &[&str] = &[
    "defaults",
];

fn sub_CDLSPINNINGTOP(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLSPINNINGTOP {
        let lb = core.CDLSPINNINGTOP_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLSPINNINGTOP", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSPINNINGTOP(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLSPINNINGTOP", label); continue; }
        r.quiet("CDLSPINNINGTOP", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSPINNINGTOP(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLSPINNINGTOP(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLSPINNINGTOP_Lookback();
    if lb == usize::MAX { r.no_legs("CDLSPINNINGTOP"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSPINNINGTOP", "inOpen", 0, run(|| core.CDLSPINNINGTOP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSPINNINGTOP", "inHigh", 1, run(|| core.CDLSPINNINGTOP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSPINNINGTOP", "inLow", 2, run(|| core.CDLSPINNINGTOP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSPINNINGTOP", "inClose", 3, run(|| core.CDLSPINNINGTOP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLSPINNINGTOP", 4);
}

const V_CDLSTALLEDPATTERN: &[&str] = &[
    "defaults",
];

fn sub_CDLSTALLEDPATTERN(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLSTALLEDPATTERN {
        let lb = core.CDLSTALLEDPATTERN_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLSTALLEDPATTERN", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSTALLEDPATTERN(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLSTALLEDPATTERN", label); continue; }
        r.quiet("CDLSTALLEDPATTERN", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSTALLEDPATTERN(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLSTALLEDPATTERN(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLSTALLEDPATTERN_Lookback();
    if lb == usize::MAX { r.no_legs("CDLSTALLEDPATTERN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSTALLEDPATTERN", "inOpen", 0, run(|| core.CDLSTALLEDPATTERN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSTALLEDPATTERN", "inHigh", 1, run(|| core.CDLSTALLEDPATTERN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSTALLEDPATTERN", "inLow", 2, run(|| core.CDLSTALLEDPATTERN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSTALLEDPATTERN", "inClose", 3, run(|| core.CDLSTALLEDPATTERN(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLSTALLEDPATTERN", 4);
}

const V_CDLSTICKSANDWICH: &[&str] = &[
    "defaults",
];

fn sub_CDLSTICKSANDWICH(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLSTICKSANDWICH {
        let lb = core.CDLSTICKSANDWICH_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLSTICKSANDWICH", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSTICKSANDWICH(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLSTICKSANDWICH", label); continue; }
        r.quiet("CDLSTICKSANDWICH", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLSTICKSANDWICH(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLSTICKSANDWICH(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLSTICKSANDWICH_Lookback();
    if lb == usize::MAX { r.no_legs("CDLSTICKSANDWICH"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSTICKSANDWICH", "inOpen", 0, run(|| core.CDLSTICKSANDWICH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSTICKSANDWICH", "inHigh", 1, run(|| core.CDLSTICKSANDWICH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSTICKSANDWICH", "inLow", 2, run(|| core.CDLSTICKSANDWICH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLSTICKSANDWICH", "inClose", 3, run(|| core.CDLSTICKSANDWICH(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLSTICKSANDWICH", 4);
}

const V_CDLTAKURI: &[&str] = &[
    "defaults",
];

fn sub_CDLTAKURI(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLTAKURI {
        let lb = core.CDLTAKURI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLTAKURI", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLTAKURI(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLTAKURI", label); continue; }
        r.quiet("CDLTAKURI", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLTAKURI(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLTAKURI(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLTAKURI_Lookback();
    if lb == usize::MAX { r.no_legs("CDLTAKURI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTAKURI", "inOpen", 0, run(|| core.CDLTAKURI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTAKURI", "inHigh", 1, run(|| core.CDLTAKURI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTAKURI", "inLow", 2, run(|| core.CDLTAKURI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTAKURI", "inClose", 3, run(|| core.CDLTAKURI(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLTAKURI", 4);
}

const V_CDLTASUKIGAP: &[&str] = &[
    "defaults",
];

fn sub_CDLTASUKIGAP(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLTASUKIGAP {
        let lb = core.CDLTASUKIGAP_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLTASUKIGAP", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLTASUKIGAP(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLTASUKIGAP", label); continue; }
        r.quiet("CDLTASUKIGAP", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLTASUKIGAP(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLTASUKIGAP(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLTASUKIGAP_Lookback();
    if lb == usize::MAX { r.no_legs("CDLTASUKIGAP"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTASUKIGAP", "inOpen", 0, run(|| core.CDLTASUKIGAP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTASUKIGAP", "inHigh", 1, run(|| core.CDLTASUKIGAP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTASUKIGAP", "inLow", 2, run(|| core.CDLTASUKIGAP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTASUKIGAP", "inClose", 3, run(|| core.CDLTASUKIGAP(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLTASUKIGAP", 4);
}

const V_CDLTHRUSTING: &[&str] = &[
    "defaults",
];

fn sub_CDLTHRUSTING(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLTHRUSTING {
        let lb = core.CDLTHRUSTING_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLTHRUSTING", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLTHRUSTING(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLTHRUSTING", label); continue; }
        r.quiet("CDLTHRUSTING", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLTHRUSTING(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLTHRUSTING(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLTHRUSTING_Lookback();
    if lb == usize::MAX { r.no_legs("CDLTHRUSTING"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTHRUSTING", "inOpen", 0, run(|| core.CDLTHRUSTING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTHRUSTING", "inHigh", 1, run(|| core.CDLTHRUSTING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTHRUSTING", "inLow", 2, run(|| core.CDLTHRUSTING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTHRUSTING", "inClose", 3, run(|| core.CDLTHRUSTING(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLTHRUSTING", 4);
}

const V_CDLTRISTAR: &[&str] = &[
    "defaults",
];

fn sub_CDLTRISTAR(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLTRISTAR {
        let lb = core.CDLTRISTAR_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLTRISTAR", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLTRISTAR(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLTRISTAR", label); continue; }
        r.quiet("CDLTRISTAR", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLTRISTAR(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLTRISTAR(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLTRISTAR_Lookback();
    if lb == usize::MAX { r.no_legs("CDLTRISTAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTRISTAR", "inOpen", 0, run(|| core.CDLTRISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTRISTAR", "inHigh", 1, run(|| core.CDLTRISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTRISTAR", "inLow", 2, run(|| core.CDLTRISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLTRISTAR", "inClose", 3, run(|| core.CDLTRISTAR(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLTRISTAR", 4);
}

const V_CDLUNIQUE3RIVER: &[&str] = &[
    "defaults",
];

fn sub_CDLUNIQUE3RIVER(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLUNIQUE3RIVER {
        let lb = core.CDLUNIQUE3RIVER_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLUNIQUE3RIVER", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLUNIQUE3RIVER(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLUNIQUE3RIVER", label); continue; }
        r.quiet("CDLUNIQUE3RIVER", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLUNIQUE3RIVER(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLUNIQUE3RIVER(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLUNIQUE3RIVER_Lookback();
    if lb == usize::MAX { r.no_legs("CDLUNIQUE3RIVER"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLUNIQUE3RIVER", "inOpen", 0, run(|| core.CDLUNIQUE3RIVER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLUNIQUE3RIVER", "inHigh", 1, run(|| core.CDLUNIQUE3RIVER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLUNIQUE3RIVER", "inLow", 2, run(|| core.CDLUNIQUE3RIVER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLUNIQUE3RIVER", "inClose", 3, run(|| core.CDLUNIQUE3RIVER(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLUNIQUE3RIVER", 4);
}

const V_CDLUPSIDEGAP2CROWS: &[&str] = &[
    "defaults",
];

fn sub_CDLUPSIDEGAP2CROWS(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLUPSIDEGAP2CROWS {
        let lb = core.CDLUPSIDEGAP2CROWS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLUPSIDEGAP2CROWS", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLUPSIDEGAP2CROWS(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLUPSIDEGAP2CROWS", label); continue; }
        r.quiet("CDLUPSIDEGAP2CROWS", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLUPSIDEGAP2CROWS(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLUPSIDEGAP2CROWS(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLUPSIDEGAP2CROWS_Lookback();
    if lb == usize::MAX { r.no_legs("CDLUPSIDEGAP2CROWS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLUPSIDEGAP2CROWS", "inOpen", 0, run(|| core.CDLUPSIDEGAP2CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLUPSIDEGAP2CROWS", "inHigh", 1, run(|| core.CDLUPSIDEGAP2CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLUPSIDEGAP2CROWS", "inLow", 2, run(|| core.CDLUPSIDEGAP2CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLUPSIDEGAP2CROWS", "inClose", 3, run(|| core.CDLUPSIDEGAP2CROWS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLUPSIDEGAP2CROWS", 4);
}

const V_CDLXSIDEGAP3METHODS: &[&str] = &[
    "defaults",
];

fn sub_CDLXSIDEGAP3METHODS(r: &mut Report) {
    let core = Core::new();
    for &label in V_CDLXSIDEGAP3METHODS {
        let lb = core.CDLXSIDEGAP3METHODS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CDLXSIDEGAP3METHODS", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLXSIDEGAP3METHODS(0, lb, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("CDLXSIDEGAP3METHODS", label); continue; }
        r.quiet("CDLXSIDEGAP3METHODS", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.CDLXSIDEGAP3METHODS(0, lb - 1, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)
        }));
    }
}

fn legs_CDLXSIDEGAP3METHODS(r: &mut Report) {
    let core = Core::new();
    let lb = core.CDLXSIDEGAP3METHODS_Lookback();
    if lb == usize::MAX { r.no_legs("CDLXSIDEGAP3METHODS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLXSIDEGAP3METHODS", "inOpen", 0, run(|| core.CDLXSIDEGAP3METHODS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLXSIDEGAP3METHODS", "inHigh", 1, run(|| core.CDLXSIDEGAP3METHODS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLXSIDEGAP3METHODS", "inLow", 2, run(|| core.CDLXSIDEGAP3METHODS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("CDLXSIDEGAP3METHODS", "inClose", 3, run(|| core.CDLXSIDEGAP3METHODS(startIdx, endIdx, &inOpen, &inHigh, &inLow, &inClose, &mut outInteger)));
    }
    r.legs_done("CDLXSIDEGAP3METHODS", 4);
}

const V_CEIL: &[&str] = &[
    "defaults",
];

fn sub_CEIL(r: &mut Report) {
    let core = Core::new();
    for &label in V_CEIL {
        let lb = core.CEIL_Lookback();
        if lb == usize::MAX { continue; }
        r.control("CEIL", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CEIL(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("CEIL", label); continue; }
        r.quiet("CEIL", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CEIL(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_CEIL(r: &mut Report) {
    let core = Core::new();
    let lb = core.CEIL_Lookback();
    if lb == usize::MAX { r.no_legs("CEIL"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CEIL", "inReal", 0, run(|| core.CEIL(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("CEIL", 1);
}

const V_CMF: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_CMF(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_CMF {
        let lb = core.CMF_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("CMF", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CMF(0, lb, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("CMF", label); continue; }
        r.quiet("CMF", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CMF(0, lb - 1, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_CMF(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.CMF_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("CMF"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CMF", "inHigh", 0, run(|| core.CMF(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CMF", "inLow", 1, run(|| core.CMF(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CMF", "inClose", 2, run(|| core.CMF(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CMF", "inVolume", 3, run(|| core.CMF(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("CMF", 4);
}

const V_CMO: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_CMO(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_CMO {
        let lb = core.CMO_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("CMO", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CMO(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("CMO", label); continue; }
        r.quiet("CMO", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CMO(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_CMO(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.CMO_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("CMO"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CMO", "inReal", 0, run(|| core.CMO(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("CMO", 1);
}

const V_CMOU: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_CMOU(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_CMOU {
        let lb = core.CMOU_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("CMOU", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CMOU(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("CMOU", label); continue; }
        r.quiet("CMOU", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CMOU(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_CMOU(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.CMOU_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("CMOU"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CMOU", "inReal", 0, run(|| core.CMOU(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("CMOU", 1);
}

const V_CORREL: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_CORREL(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_CORREL {
        let lb = core.CORREL_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("CORREL", label, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CORREL(0, lb, &inReal0, &inReal1, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("CORREL", label); continue; }
        r.quiet("CORREL", label, lb, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.CORREL(0, lb - 1, &inReal0, &inReal1, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_CORREL(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.CORREL_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("CORREL"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal0: Vec<f64> = Vec::with_capacity(1);
        let inReal1: Vec<f64> = series("real", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CORREL", "inReal0", 0, run(|| core.CORREL(startIdx, endIdx, &inReal0, &inReal1, optInTimePeriod, &mut outReal)));
    }
    {
        let inReal0: Vec<f64> = series("real", endIdx + 1);
        let inReal1: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("CORREL", "inReal1", 1, run(|| core.CORREL(startIdx, endIdx, &inReal0, &inReal1, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("CORREL", 2);
}

const V_COS: &[&str] = &[
    "defaults",
];

fn sub_COS(r: &mut Report) {
    let core = Core::new();
    for &label in V_COS {
        let lb = core.COS_Lookback();
        if lb == usize::MAX { continue; }
        r.control("COS", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.COS(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("COS", label); continue; }
        r.quiet("COS", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.COS(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_COS(r: &mut Report) {
    let core = Core::new();
    let lb = core.COS_Lookback();
    if lb == usize::MAX { r.no_legs("COS"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("COS", "inReal", 0, run(|| core.COS(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("COS", 1);
}

const V_COSH: &[&str] = &[
    "defaults",
];

fn sub_COSH(r: &mut Report) {
    let core = Core::new();
    for &label in V_COSH {
        let lb = core.COSH_Lookback();
        if lb == usize::MAX { continue; }
        r.control("COSH", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.COSH(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("COSH", label); continue; }
        r.quiet("COSH", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.COSH(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_COSH(r: &mut Report) {
    let core = Core::new();
    let lb = core.COSH_Lookback();
    if lb == usize::MAX { r.no_legs("COSH"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("COSH", "inReal", 0, run(|| core.COSH(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("COSH", 1);
}

const V_DEMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_DEMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_DEMA {
        let lb = core.DEMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("DEMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.DEMA(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("DEMA", label); continue; }
        r.quiet("DEMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.DEMA(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_DEMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.DEMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("DEMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("DEMA", "inReal", 0, run(|| core.DEMA(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("DEMA", 1);
}

const V_DIV: &[&str] = &[
    "defaults",
];

fn sub_DIV(r: &mut Report) {
    let core = Core::new();
    for &label in V_DIV {
        let lb = core.DIV_Lookback();
        if lb == usize::MAX { continue; }
        r.control("DIV", label, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.DIV(0, lb, &inReal0, &inReal1, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("DIV", label); continue; }
        r.quiet("DIV", label, lb, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.DIV(0, lb - 1, &inReal0, &inReal1, &mut outReal)
        }));
    }
}

fn legs_DIV(r: &mut Report) {
    let core = Core::new();
    let lb = core.DIV_Lookback();
    if lb == usize::MAX { r.no_legs("DIV"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal0: Vec<f64> = Vec::with_capacity(1);
        let inReal1: Vec<f64> = series("real", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("DIV", "inReal0", 0, run(|| core.DIV(startIdx, endIdx, &inReal0, &inReal1, &mut outReal)));
    }
    {
        let inReal0: Vec<f64> = series("real", endIdx + 1);
        let inReal1: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("DIV", "inReal1", 1, run(|| core.DIV(startIdx, endIdx, &inReal0, &inReal1, &mut outReal)));
    }
    r.legs_done("DIV", 2);
}

const V_DX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_DX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_DX {
        let lb = core.DX_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("DX", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.DX(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("DX", label); continue; }
        r.quiet("DX", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.DX(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_DX(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.DX_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("DX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("DX", "inHigh", 0, run(|| core.DX(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("DX", "inLow", 1, run(|| core.DX(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("DX", "inClose", 2, run(|| core.DX(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("DX", 3);
}

const V_EFI: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_EFI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_EFI {
        let lb = core.EFI_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("EFI", label, run(|| {
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.EFI(0, lb, &inClose, &inVolume, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("EFI", label); continue; }
        r.quiet("EFI", label, lb, run(|| {
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.EFI(0, lb - 1, &inClose, &inVolume, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_EFI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.EFI_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("EFI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("EFI", "inClose", 0, run(|| core.EFI(startIdx, endIdx, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    {
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("EFI", "inVolume", 1, run(|| core.EFI(startIdx, endIdx, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("EFI", 2);
}

const V_EMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_EMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_EMA {
        let lb = core.EMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("EMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.EMA(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("EMA", label); continue; }
        r.quiet("EMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.EMA(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_EMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.EMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("EMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("EMA", "inReal", 0, run(|| core.EMA(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("EMA", 1);
}

const V_EXP: &[&str] = &[
    "defaults",
];

fn sub_EXP(r: &mut Report) {
    let core = Core::new();
    for &label in V_EXP {
        let lb = core.EXP_Lookback();
        if lb == usize::MAX { continue; }
        r.control("EXP", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.EXP(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("EXP", label); continue; }
        r.quiet("EXP", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.EXP(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_EXP(r: &mut Report) {
    let core = Core::new();
    let lb = core.EXP_Lookback();
    if lb == usize::MAX { r.no_legs("EXP"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("EXP", "inReal", 0, run(|| core.EXP(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("EXP", 1);
}

const V_FLOOR: &[&str] = &[
    "defaults",
];

fn sub_FLOOR(r: &mut Report) {
    let core = Core::new();
    for &label in V_FLOOR {
        let lb = core.FLOOR_Lookback();
        if lb == usize::MAX { continue; }
        r.control("FLOOR", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.FLOOR(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("FLOOR", label); continue; }
        r.quiet("FLOOR", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.FLOOR(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_FLOOR(r: &mut Report) {
    let core = Core::new();
    let lb = core.FLOOR_Lookback();
    if lb == usize::MAX { r.no_legs("FLOOR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("FLOOR", "inReal", 0, run(|| core.FLOOR(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("FLOOR", 1);
}

const V_HMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_HMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_HMA {
        let lb = core.HMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("HMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.HMA(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("HMA", label); continue; }
        r.quiet("HMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.HMA(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_HMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.HMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("HMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("HMA", "inReal", 0, run(|| core.HMA(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("HMA", 1);
}

const V_HT_DCPERIOD: &[&str] = &[
    "defaults",
];

fn sub_HT_DCPERIOD(r: &mut Report) {
    let core = Core::new();
    for &label in V_HT_DCPERIOD {
        let lb = core.HT_DCPERIOD_Lookback();
        if lb == usize::MAX { continue; }
        r.control("HT_DCPERIOD", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.HT_DCPERIOD(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("HT_DCPERIOD", label); continue; }
        r.quiet("HT_DCPERIOD", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.HT_DCPERIOD(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_HT_DCPERIOD(r: &mut Report) {
    let core = Core::new();
    let lb = core.HT_DCPERIOD_Lookback();
    if lb == usize::MAX { r.no_legs("HT_DCPERIOD"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("HT_DCPERIOD", "inReal", 0, run(|| core.HT_DCPERIOD(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("HT_DCPERIOD", 1);
}

const V_HT_DCPHASE: &[&str] = &[
    "defaults",
];

fn sub_HT_DCPHASE(r: &mut Report) {
    let core = Core::new();
    for &label in V_HT_DCPHASE {
        let lb = core.HT_DCPHASE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("HT_DCPHASE", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.HT_DCPHASE(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("HT_DCPHASE", label); continue; }
        r.quiet("HT_DCPHASE", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.HT_DCPHASE(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_HT_DCPHASE(r: &mut Report) {
    let core = Core::new();
    let lb = core.HT_DCPHASE_Lookback();
    if lb == usize::MAX { r.no_legs("HT_DCPHASE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("HT_DCPHASE", "inReal", 0, run(|| core.HT_DCPHASE(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("HT_DCPHASE", 1);
}

const V_HT_PHASOR: &[&str] = &[
    "defaults",
];

fn sub_HT_PHASOR(r: &mut Report) {
    let core = Core::new();
    for &label in V_HT_PHASOR {
        let lb = core.HT_PHASOR_Lookback();
        if lb == usize::MAX { continue; }
        r.control("HT_PHASOR", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outInPhase: Vec<f64> = Vec::with_capacity(1);
            let mut outQuadrature: Vec<f64> = Vec::with_capacity(1);
            core.HT_PHASOR(0, lb, &inReal, &mut outInPhase, &mut outQuadrature)
        }));
        if lb < 1 { r.no_quiet_range("HT_PHASOR", label); continue; }
        r.quiet("HT_PHASOR", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outInPhase: Vec<f64> = Vec::with_capacity(1);
            let mut outQuadrature: Vec<f64> = Vec::with_capacity(1);
            core.HT_PHASOR(0, lb - 1, &inReal, &mut outInPhase, &mut outQuadrature)
        }));
    }
}

fn legs_HT_PHASOR(r: &mut Report) {
    let core = Core::new();
    let lb = core.HT_PHASOR_Lookback();
    if lb == usize::MAX { r.no_legs("HT_PHASOR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outInPhase: Vec<f64> = vec![Default::default(); 5];
        let mut outQuadrature: Vec<f64> = vec![Default::default(); 5];
        r.leg("HT_PHASOR", "inReal", 0, run(|| core.HT_PHASOR(startIdx, endIdx, &inReal, &mut outInPhase, &mut outQuadrature)));
    }
    r.legs_done("HT_PHASOR", 1);
}

const V_HT_SINE: &[&str] = &[
    "defaults",
];

fn sub_HT_SINE(r: &mut Report) {
    let core = Core::new();
    for &label in V_HT_SINE {
        let lb = core.HT_SINE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("HT_SINE", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outSine: Vec<f64> = Vec::with_capacity(1);
            let mut outLeadSine: Vec<f64> = Vec::with_capacity(1);
            core.HT_SINE(0, lb, &inReal, &mut outSine, &mut outLeadSine)
        }));
        if lb < 1 { r.no_quiet_range("HT_SINE", label); continue; }
        r.quiet("HT_SINE", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outSine: Vec<f64> = Vec::with_capacity(1);
            let mut outLeadSine: Vec<f64> = Vec::with_capacity(1);
            core.HT_SINE(0, lb - 1, &inReal, &mut outSine, &mut outLeadSine)
        }));
    }
}

fn legs_HT_SINE(r: &mut Report) {
    let core = Core::new();
    let lb = core.HT_SINE_Lookback();
    if lb == usize::MAX { r.no_legs("HT_SINE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outSine: Vec<f64> = vec![Default::default(); 5];
        let mut outLeadSine: Vec<f64> = vec![Default::default(); 5];
        r.leg("HT_SINE", "inReal", 0, run(|| core.HT_SINE(startIdx, endIdx, &inReal, &mut outSine, &mut outLeadSine)));
    }
    r.legs_done("HT_SINE", 1);
}

const V_HT_TRENDLINE: &[&str] = &[
    "defaults",
];

fn sub_HT_TRENDLINE(r: &mut Report) {
    let core = Core::new();
    for &label in V_HT_TRENDLINE {
        let lb = core.HT_TRENDLINE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("HT_TRENDLINE", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.HT_TRENDLINE(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("HT_TRENDLINE", label); continue; }
        r.quiet("HT_TRENDLINE", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.HT_TRENDLINE(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_HT_TRENDLINE(r: &mut Report) {
    let core = Core::new();
    let lb = core.HT_TRENDLINE_Lookback();
    if lb == usize::MAX { r.no_legs("HT_TRENDLINE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("HT_TRENDLINE", "inReal", 0, run(|| core.HT_TRENDLINE(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("HT_TRENDLINE", 1);
}

const V_HT_TRENDMODE: &[&str] = &[
    "defaults",
];

fn sub_HT_TRENDMODE(r: &mut Report) {
    let core = Core::new();
    for &label in V_HT_TRENDMODE {
        let lb = core.HT_TRENDMODE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("HT_TRENDMODE", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.HT_TRENDMODE(0, lb, &inReal, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("HT_TRENDMODE", label); continue; }
        r.quiet("HT_TRENDMODE", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.HT_TRENDMODE(0, lb - 1, &inReal, &mut outInteger)
        }));
    }
}

fn legs_HT_TRENDMODE(r: &mut Report) {
    let core = Core::new();
    let lb = core.HT_TRENDMODE_Lookback();
    if lb == usize::MAX { r.no_legs("HT_TRENDMODE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("HT_TRENDMODE", "inReal", 0, run(|| core.HT_TRENDMODE(startIdx, endIdx, &inReal, &mut outInteger)));
    }
    r.legs_done("HT_TRENDMODE", 1);
}

const V_IMI: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_IMI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_IMI {
        let lb = core.IMI_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("IMI", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.IMI(0, lb, &inOpen, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("IMI", label); continue; }
        r.quiet("IMI", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.IMI(0, lb - 1, &inOpen, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_IMI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.IMI_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("IMI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("IMI", "inOpen", 0, run(|| core.IMI(startIdx, endIdx, &inOpen, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("IMI", "inClose", 1, run(|| core.IMI(startIdx, endIdx, &inOpen, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("IMI", 2);
}

const V_KAMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_KAMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_KAMA {
        let lb = core.KAMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("KAMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.KAMA(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("KAMA", label); continue; }
        r.quiet("KAMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.KAMA(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_KAMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.KAMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("KAMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("KAMA", "inReal", 0, run(|| core.KAMA(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("KAMA", 1);
}

const V_LINEARREG: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_LINEARREG(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_LINEARREG {
        let lb = core.LINEARREG_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("LINEARREG", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LINEARREG(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("LINEARREG", label); continue; }
        r.quiet("LINEARREG", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LINEARREG(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_LINEARREG(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.LINEARREG_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("LINEARREG"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("LINEARREG", "inReal", 0, run(|| core.LINEARREG(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("LINEARREG", 1);
}

const V_LINEARREG_ANGLE: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_LINEARREG_ANGLE(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_LINEARREG_ANGLE {
        let lb = core.LINEARREG_ANGLE_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("LINEARREG_ANGLE", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LINEARREG_ANGLE(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("LINEARREG_ANGLE", label); continue; }
        r.quiet("LINEARREG_ANGLE", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LINEARREG_ANGLE(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_LINEARREG_ANGLE(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.LINEARREG_ANGLE_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("LINEARREG_ANGLE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("LINEARREG_ANGLE", "inReal", 0, run(|| core.LINEARREG_ANGLE(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("LINEARREG_ANGLE", 1);
}

const V_LINEARREG_INTERCEPT: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_LINEARREG_INTERCEPT(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_LINEARREG_INTERCEPT {
        let lb = core.LINEARREG_INTERCEPT_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("LINEARREG_INTERCEPT", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LINEARREG_INTERCEPT(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("LINEARREG_INTERCEPT", label); continue; }
        r.quiet("LINEARREG_INTERCEPT", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LINEARREG_INTERCEPT(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_LINEARREG_INTERCEPT(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.LINEARREG_INTERCEPT_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("LINEARREG_INTERCEPT"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("LINEARREG_INTERCEPT", "inReal", 0, run(|| core.LINEARREG_INTERCEPT(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("LINEARREG_INTERCEPT", 1);
}

const V_LINEARREG_SLOPE: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_LINEARREG_SLOPE(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_LINEARREG_SLOPE {
        let lb = core.LINEARREG_SLOPE_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("LINEARREG_SLOPE", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LINEARREG_SLOPE(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("LINEARREG_SLOPE", label); continue; }
        r.quiet("LINEARREG_SLOPE", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LINEARREG_SLOPE(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_LINEARREG_SLOPE(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.LINEARREG_SLOPE_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("LINEARREG_SLOPE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("LINEARREG_SLOPE", "inReal", 0, run(|| core.LINEARREG_SLOPE(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("LINEARREG_SLOPE", 1);
}

const V_LN: &[&str] = &[
    "defaults",
];

fn sub_LN(r: &mut Report) {
    let core = Core::new();
    for &label in V_LN {
        let lb = core.LN_Lookback();
        if lb == usize::MAX { continue; }
        r.control("LN", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LN(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("LN", label); continue; }
        r.quiet("LN", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LN(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_LN(r: &mut Report) {
    let core = Core::new();
    let lb = core.LN_Lookback();
    if lb == usize::MAX { r.no_legs("LN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("LN", "inReal", 0, run(|| core.LN(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("LN", 1);
}

const V_LOG10: &[&str] = &[
    "defaults",
];

fn sub_LOG10(r: &mut Report) {
    let core = Core::new();
    for &label in V_LOG10 {
        let lb = core.LOG10_Lookback();
        if lb == usize::MAX { continue; }
        r.control("LOG10", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LOG10(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("LOG10", label); continue; }
        r.quiet("LOG10", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.LOG10(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_LOG10(r: &mut Report) {
    let core = Core::new();
    let lb = core.LOG10_Lookback();
    if lb == usize::MAX { r.no_legs("LOG10"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("LOG10", "inReal", 0, run(|| core.LOG10(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("LOG10", 1);
}

const V_MA: &[(&str, i32, MAType)] = &[
    ("defaults", i32::MIN, MAType::DEFAULT),
    ("minimums", 1i32, MAType::DEFAULT),
    ("optInMAType=SMA", i32::MIN, MAType::SMA),
    ("optInMAType=SMA, periods doubled", 60i32, MAType::SMA),
    ("optInMAType=EMA", i32::MIN, MAType::EMA),
    ("optInMAType=EMA, periods doubled", 60i32, MAType::EMA),
    ("optInMAType=WMA", i32::MIN, MAType::WMA),
    ("optInMAType=WMA, periods doubled", 60i32, MAType::WMA),
    ("optInMAType=DEMA", i32::MIN, MAType::DEMA),
    ("optInMAType=DEMA, periods doubled", 60i32, MAType::DEMA),
    ("optInMAType=TEMA", i32::MIN, MAType::TEMA),
    ("optInMAType=TEMA, periods doubled", 60i32, MAType::TEMA),
    ("optInMAType=TRIMA", i32::MIN, MAType::TRIMA),
    ("optInMAType=TRIMA, periods doubled", 60i32, MAType::TRIMA),
    ("optInMAType=KAMA", i32::MIN, MAType::KAMA),
    ("optInMAType=KAMA, periods doubled", 60i32, MAType::KAMA),
    ("optInMAType=MAMA", i32::MIN, MAType::MAMA),
    ("optInMAType=MAMA, periods doubled", 60i32, MAType::MAMA),
    ("optInMAType=T3", i32::MIN, MAType::T3),
    ("optInMAType=T3, periods doubled", 60i32, MAType::T3),
    ("optInMAType=HMA", i32::MIN, MAType::HMA),
    ("optInMAType=HMA, periods doubled", 60i32, MAType::HMA),
    ("optInMAType=DISABLED", i32::MIN, MAType::DISABLED),
    ("optInMAType=DISABLED, periods doubled", 60i32, MAType::DISABLED),
    ("optInMAType=DEFAULT", i32::MIN, MAType::DEFAULT),
    ("optInMAType=DEFAULT, periods doubled", 60i32, MAType::DEFAULT),
];

fn sub_MA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod, optInMAType) in V_MA {
        let lb = core.MA_Lookback(optInTimePeriod, optInMAType);
        if lb == usize::MAX { continue; }
        r.control("MA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MA(0, lb, &inReal, optInTimePeriod, optInMAType, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MA", label); continue; }
        r.quiet("MA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MA(0, lb - 1, &inReal, optInTimePeriod, optInMAType, &mut outReal)
        }));
    }
}

fn legs_MA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let optInMAType = MAType::DEFAULT;
    let lb = core.MA_Lookback(optInTimePeriod, optInMAType);
    if lb == usize::MAX { r.no_legs("MA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MA", "inReal", 0, run(|| core.MA(startIdx, endIdx, &inReal, optInTimePeriod, optInMAType, &mut outReal)));
    }
    r.legs_done("MA", 1);
}

const V_MACD: &[(&str, i32, i32, i32)] = &[
    ("defaults", i32::MIN, i32::MIN, i32::MIN),
    ("minimums", 2i32, 2i32, 1i32),
];

fn sub_MACD(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastPeriod, optInSlowPeriod, optInSignalPeriod) in V_MACD {
        let lb = core.MACD_Lookback(optInFastPeriod, optInSlowPeriod, optInSignalPeriod);
        if lb == usize::MAX { continue; }
        r.control("MACD", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACD: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDSignal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDHist: Vec<f64> = Vec::with_capacity(1);
            core.MACD(0, lb, &inReal, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)
        }));
        if lb < 1 { r.no_quiet_range("MACD", label); continue; }
        r.quiet("MACD", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACD: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDSignal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDHist: Vec<f64> = Vec::with_capacity(1);
            core.MACD(0, lb - 1, &inReal, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)
        }));
    }
}

fn legs_MACD(r: &mut Report) {
    let core = Core::new();
    let optInFastPeriod = i32::MIN;
    let optInSlowPeriod = i32::MIN;
    let optInSignalPeriod = i32::MIN;
    let lb = core.MACD_Lookback(optInFastPeriod, optInSlowPeriod, optInSignalPeriod);
    if lb == usize::MAX { r.no_legs("MACD"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outMACD: Vec<f64> = vec![Default::default(); 5];
        let mut outMACDSignal: Vec<f64> = vec![Default::default(); 5];
        let mut outMACDHist: Vec<f64> = vec![Default::default(); 5];
        r.leg("MACD", "inReal", 0, run(|| core.MACD(startIdx, endIdx, &inReal, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)));
    }
    r.legs_done("MACD", 1);
}

const V_MACDEXT: &[(&str, i32, MAType, i32, MAType, i32, MAType)] = &[
    ("defaults", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("minimums", 2i32, MAType::DEFAULT, 2i32, MAType::DEFAULT, 1i32, MAType::DEFAULT),
    ("optInFastMAType=SMA", i32::MIN, MAType::SMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=SMA, periods doubled", 24i32, MAType::SMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=EMA", i32::MIN, MAType::EMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=EMA, periods doubled", 24i32, MAType::EMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=WMA", i32::MIN, MAType::WMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=WMA, periods doubled", 24i32, MAType::WMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=DEMA", i32::MIN, MAType::DEMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=DEMA, periods doubled", 24i32, MAType::DEMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=TEMA", i32::MIN, MAType::TEMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=TEMA, periods doubled", 24i32, MAType::TEMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=TRIMA", i32::MIN, MAType::TRIMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=TRIMA, periods doubled", 24i32, MAType::TRIMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=KAMA", i32::MIN, MAType::KAMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=KAMA, periods doubled", 24i32, MAType::KAMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=MAMA", i32::MIN, MAType::MAMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=MAMA, periods doubled", 24i32, MAType::MAMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=T3", i32::MIN, MAType::T3, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=T3, periods doubled", 24i32, MAType::T3, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=HMA", i32::MIN, MAType::HMA, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=HMA, periods doubled", 24i32, MAType::HMA, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=DISABLED", i32::MIN, MAType::DISABLED, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=DISABLED, periods doubled", 24i32, MAType::DISABLED, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInFastMAType=DEFAULT", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInFastMAType=DEFAULT, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=SMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::SMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=SMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::SMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=EMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::EMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=EMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::EMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=WMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::WMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=WMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::WMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=DEMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=DEMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=TEMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::TEMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=TEMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::TEMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=TRIMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::TRIMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=TRIMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::TRIMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=KAMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::KAMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=KAMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::KAMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=MAMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::MAMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=MAMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::MAMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=T3", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::T3, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=T3, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::T3, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=HMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::HMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=HMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::HMA, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=DISABLED", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DISABLED, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=DISABLED, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DISABLED, 18i32, MAType::DEFAULT),
    ("optInSlowMAType=DEFAULT", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInSlowMAType=DEFAULT, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
    ("optInSignalMAType=SMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::SMA),
    ("optInSignalMAType=SMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::SMA),
    ("optInSignalMAType=EMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::EMA),
    ("optInSignalMAType=EMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::EMA),
    ("optInSignalMAType=WMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::WMA),
    ("optInSignalMAType=WMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::WMA),
    ("optInSignalMAType=DEMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEMA),
    ("optInSignalMAType=DEMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::DEMA),
    ("optInSignalMAType=TEMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::TEMA),
    ("optInSignalMAType=TEMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::TEMA),
    ("optInSignalMAType=TRIMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::TRIMA),
    ("optInSignalMAType=TRIMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::TRIMA),
    ("optInSignalMAType=KAMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::KAMA),
    ("optInSignalMAType=KAMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::KAMA),
    ("optInSignalMAType=MAMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::MAMA),
    ("optInSignalMAType=MAMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::MAMA),
    ("optInSignalMAType=T3", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::T3),
    ("optInSignalMAType=T3, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::T3),
    ("optInSignalMAType=HMA", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::HMA),
    ("optInSignalMAType=HMA, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::HMA),
    ("optInSignalMAType=DISABLED", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DISABLED),
    ("optInSignalMAType=DISABLED, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::DISABLED),
    ("optInSignalMAType=DEFAULT", i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInSignalMAType=DEFAULT, periods doubled", 24i32, MAType::DEFAULT, 52i32, MAType::DEFAULT, 18i32, MAType::DEFAULT),
];

fn sub_MACDEXT(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastPeriod, optInFastMAType, optInSlowPeriod, optInSlowMAType, optInSignalPeriod, optInSignalMAType) in V_MACDEXT {
        let lb = core.MACDEXT_Lookback(optInFastPeriod, optInFastMAType, optInSlowPeriod, optInSlowMAType, optInSignalPeriod, optInSignalMAType);
        if lb == usize::MAX { continue; }
        r.control("MACDEXT", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACD: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDSignal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDHist: Vec<f64> = Vec::with_capacity(1);
            core.MACDEXT(0, lb, &inReal, optInFastPeriod, optInFastMAType, optInSlowPeriod, optInSlowMAType, optInSignalPeriod, optInSignalMAType, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)
        }));
        if lb < 1 { r.no_quiet_range("MACDEXT", label); continue; }
        r.quiet("MACDEXT", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACD: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDSignal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDHist: Vec<f64> = Vec::with_capacity(1);
            core.MACDEXT(0, lb - 1, &inReal, optInFastPeriod, optInFastMAType, optInSlowPeriod, optInSlowMAType, optInSignalPeriod, optInSignalMAType, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)
        }));
    }
}

fn legs_MACDEXT(r: &mut Report) {
    let core = Core::new();
    let optInFastPeriod = i32::MIN;
    let optInFastMAType = MAType::DEFAULT;
    let optInSlowPeriod = i32::MIN;
    let optInSlowMAType = MAType::DEFAULT;
    let optInSignalPeriod = i32::MIN;
    let optInSignalMAType = MAType::DEFAULT;
    let lb = core.MACDEXT_Lookback(optInFastPeriod, optInFastMAType, optInSlowPeriod, optInSlowMAType, optInSignalPeriod, optInSignalMAType);
    if lb == usize::MAX { r.no_legs("MACDEXT"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outMACD: Vec<f64> = vec![Default::default(); 5];
        let mut outMACDSignal: Vec<f64> = vec![Default::default(); 5];
        let mut outMACDHist: Vec<f64> = vec![Default::default(); 5];
        r.leg("MACDEXT", "inReal", 0, run(|| core.MACDEXT(startIdx, endIdx, &inReal, optInFastPeriod, optInFastMAType, optInSlowPeriod, optInSlowMAType, optInSignalPeriod, optInSignalMAType, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)));
    }
    r.legs_done("MACDEXT", 1);
}

const V_MACDFIX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_MACDFIX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInSignalPeriod) in V_MACDFIX {
        let lb = core.MACDFIX_Lookback(optInSignalPeriod);
        if lb == usize::MAX { continue; }
        r.control("MACDFIX", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACD: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDSignal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDHist: Vec<f64> = Vec::with_capacity(1);
            core.MACDFIX(0, lb, &inReal, optInSignalPeriod, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)
        }));
        if lb < 1 { r.no_quiet_range("MACDFIX", label); continue; }
        r.quiet("MACDFIX", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACD: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDSignal: Vec<f64> = Vec::with_capacity(1);
            let mut outMACDHist: Vec<f64> = Vec::with_capacity(1);
            core.MACDFIX(0, lb - 1, &inReal, optInSignalPeriod, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)
        }));
    }
}

fn legs_MACDFIX(r: &mut Report) {
    let core = Core::new();
    let optInSignalPeriod = i32::MIN;
    let lb = core.MACDFIX_Lookback(optInSignalPeriod);
    if lb == usize::MAX { r.no_legs("MACDFIX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outMACD: Vec<f64> = vec![Default::default(); 5];
        let mut outMACDSignal: Vec<f64> = vec![Default::default(); 5];
        let mut outMACDHist: Vec<f64> = vec![Default::default(); 5];
        r.leg("MACDFIX", "inReal", 0, run(|| core.MACDFIX(startIdx, endIdx, &inReal, optInSignalPeriod, &mut outMACD, &mut outMACDSignal, &mut outMACDHist)));
    }
    r.legs_done("MACDFIX", 1);
}

const V_MAMA: &[(&str, f64, f64)] = &[
    ("defaults", Core::REAL_DEFAULT, Core::REAL_DEFAULT),
    ("minimums", 0.01f64, 0.01f64),
];

fn sub_MAMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastLimit, optInSlowLimit) in V_MAMA {
        let lb = core.MAMA_Lookback(optInFastLimit, optInSlowLimit);
        if lb == usize::MAX { continue; }
        r.control("MAMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMAMA: Vec<f64> = Vec::with_capacity(1);
            let mut outFAMA: Vec<f64> = Vec::with_capacity(1);
            core.MAMA(0, lb, &inReal, optInFastLimit, optInSlowLimit, &mut outMAMA, &mut outFAMA)
        }));
        if lb < 1 { r.no_quiet_range("MAMA", label); continue; }
        r.quiet("MAMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMAMA: Vec<f64> = Vec::with_capacity(1);
            let mut outFAMA: Vec<f64> = Vec::with_capacity(1);
            core.MAMA(0, lb - 1, &inReal, optInFastLimit, optInSlowLimit, &mut outMAMA, &mut outFAMA)
        }));
    }
}

fn legs_MAMA(r: &mut Report) {
    let core = Core::new();
    let optInFastLimit = Core::REAL_DEFAULT;
    let optInSlowLimit = Core::REAL_DEFAULT;
    let lb = core.MAMA_Lookback(optInFastLimit, optInSlowLimit);
    if lb == usize::MAX { r.no_legs("MAMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outMAMA: Vec<f64> = vec![Default::default(); 5];
        let mut outFAMA: Vec<f64> = vec![Default::default(); 5];
        r.leg("MAMA", "inReal", 0, run(|| core.MAMA(startIdx, endIdx, &inReal, optInFastLimit, optInSlowLimit, &mut outMAMA, &mut outFAMA)));
    }
    r.legs_done("MAMA", 1);
}

const V_MARKETFI: &[&str] = &[
    "defaults",
];

fn sub_MARKETFI(r: &mut Report) {
    let core = Core::new();
    for &label in V_MARKETFI {
        let lb = core.MARKETFI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("MARKETFI", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MARKETFI(0, lb, &inHigh, &inLow, &inVolume, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MARKETFI", label); continue; }
        r.quiet("MARKETFI", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MARKETFI(0, lb - 1, &inHigh, &inLow, &inVolume, &mut outReal)
        }));
    }
}

fn legs_MARKETFI(r: &mut Report) {
    let core = Core::new();
    let lb = core.MARKETFI_Lookback();
    if lb == usize::MAX { r.no_legs("MARKETFI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MARKETFI", "inHigh", 0, run(|| core.MARKETFI(startIdx, endIdx, &inHigh, &inLow, &inVolume, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MARKETFI", "inLow", 1, run(|| core.MARKETFI(startIdx, endIdx, &inHigh, &inLow, &inVolume, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MARKETFI", "inVolume", 2, run(|| core.MARKETFI(startIdx, endIdx, &inHigh, &inLow, &inVolume, &mut outReal)));
    }
    r.legs_done("MARKETFI", 3);
}

const V_MAVP: &[(&str, i32, i32, MAType)] = &[
    ("defaults", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("minimums", 1i32, 1i32, MAType::DEFAULT),
    ("optInMAType=SMA", i32::MIN, i32::MIN, MAType::SMA),
    ("optInMAType=SMA, periods doubled", 4i32, 60i32, MAType::SMA),
    ("optInMAType=EMA", i32::MIN, i32::MIN, MAType::EMA),
    ("optInMAType=EMA, periods doubled", 4i32, 60i32, MAType::EMA),
    ("optInMAType=WMA", i32::MIN, i32::MIN, MAType::WMA),
    ("optInMAType=WMA, periods doubled", 4i32, 60i32, MAType::WMA),
    ("optInMAType=DEMA", i32::MIN, i32::MIN, MAType::DEMA),
    ("optInMAType=DEMA, periods doubled", 4i32, 60i32, MAType::DEMA),
    ("optInMAType=TEMA", i32::MIN, i32::MIN, MAType::TEMA),
    ("optInMAType=TEMA, periods doubled", 4i32, 60i32, MAType::TEMA),
    ("optInMAType=TRIMA", i32::MIN, i32::MIN, MAType::TRIMA),
    ("optInMAType=TRIMA, periods doubled", 4i32, 60i32, MAType::TRIMA),
    ("optInMAType=KAMA", i32::MIN, i32::MIN, MAType::KAMA),
    ("optInMAType=KAMA, periods doubled", 4i32, 60i32, MAType::KAMA),
    ("optInMAType=MAMA", i32::MIN, i32::MIN, MAType::MAMA),
    ("optInMAType=MAMA, periods doubled", 4i32, 60i32, MAType::MAMA),
    ("optInMAType=T3", i32::MIN, i32::MIN, MAType::T3),
    ("optInMAType=T3, periods doubled", 4i32, 60i32, MAType::T3),
    ("optInMAType=HMA", i32::MIN, i32::MIN, MAType::HMA),
    ("optInMAType=HMA, periods doubled", 4i32, 60i32, MAType::HMA),
    ("optInMAType=DISABLED", i32::MIN, i32::MIN, MAType::DISABLED),
    ("optInMAType=DISABLED, periods doubled", 4i32, 60i32, MAType::DISABLED),
    ("optInMAType=DEFAULT", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("optInMAType=DEFAULT, periods doubled", 4i32, 60i32, MAType::DEFAULT),
];

fn sub_MAVP(r: &mut Report) {
    let core = Core::new();
    for &(label, optInMinPeriod, optInMaxPeriod, optInMAType) in V_MAVP {
        let lb = core.MAVP_Lookback(optInMinPeriod, optInMaxPeriod, optInMAType);
        if lb == usize::MAX { continue; }
        r.control("MAVP", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let inPeriods: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MAVP(0, lb, &inReal, &inPeriods, optInMinPeriod, optInMaxPeriod, optInMAType, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MAVP", label); continue; }
        r.quiet("MAVP", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let inPeriods: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MAVP(0, lb - 1, &inReal, &inPeriods, optInMinPeriod, optInMaxPeriod, optInMAType, &mut outReal)
        }));
    }
}

fn legs_MAVP(r: &mut Report) {
    let core = Core::new();
    let optInMinPeriod = i32::MIN;
    let optInMaxPeriod = i32::MIN;
    let optInMAType = MAType::DEFAULT;
    let lb = core.MAVP_Lookback(optInMinPeriod, optInMaxPeriod, optInMAType);
    if lb == usize::MAX { r.no_legs("MAVP"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let inPeriods: Vec<f64> = series("inPeriods", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MAVP", "inReal", 0, run(|| core.MAVP(startIdx, endIdx, &inReal, &inPeriods, optInMinPeriod, optInMaxPeriod, optInMAType, &mut outReal)));
    }
    {
        let inReal: Vec<f64> = series("real", endIdx + 1);
        let inPeriods: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MAVP", "inPeriods", 1, run(|| core.MAVP(startIdx, endIdx, &inReal, &inPeriods, optInMinPeriod, optInMaxPeriod, optInMAType, &mut outReal)));
    }
    r.legs_done("MAVP", 2);
}

const V_MAX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MAX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MAX {
        let lb = core.MAX_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MAX", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MAX(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MAX", label); continue; }
        r.quiet("MAX", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MAX(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_MAX(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MAX_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MAX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MAX", "inReal", 0, run(|| core.MAX(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("MAX", 1);
}

const V_MAXINDEX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MAXINDEX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MAXINDEX {
        let lb = core.MAXINDEX_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MAXINDEX", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.MAXINDEX(0, lb, &inReal, optInTimePeriod, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("MAXINDEX", label); continue; }
        r.quiet("MAXINDEX", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.MAXINDEX(0, lb - 1, &inReal, optInTimePeriod, &mut outInteger)
        }));
    }
}

fn legs_MAXINDEX(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MAXINDEX_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MAXINDEX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("MAXINDEX", "inReal", 0, run(|| core.MAXINDEX(startIdx, endIdx, &inReal, optInTimePeriod, &mut outInteger)));
    }
    r.legs_done("MAXINDEX", 1);
}

const V_MEDPRICE: &[&str] = &[
    "defaults",
];

fn sub_MEDPRICE(r: &mut Report) {
    let core = Core::new();
    for &label in V_MEDPRICE {
        let lb = core.MEDPRICE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("MEDPRICE", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MEDPRICE(0, lb, &inHigh, &inLow, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MEDPRICE", label); continue; }
        r.quiet("MEDPRICE", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MEDPRICE(0, lb - 1, &inHigh, &inLow, &mut outReal)
        }));
    }
}

fn legs_MEDPRICE(r: &mut Report) {
    let core = Core::new();
    let lb = core.MEDPRICE_Lookback();
    if lb == usize::MAX { r.no_legs("MEDPRICE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MEDPRICE", "inHigh", 0, run(|| core.MEDPRICE(startIdx, endIdx, &inHigh, &inLow, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MEDPRICE", "inLow", 1, run(|| core.MEDPRICE(startIdx, endIdx, &inHigh, &inLow, &mut outReal)));
    }
    r.legs_done("MEDPRICE", 2);
}

const V_MFI: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MFI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MFI {
        let lb = core.MFI_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MFI", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MFI(0, lb, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MFI", label); continue; }
        r.quiet("MFI", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MFI(0, lb - 1, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_MFI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MFI_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MFI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MFI", "inHigh", 0, run(|| core.MFI(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MFI", "inLow", 1, run(|| core.MFI(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MFI", "inClose", 2, run(|| core.MFI(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MFI", "inVolume", 3, run(|| core.MFI(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("MFI", 4);
}

const V_MIDPOINT: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MIDPOINT(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MIDPOINT {
        let lb = core.MIDPOINT_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MIDPOINT", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MIDPOINT(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MIDPOINT", label); continue; }
        r.quiet("MIDPOINT", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MIDPOINT(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_MIDPOINT(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MIDPOINT_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MIDPOINT"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MIDPOINT", "inReal", 0, run(|| core.MIDPOINT(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("MIDPOINT", 1);
}

const V_MIDPRICE: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MIDPRICE(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MIDPRICE {
        let lb = core.MIDPRICE_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MIDPRICE", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MIDPRICE(0, lb, &inHigh, &inLow, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MIDPRICE", label); continue; }
        r.quiet("MIDPRICE", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MIDPRICE(0, lb - 1, &inHigh, &inLow, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_MIDPRICE(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MIDPRICE_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MIDPRICE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MIDPRICE", "inHigh", 0, run(|| core.MIDPRICE(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MIDPRICE", "inLow", 1, run(|| core.MIDPRICE(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("MIDPRICE", 2);
}

const V_MIN: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MIN(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MIN {
        let lb = core.MIN_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MIN", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MIN(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MIN", label); continue; }
        r.quiet("MIN", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MIN(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_MIN(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MIN_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MIN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MIN", "inReal", 0, run(|| core.MIN(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("MIN", 1);
}

const V_MININDEX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MININDEX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MININDEX {
        let lb = core.MININDEX_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MININDEX", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.MININDEX(0, lb, &inReal, optInTimePeriod, &mut outInteger)
        }));
        if lb < 1 { r.no_quiet_range("MININDEX", label); continue; }
        r.quiet("MININDEX", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outInteger: Vec<i32> = Vec::with_capacity(1);
            core.MININDEX(0, lb - 1, &inReal, optInTimePeriod, &mut outInteger)
        }));
    }
}

fn legs_MININDEX(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MININDEX_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MININDEX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outInteger: Vec<i32> = vec![Default::default(); 5];
        r.leg("MININDEX", "inReal", 0, run(|| core.MININDEX(startIdx, endIdx, &inReal, optInTimePeriod, &mut outInteger)));
    }
    r.legs_done("MININDEX", 1);
}

const V_MINMAX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MINMAX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MINMAX {
        let lb = core.MINMAX_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MINMAX", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMin: Vec<f64> = Vec::with_capacity(1);
            let mut outMax: Vec<f64> = Vec::with_capacity(1);
            core.MINMAX(0, lb, &inReal, optInTimePeriod, &mut outMin, &mut outMax)
        }));
        if lb < 1 { r.no_quiet_range("MINMAX", label); continue; }
        r.quiet("MINMAX", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMin: Vec<f64> = Vec::with_capacity(1);
            let mut outMax: Vec<f64> = Vec::with_capacity(1);
            core.MINMAX(0, lb - 1, &inReal, optInTimePeriod, &mut outMin, &mut outMax)
        }));
    }
}

fn legs_MINMAX(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MINMAX_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MINMAX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outMin: Vec<f64> = vec![Default::default(); 5];
        let mut outMax: Vec<f64> = vec![Default::default(); 5];
        r.leg("MINMAX", "inReal", 0, run(|| core.MINMAX(startIdx, endIdx, &inReal, optInTimePeriod, &mut outMin, &mut outMax)));
    }
    r.legs_done("MINMAX", 1);
}

const V_MINMAXINDEX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_MINMAXINDEX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MINMAXINDEX {
        let lb = core.MINMAXINDEX_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MINMAXINDEX", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMinIdx: Vec<i32> = Vec::with_capacity(1);
            let mut outMaxIdx: Vec<i32> = Vec::with_capacity(1);
            core.MINMAXINDEX(0, lb, &inReal, optInTimePeriod, &mut outMinIdx, &mut outMaxIdx)
        }));
        if lb < 1 { r.no_quiet_range("MINMAXINDEX", label); continue; }
        r.quiet("MINMAXINDEX", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outMinIdx: Vec<i32> = Vec::with_capacity(1);
            let mut outMaxIdx: Vec<i32> = Vec::with_capacity(1);
            core.MINMAXINDEX(0, lb - 1, &inReal, optInTimePeriod, &mut outMinIdx, &mut outMaxIdx)
        }));
    }
}

fn legs_MINMAXINDEX(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MINMAXINDEX_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MINMAXINDEX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outMinIdx: Vec<i32> = vec![Default::default(); 5];
        let mut outMaxIdx: Vec<i32> = vec![Default::default(); 5];
        r.leg("MINMAXINDEX", "inReal", 0, run(|| core.MINMAXINDEX(startIdx, endIdx, &inReal, optInTimePeriod, &mut outMinIdx, &mut outMaxIdx)));
    }
    r.legs_done("MINMAXINDEX", 1);
}

const V_MINUS_DI: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_MINUS_DI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MINUS_DI {
        let lb = core.MINUS_DI_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MINUS_DI", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MINUS_DI(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MINUS_DI", label); continue; }
        r.quiet("MINUS_DI", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MINUS_DI(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_MINUS_DI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MINUS_DI_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MINUS_DI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MINUS_DI", "inHigh", 0, run(|| core.MINUS_DI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MINUS_DI", "inLow", 1, run(|| core.MINUS_DI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MINUS_DI", "inClose", 2, run(|| core.MINUS_DI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("MINUS_DI", 3);
}

const V_MINUS_DM: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_MINUS_DM(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MINUS_DM {
        let lb = core.MINUS_DM_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MINUS_DM", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MINUS_DM(0, lb, &inHigh, &inLow, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MINUS_DM", label); continue; }
        r.quiet("MINUS_DM", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MINUS_DM(0, lb - 1, &inHigh, &inLow, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_MINUS_DM(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MINUS_DM_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MINUS_DM"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MINUS_DM", "inHigh", 0, run(|| core.MINUS_DM(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MINUS_DM", "inLow", 1, run(|| core.MINUS_DM(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("MINUS_DM", 2);
}

const V_MOM: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_MOM(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_MOM {
        let lb = core.MOM_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("MOM", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MOM(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MOM", label); continue; }
        r.quiet("MOM", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MOM(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_MOM(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.MOM_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("MOM"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MOM", "inReal", 0, run(|| core.MOM(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("MOM", 1);
}

const V_MULT: &[&str] = &[
    "defaults",
];

fn sub_MULT(r: &mut Report) {
    let core = Core::new();
    for &label in V_MULT {
        let lb = core.MULT_Lookback();
        if lb == usize::MAX { continue; }
        r.control("MULT", label, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MULT(0, lb, &inReal0, &inReal1, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("MULT", label); continue; }
        r.quiet("MULT", label, lb, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.MULT(0, lb - 1, &inReal0, &inReal1, &mut outReal)
        }));
    }
}

fn legs_MULT(r: &mut Report) {
    let core = Core::new();
    let lb = core.MULT_Lookback();
    if lb == usize::MAX { r.no_legs("MULT"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal0: Vec<f64> = Vec::with_capacity(1);
        let inReal1: Vec<f64> = series("real", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MULT", "inReal0", 0, run(|| core.MULT(startIdx, endIdx, &inReal0, &inReal1, &mut outReal)));
    }
    {
        let inReal0: Vec<f64> = series("real", endIdx + 1);
        let inReal1: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("MULT", "inReal1", 1, run(|| core.MULT(startIdx, endIdx, &inReal0, &inReal1, &mut outReal)));
    }
    r.legs_done("MULT", 2);
}

const V_NATR: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_NATR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_NATR {
        let lb = core.NATR_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("NATR", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.NATR(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("NATR", label); continue; }
        r.quiet("NATR", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.NATR(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_NATR(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.NATR_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("NATR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("NATR", "inHigh", 0, run(|| core.NATR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("NATR", "inLow", 1, run(|| core.NATR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("NATR", "inClose", 2, run(|| core.NATR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("NATR", 3);
}

const V_NVI: &[&str] = &[
    "defaults",
];

fn sub_NVI(r: &mut Report) {
    let core = Core::new();
    for &label in V_NVI {
        let lb = core.NVI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("NVI", label, run(|| {
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.NVI(0, lb, &inClose, &inVolume, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("NVI", label); continue; }
        r.quiet("NVI", label, lb, run(|| {
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.NVI(0, lb - 1, &inClose, &inVolume, &mut outReal)
        }));
    }
}

fn legs_NVI(r: &mut Report) {
    let core = Core::new();
    let lb = core.NVI_Lookback();
    if lb == usize::MAX { r.no_legs("NVI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("NVI", "inClose", 0, run(|| core.NVI(startIdx, endIdx, &inClose, &inVolume, &mut outReal)));
    }
    {
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("NVI", "inVolume", 1, run(|| core.NVI(startIdx, endIdx, &inClose, &inVolume, &mut outReal)));
    }
    r.legs_done("NVI", 2);
}

const V_OBV: &[&str] = &[
    "defaults",
];

fn sub_OBV(r: &mut Report) {
    let core = Core::new();
    for &label in V_OBV {
        let lb = core.OBV_Lookback();
        if lb == usize::MAX { continue; }
        r.control("OBV", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.OBV(0, lb, &inReal, &inVolume, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("OBV", label); continue; }
        r.quiet("OBV", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.OBV(0, lb - 1, &inReal, &inVolume, &mut outReal)
        }));
    }
}

fn legs_OBV(r: &mut Report) {
    let core = Core::new();
    let lb = core.OBV_Lookback();
    if lb == usize::MAX { r.no_legs("OBV"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("OBV", "inReal", 0, run(|| core.OBV(startIdx, endIdx, &inReal, &inVolume, &mut outReal)));
    }
    {
        let inReal: Vec<f64> = series("real", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("OBV", "inVolume", 1, run(|| core.OBV(startIdx, endIdx, &inReal, &inVolume, &mut outReal)));
    }
    r.legs_done("OBV", 2);
}

const V_PLUS_DI: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_PLUS_DI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_PLUS_DI {
        let lb = core.PLUS_DI_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("PLUS_DI", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PLUS_DI(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("PLUS_DI", label); continue; }
        r.quiet("PLUS_DI", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PLUS_DI(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_PLUS_DI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.PLUS_DI_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("PLUS_DI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PLUS_DI", "inHigh", 0, run(|| core.PLUS_DI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PLUS_DI", "inLow", 1, run(|| core.PLUS_DI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PLUS_DI", "inClose", 2, run(|| core.PLUS_DI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("PLUS_DI", 3);
}

const V_PLUS_DM: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_PLUS_DM(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_PLUS_DM {
        let lb = core.PLUS_DM_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("PLUS_DM", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PLUS_DM(0, lb, &inHigh, &inLow, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("PLUS_DM", label); continue; }
        r.quiet("PLUS_DM", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PLUS_DM(0, lb - 1, &inHigh, &inLow, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_PLUS_DM(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.PLUS_DM_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("PLUS_DM"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PLUS_DM", "inHigh", 0, run(|| core.PLUS_DM(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PLUS_DM", "inLow", 1, run(|| core.PLUS_DM(startIdx, endIdx, &inHigh, &inLow, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("PLUS_DM", 2);
}

const V_PPO: &[(&str, i32, i32, MAType)] = &[
    ("defaults", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("minimums", 2i32, 2i32, MAType::DEFAULT),
    ("optInMAType=SMA", i32::MIN, i32::MIN, MAType::SMA),
    ("optInMAType=SMA, periods doubled", 24i32, 52i32, MAType::SMA),
    ("optInMAType=EMA", i32::MIN, i32::MIN, MAType::EMA),
    ("optInMAType=EMA, periods doubled", 24i32, 52i32, MAType::EMA),
    ("optInMAType=WMA", i32::MIN, i32::MIN, MAType::WMA),
    ("optInMAType=WMA, periods doubled", 24i32, 52i32, MAType::WMA),
    ("optInMAType=DEMA", i32::MIN, i32::MIN, MAType::DEMA),
    ("optInMAType=DEMA, periods doubled", 24i32, 52i32, MAType::DEMA),
    ("optInMAType=TEMA", i32::MIN, i32::MIN, MAType::TEMA),
    ("optInMAType=TEMA, periods doubled", 24i32, 52i32, MAType::TEMA),
    ("optInMAType=TRIMA", i32::MIN, i32::MIN, MAType::TRIMA),
    ("optInMAType=TRIMA, periods doubled", 24i32, 52i32, MAType::TRIMA),
    ("optInMAType=KAMA", i32::MIN, i32::MIN, MAType::KAMA),
    ("optInMAType=KAMA, periods doubled", 24i32, 52i32, MAType::KAMA),
    ("optInMAType=MAMA", i32::MIN, i32::MIN, MAType::MAMA),
    ("optInMAType=MAMA, periods doubled", 24i32, 52i32, MAType::MAMA),
    ("optInMAType=T3", i32::MIN, i32::MIN, MAType::T3),
    ("optInMAType=T3, periods doubled", 24i32, 52i32, MAType::T3),
    ("optInMAType=HMA", i32::MIN, i32::MIN, MAType::HMA),
    ("optInMAType=HMA, periods doubled", 24i32, 52i32, MAType::HMA),
    ("optInMAType=DISABLED", i32::MIN, i32::MIN, MAType::DISABLED),
    ("optInMAType=DISABLED, periods doubled", 24i32, 52i32, MAType::DISABLED),
    ("optInMAType=DEFAULT", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("optInMAType=DEFAULT, periods doubled", 24i32, 52i32, MAType::DEFAULT),
];

fn sub_PPO(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastPeriod, optInSlowPeriod, optInMAType) in V_PPO {
        let lb = core.PPO_Lookback(optInFastPeriod, optInSlowPeriod, optInMAType);
        if lb == usize::MAX { continue; }
        r.control("PPO", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PPO(0, lb, &inReal, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("PPO", label); continue; }
        r.quiet("PPO", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PPO(0, lb - 1, &inReal, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)
        }));
    }
}

fn legs_PPO(r: &mut Report) {
    let core = Core::new();
    let optInFastPeriod = i32::MIN;
    let optInSlowPeriod = i32::MIN;
    let optInMAType = MAType::DEFAULT;
    let lb = core.PPO_Lookback(optInFastPeriod, optInSlowPeriod, optInMAType);
    if lb == usize::MAX { r.no_legs("PPO"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PPO", "inReal", 0, run(|| core.PPO(startIdx, endIdx, &inReal, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)));
    }
    r.legs_done("PPO", 1);
}

const V_PVI: &[&str] = &[
    "defaults",
];

fn sub_PVI(r: &mut Report) {
    let core = Core::new();
    for &label in V_PVI {
        let lb = core.PVI_Lookback();
        if lb == usize::MAX { continue; }
        r.control("PVI", label, run(|| {
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PVI(0, lb, &inClose, &inVolume, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("PVI", label); continue; }
        r.quiet("PVI", label, lb, run(|| {
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PVI(0, lb - 1, &inClose, &inVolume, &mut outReal)
        }));
    }
}

fn legs_PVI(r: &mut Report) {
    let core = Core::new();
    let lb = core.PVI_Lookback();
    if lb == usize::MAX { r.no_legs("PVI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PVI", "inClose", 0, run(|| core.PVI(startIdx, endIdx, &inClose, &inVolume, &mut outReal)));
    }
    {
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PVI", "inVolume", 1, run(|| core.PVI(startIdx, endIdx, &inClose, &inVolume, &mut outReal)));
    }
    r.legs_done("PVI", 2);
}

const V_PVO: &[(&str, i32, i32, MAType)] = &[
    ("defaults", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("minimums", 2i32, 2i32, MAType::DEFAULT),
    ("optInMAType=SMA", i32::MIN, i32::MIN, MAType::SMA),
    ("optInMAType=SMA, periods doubled", 24i32, 52i32, MAType::SMA),
    ("optInMAType=EMA", i32::MIN, i32::MIN, MAType::EMA),
    ("optInMAType=EMA, periods doubled", 24i32, 52i32, MAType::EMA),
    ("optInMAType=WMA", i32::MIN, i32::MIN, MAType::WMA),
    ("optInMAType=WMA, periods doubled", 24i32, 52i32, MAType::WMA),
    ("optInMAType=DEMA", i32::MIN, i32::MIN, MAType::DEMA),
    ("optInMAType=DEMA, periods doubled", 24i32, 52i32, MAType::DEMA),
    ("optInMAType=TEMA", i32::MIN, i32::MIN, MAType::TEMA),
    ("optInMAType=TEMA, periods doubled", 24i32, 52i32, MAType::TEMA),
    ("optInMAType=TRIMA", i32::MIN, i32::MIN, MAType::TRIMA),
    ("optInMAType=TRIMA, periods doubled", 24i32, 52i32, MAType::TRIMA),
    ("optInMAType=KAMA", i32::MIN, i32::MIN, MAType::KAMA),
    ("optInMAType=KAMA, periods doubled", 24i32, 52i32, MAType::KAMA),
    ("optInMAType=MAMA", i32::MIN, i32::MIN, MAType::MAMA),
    ("optInMAType=MAMA, periods doubled", 24i32, 52i32, MAType::MAMA),
    ("optInMAType=T3", i32::MIN, i32::MIN, MAType::T3),
    ("optInMAType=T3, periods doubled", 24i32, 52i32, MAType::T3),
    ("optInMAType=HMA", i32::MIN, i32::MIN, MAType::HMA),
    ("optInMAType=HMA, periods doubled", 24i32, 52i32, MAType::HMA),
    ("optInMAType=DISABLED", i32::MIN, i32::MIN, MAType::DISABLED),
    ("optInMAType=DISABLED, periods doubled", 24i32, 52i32, MAType::DISABLED),
    ("optInMAType=DEFAULT", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("optInMAType=DEFAULT, periods doubled", 24i32, 52i32, MAType::DEFAULT),
];

fn sub_PVO(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastPeriod, optInSlowPeriod, optInMAType) in V_PVO {
        let lb = core.PVO_Lookback(optInFastPeriod, optInSlowPeriod, optInMAType);
        if lb == usize::MAX { continue; }
        r.control("PVO", label, run(|| {
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PVO(0, lb, &inVolume, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("PVO", label); continue; }
        r.quiet("PVO", label, lb, run(|| {
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.PVO(0, lb - 1, &inVolume, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)
        }));
    }
}

fn legs_PVO(r: &mut Report) {
    let core = Core::new();
    let optInFastPeriod = i32::MIN;
    let optInSlowPeriod = i32::MIN;
    let optInMAType = MAType::DEFAULT;
    let lb = core.PVO_Lookback(optInFastPeriod, optInSlowPeriod, optInMAType);
    if lb == usize::MAX { r.no_legs("PVO"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("PVO", "inVolume", 0, run(|| core.PVO(startIdx, endIdx, &inVolume, optInFastPeriod, optInSlowPeriod, optInMAType, &mut outReal)));
    }
    r.legs_done("PVO", 1);
}

const V_QSTICK: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_QSTICK(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_QSTICK {
        let lb = core.QSTICK_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("QSTICK", label, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.QSTICK(0, lb, &inOpen, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("QSTICK", label); continue; }
        r.quiet("QSTICK", label, lb, run(|| {
            let inOpen: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.QSTICK(0, lb - 1, &inOpen, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_QSTICK(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.QSTICK_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("QSTICK"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inOpen: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("QSTICK", "inOpen", 0, run(|| core.QSTICK(startIdx, endIdx, &inOpen, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inOpen: Vec<f64> = series("open", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("QSTICK", "inClose", 1, run(|| core.QSTICK(startIdx, endIdx, &inOpen, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("QSTICK", 2);
}

const V_ROC: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_ROC(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_ROC {
        let lb = core.ROC_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("ROC", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ROC(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ROC", label); continue; }
        r.quiet("ROC", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ROC(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_ROC(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.ROC_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("ROC"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ROC", "inReal", 0, run(|| core.ROC(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("ROC", 1);
}

const V_ROCP: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_ROCP(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_ROCP {
        let lb = core.ROCP_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("ROCP", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ROCP(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ROCP", label); continue; }
        r.quiet("ROCP", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ROCP(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_ROCP(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.ROCP_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("ROCP"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ROCP", "inReal", 0, run(|| core.ROCP(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("ROCP", 1);
}

const V_ROCR: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_ROCR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_ROCR {
        let lb = core.ROCR_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("ROCR", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ROCR(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ROCR", label); continue; }
        r.quiet("ROCR", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ROCR(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_ROCR(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.ROCR_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("ROCR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ROCR", "inReal", 0, run(|| core.ROCR(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("ROCR", 1);
}

const V_ROCR100: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_ROCR100(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_ROCR100 {
        let lb = core.ROCR100_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("ROCR100", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ROCR100(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ROCR100", label); continue; }
        r.quiet("ROCR100", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ROCR100(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_ROCR100(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.ROCR100_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("ROCR100"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ROCR100", "inReal", 0, run(|| core.ROCR100(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("ROCR100", 1);
}

const V_RSI: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_RSI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_RSI {
        let lb = core.RSI_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("RSI", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.RSI(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("RSI", label); continue; }
        r.quiet("RSI", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.RSI(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_RSI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.RSI_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("RSI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("RSI", "inReal", 0, run(|| core.RSI(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("RSI", 1);
}

const V_SAR: &[(&str, f64, f64)] = &[
    ("defaults", Core::REAL_DEFAULT, Core::REAL_DEFAULT),
    ("minimums", 0.0f64, 0.0f64),
];

fn sub_SAR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInAcceleration, optInMaximum) in V_SAR {
        let lb = core.SAR_Lookback(optInAcceleration, optInMaximum);
        if lb == usize::MAX { continue; }
        r.control("SAR", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SAR(0, lb, &inHigh, &inLow, optInAcceleration, optInMaximum, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("SAR", label); continue; }
        r.quiet("SAR", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SAR(0, lb - 1, &inHigh, &inLow, optInAcceleration, optInMaximum, &mut outReal)
        }));
    }
}

fn legs_SAR(r: &mut Report) {
    let core = Core::new();
    let optInAcceleration = Core::REAL_DEFAULT;
    let optInMaximum = Core::REAL_DEFAULT;
    let lb = core.SAR_Lookback(optInAcceleration, optInMaximum);
    if lb == usize::MAX { r.no_legs("SAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SAR", "inHigh", 0, run(|| core.SAR(startIdx, endIdx, &inHigh, &inLow, optInAcceleration, optInMaximum, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SAR", "inLow", 1, run(|| core.SAR(startIdx, endIdx, &inHigh, &inLow, optInAcceleration, optInMaximum, &mut outReal)));
    }
    r.legs_done("SAR", 2);
}

const V_SAREXT: &[(&str, f64, f64, f64, f64, f64, f64, f64, f64)] = &[
    ("defaults", Core::REAL_DEFAULT, Core::REAL_DEFAULT, Core::REAL_DEFAULT, Core::REAL_DEFAULT, Core::REAL_DEFAULT, Core::REAL_DEFAULT, Core::REAL_DEFAULT, Core::REAL_DEFAULT),
    ("minimums", -3e37f64, 0.0f64, 0.0f64, 0.0f64, 0.0f64, 0.0f64, 0.0f64, 0.0f64),
];

fn sub_SAREXT(r: &mut Report) {
    let core = Core::new();
    for &(label, optInStartValue, optInOffsetOnReverse, optInAccelerationInitLong, optInAccelerationLong, optInAccelerationMaxLong, optInAccelerationInitShort, optInAccelerationShort, optInAccelerationMaxShort) in V_SAREXT {
        let lb = core.SAREXT_Lookback(optInStartValue, optInOffsetOnReverse, optInAccelerationInitLong, optInAccelerationLong, optInAccelerationMaxLong, optInAccelerationInitShort, optInAccelerationShort, optInAccelerationMaxShort);
        if lb == usize::MAX { continue; }
        r.control("SAREXT", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SAREXT(0, lb, &inHigh, &inLow, optInStartValue, optInOffsetOnReverse, optInAccelerationInitLong, optInAccelerationLong, optInAccelerationMaxLong, optInAccelerationInitShort, optInAccelerationShort, optInAccelerationMaxShort, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("SAREXT", label); continue; }
        r.quiet("SAREXT", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SAREXT(0, lb - 1, &inHigh, &inLow, optInStartValue, optInOffsetOnReverse, optInAccelerationInitLong, optInAccelerationLong, optInAccelerationMaxLong, optInAccelerationInitShort, optInAccelerationShort, optInAccelerationMaxShort, &mut outReal)
        }));
    }
}

fn legs_SAREXT(r: &mut Report) {
    let core = Core::new();
    let optInStartValue = Core::REAL_DEFAULT;
    let optInOffsetOnReverse = Core::REAL_DEFAULT;
    let optInAccelerationInitLong = Core::REAL_DEFAULT;
    let optInAccelerationLong = Core::REAL_DEFAULT;
    let optInAccelerationMaxLong = Core::REAL_DEFAULT;
    let optInAccelerationInitShort = Core::REAL_DEFAULT;
    let optInAccelerationShort = Core::REAL_DEFAULT;
    let optInAccelerationMaxShort = Core::REAL_DEFAULT;
    let lb = core.SAREXT_Lookback(optInStartValue, optInOffsetOnReverse, optInAccelerationInitLong, optInAccelerationLong, optInAccelerationMaxLong, optInAccelerationInitShort, optInAccelerationShort, optInAccelerationMaxShort);
    if lb == usize::MAX { r.no_legs("SAREXT"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SAREXT", "inHigh", 0, run(|| core.SAREXT(startIdx, endIdx, &inHigh, &inLow, optInStartValue, optInOffsetOnReverse, optInAccelerationInitLong, optInAccelerationLong, optInAccelerationMaxLong, optInAccelerationInitShort, optInAccelerationShort, optInAccelerationMaxShort, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SAREXT", "inLow", 1, run(|| core.SAREXT(startIdx, endIdx, &inHigh, &inLow, optInStartValue, optInOffsetOnReverse, optInAccelerationInitLong, optInAccelerationLong, optInAccelerationMaxLong, optInAccelerationInitShort, optInAccelerationShort, optInAccelerationMaxShort, &mut outReal)));
    }
    r.legs_done("SAREXT", 2);
}

const V_SIN: &[&str] = &[
    "defaults",
];

fn sub_SIN(r: &mut Report) {
    let core = Core::new();
    for &label in V_SIN {
        let lb = core.SIN_Lookback();
        if lb == usize::MAX { continue; }
        r.control("SIN", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SIN(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("SIN", label); continue; }
        r.quiet("SIN", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SIN(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_SIN(r: &mut Report) {
    let core = Core::new();
    let lb = core.SIN_Lookback();
    if lb == usize::MAX { r.no_legs("SIN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SIN", "inReal", 0, run(|| core.SIN(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("SIN", 1);
}

const V_SINH: &[&str] = &[
    "defaults",
];

fn sub_SINH(r: &mut Report) {
    let core = Core::new();
    for &label in V_SINH {
        let lb = core.SINH_Lookback();
        if lb == usize::MAX { continue; }
        r.control("SINH", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SINH(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("SINH", label); continue; }
        r.quiet("SINH", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SINH(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_SINH(r: &mut Report) {
    let core = Core::new();
    let lb = core.SINH_Lookback();
    if lb == usize::MAX { r.no_legs("SINH"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SINH", "inReal", 0, run(|| core.SINH(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("SINH", 1);
}

const V_SMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_SMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_SMA {
        let lb = core.SMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("SMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SMA(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("SMA", label); continue; }
        r.quiet("SMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SMA(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_SMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.SMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("SMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SMA", "inReal", 0, run(|| core.SMA(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("SMA", 1);
}

const V_SMI: &[(&str, i32, i32, i32, i32)] = &[
    ("defaults", i32::MIN, i32::MIN, i32::MIN, i32::MIN),
    ("minimums", 2i32, 2i32, 2i32, 2i32),
];

fn sub_SMI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod, optInFastPeriod, optInSlowPeriod, optInSignalPeriod) in V_SMI {
        let lb = core.SMI_Lookback(optInTimePeriod, optInFastPeriod, optInSlowPeriod, optInSignalPeriod);
        if lb == usize::MAX { continue; }
        r.control("SMI", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outSMI: Vec<f64> = Vec::with_capacity(1);
            let mut outSMISignal: Vec<f64> = Vec::with_capacity(1);
            core.SMI(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outSMI, &mut outSMISignal)
        }));
        if lb < 1 { r.no_quiet_range("SMI", label); continue; }
        r.quiet("SMI", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outSMI: Vec<f64> = Vec::with_capacity(1);
            let mut outSMISignal: Vec<f64> = Vec::with_capacity(1);
            core.SMI(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outSMI, &mut outSMISignal)
        }));
    }
}

fn legs_SMI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let optInFastPeriod = i32::MIN;
    let optInSlowPeriod = i32::MIN;
    let optInSignalPeriod = i32::MIN;
    let lb = core.SMI_Lookback(optInTimePeriod, optInFastPeriod, optInSlowPeriod, optInSignalPeriod);
    if lb == usize::MAX { r.no_legs("SMI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outSMI: Vec<f64> = vec![Default::default(); 5];
        let mut outSMISignal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SMI", "inHigh", 0, run(|| core.SMI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outSMI, &mut outSMISignal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outSMI: Vec<f64> = vec![Default::default(); 5];
        let mut outSMISignal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SMI", "inLow", 1, run(|| core.SMI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outSMI, &mut outSMISignal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outSMI: Vec<f64> = vec![Default::default(); 5];
        let mut outSMISignal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SMI", "inClose", 2, run(|| core.SMI(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, optInFastPeriod, optInSlowPeriod, optInSignalPeriod, &mut outSMI, &mut outSMISignal)));
    }
    r.legs_done("SMI", 3);
}

const V_SQRT: &[&str] = &[
    "defaults",
];

fn sub_SQRT(r: &mut Report) {
    let core = Core::new();
    for &label in V_SQRT {
        let lb = core.SQRT_Lookback();
        if lb == usize::MAX { continue; }
        r.control("SQRT", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SQRT(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("SQRT", label); continue; }
        r.quiet("SQRT", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SQRT(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_SQRT(r: &mut Report) {
    let core = Core::new();
    let lb = core.SQRT_Lookback();
    if lb == usize::MAX { r.no_legs("SQRT"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SQRT", "inReal", 0, run(|| core.SQRT(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("SQRT", 1);
}

const V_STDDEV: &[(&str, i32, f64)] = &[
    ("defaults", i32::MIN, Core::REAL_DEFAULT),
    ("minimums", 2i32, -3e37f64),
];

fn sub_STDDEV(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod, optInNbDev) in V_STDDEV {
        let lb = core.STDDEV_Lookback(optInTimePeriod, optInNbDev);
        if lb == usize::MAX { continue; }
        r.control("STDDEV", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.STDDEV(0, lb, &inReal, optInTimePeriod, optInNbDev, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("STDDEV", label); continue; }
        r.quiet("STDDEV", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.STDDEV(0, lb - 1, &inReal, optInTimePeriod, optInNbDev, &mut outReal)
        }));
    }
}

fn legs_STDDEV(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let optInNbDev = Core::REAL_DEFAULT;
    let lb = core.STDDEV_Lookback(optInTimePeriod, optInNbDev);
    if lb == usize::MAX { r.no_legs("STDDEV"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("STDDEV", "inReal", 0, run(|| core.STDDEV(startIdx, endIdx, &inReal, optInTimePeriod, optInNbDev, &mut outReal)));
    }
    r.legs_done("STDDEV", 1);
}

const V_STOCH: &[(&str, i32, i32, MAType, i32, MAType)] = &[
    ("defaults", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("minimums", 1i32, 1i32, MAType::DEFAULT, 1i32, MAType::DEFAULT),
    ("optInSlowK_MAType=SMA", i32::MIN, i32::MIN, MAType::SMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=SMA, periods doubled", 10i32, 6i32, MAType::SMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=EMA", i32::MIN, i32::MIN, MAType::EMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=EMA, periods doubled", 10i32, 6i32, MAType::EMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=WMA", i32::MIN, i32::MIN, MAType::WMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=WMA, periods doubled", 10i32, 6i32, MAType::WMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=DEMA", i32::MIN, i32::MIN, MAType::DEMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=DEMA, periods doubled", 10i32, 6i32, MAType::DEMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=TEMA", i32::MIN, i32::MIN, MAType::TEMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=TEMA, periods doubled", 10i32, 6i32, MAType::TEMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=TRIMA", i32::MIN, i32::MIN, MAType::TRIMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=TRIMA, periods doubled", 10i32, 6i32, MAType::TRIMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=KAMA", i32::MIN, i32::MIN, MAType::KAMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=KAMA, periods doubled", 10i32, 6i32, MAType::KAMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=MAMA", i32::MIN, i32::MIN, MAType::MAMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=MAMA, periods doubled", 10i32, 6i32, MAType::MAMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=T3", i32::MIN, i32::MIN, MAType::T3, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=T3, periods doubled", 10i32, 6i32, MAType::T3, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=HMA", i32::MIN, i32::MIN, MAType::HMA, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=HMA, periods doubled", 10i32, 6i32, MAType::HMA, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=DISABLED", i32::MIN, i32::MIN, MAType::DISABLED, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=DISABLED, periods doubled", 10i32, 6i32, MAType::DISABLED, 6i32, MAType::DEFAULT),
    ("optInSlowK_MAType=DEFAULT", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInSlowK_MAType=DEFAULT, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::DEFAULT),
    ("optInSlowD_MAType=SMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::SMA),
    ("optInSlowD_MAType=SMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::SMA),
    ("optInSlowD_MAType=EMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::EMA),
    ("optInSlowD_MAType=EMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::EMA),
    ("optInSlowD_MAType=WMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::WMA),
    ("optInSlowD_MAType=WMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::WMA),
    ("optInSlowD_MAType=DEMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEMA),
    ("optInSlowD_MAType=DEMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::DEMA),
    ("optInSlowD_MAType=TEMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::TEMA),
    ("optInSlowD_MAType=TEMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::TEMA),
    ("optInSlowD_MAType=TRIMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::TRIMA),
    ("optInSlowD_MAType=TRIMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::TRIMA),
    ("optInSlowD_MAType=KAMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::KAMA),
    ("optInSlowD_MAType=KAMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::KAMA),
    ("optInSlowD_MAType=MAMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::MAMA),
    ("optInSlowD_MAType=MAMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::MAMA),
    ("optInSlowD_MAType=T3", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::T3),
    ("optInSlowD_MAType=T3, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::T3),
    ("optInSlowD_MAType=HMA", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::HMA),
    ("optInSlowD_MAType=HMA, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::HMA),
    ("optInSlowD_MAType=DISABLED", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DISABLED),
    ("optInSlowD_MAType=DISABLED, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::DISABLED),
    ("optInSlowD_MAType=DEFAULT", i32::MIN, i32::MIN, MAType::DEFAULT, i32::MIN, MAType::DEFAULT),
    ("optInSlowD_MAType=DEFAULT, periods doubled", 10i32, 6i32, MAType::DEFAULT, 6i32, MAType::DEFAULT),
];

fn sub_STOCH(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastK_Period, optInSlowK_Period, optInSlowK_MAType, optInSlowD_Period, optInSlowD_MAType) in V_STOCH {
        let lb = core.STOCH_Lookback(optInFastK_Period, optInSlowK_Period, optInSlowK_MAType, optInSlowD_Period, optInSlowD_MAType);
        if lb == usize::MAX { continue; }
        r.control("STOCH", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outSlowK: Vec<f64> = Vec::with_capacity(1);
            let mut outSlowD: Vec<f64> = Vec::with_capacity(1);
            core.STOCH(0, lb, &inHigh, &inLow, &inClose, optInFastK_Period, optInSlowK_Period, optInSlowK_MAType, optInSlowD_Period, optInSlowD_MAType, &mut outSlowK, &mut outSlowD)
        }));
        if lb < 1 { r.no_quiet_range("STOCH", label); continue; }
        r.quiet("STOCH", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outSlowK: Vec<f64> = Vec::with_capacity(1);
            let mut outSlowD: Vec<f64> = Vec::with_capacity(1);
            core.STOCH(0, lb - 1, &inHigh, &inLow, &inClose, optInFastK_Period, optInSlowK_Period, optInSlowK_MAType, optInSlowD_Period, optInSlowD_MAType, &mut outSlowK, &mut outSlowD)
        }));
    }
}

fn legs_STOCH(r: &mut Report) {
    let core = Core::new();
    let optInFastK_Period = i32::MIN;
    let optInSlowK_Period = i32::MIN;
    let optInSlowK_MAType = MAType::DEFAULT;
    let optInSlowD_Period = i32::MIN;
    let optInSlowD_MAType = MAType::DEFAULT;
    let lb = core.STOCH_Lookback(optInFastK_Period, optInSlowK_Period, optInSlowK_MAType, optInSlowD_Period, optInSlowD_MAType);
    if lb == usize::MAX { r.no_legs("STOCH"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outSlowK: Vec<f64> = vec![Default::default(); 5];
        let mut outSlowD: Vec<f64> = vec![Default::default(); 5];
        r.leg("STOCH", "inHigh", 0, run(|| core.STOCH(startIdx, endIdx, &inHigh, &inLow, &inClose, optInFastK_Period, optInSlowK_Period, optInSlowK_MAType, optInSlowD_Period, optInSlowD_MAType, &mut outSlowK, &mut outSlowD)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outSlowK: Vec<f64> = vec![Default::default(); 5];
        let mut outSlowD: Vec<f64> = vec![Default::default(); 5];
        r.leg("STOCH", "inLow", 1, run(|| core.STOCH(startIdx, endIdx, &inHigh, &inLow, &inClose, optInFastK_Period, optInSlowK_Period, optInSlowK_MAType, optInSlowD_Period, optInSlowD_MAType, &mut outSlowK, &mut outSlowD)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outSlowK: Vec<f64> = vec![Default::default(); 5];
        let mut outSlowD: Vec<f64> = vec![Default::default(); 5];
        r.leg("STOCH", "inClose", 2, run(|| core.STOCH(startIdx, endIdx, &inHigh, &inLow, &inClose, optInFastK_Period, optInSlowK_Period, optInSlowK_MAType, optInSlowD_Period, optInSlowD_MAType, &mut outSlowK, &mut outSlowD)));
    }
    r.legs_done("STOCH", 3);
}

const V_STOCHF: &[(&str, i32, i32, MAType)] = &[
    ("defaults", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("minimums", 1i32, 1i32, MAType::DEFAULT),
    ("optInFastD_MAType=SMA", i32::MIN, i32::MIN, MAType::SMA),
    ("optInFastD_MAType=SMA, periods doubled", 10i32, 6i32, MAType::SMA),
    ("optInFastD_MAType=EMA", i32::MIN, i32::MIN, MAType::EMA),
    ("optInFastD_MAType=EMA, periods doubled", 10i32, 6i32, MAType::EMA),
    ("optInFastD_MAType=WMA", i32::MIN, i32::MIN, MAType::WMA),
    ("optInFastD_MAType=WMA, periods doubled", 10i32, 6i32, MAType::WMA),
    ("optInFastD_MAType=DEMA", i32::MIN, i32::MIN, MAType::DEMA),
    ("optInFastD_MAType=DEMA, periods doubled", 10i32, 6i32, MAType::DEMA),
    ("optInFastD_MAType=TEMA", i32::MIN, i32::MIN, MAType::TEMA),
    ("optInFastD_MAType=TEMA, periods doubled", 10i32, 6i32, MAType::TEMA),
    ("optInFastD_MAType=TRIMA", i32::MIN, i32::MIN, MAType::TRIMA),
    ("optInFastD_MAType=TRIMA, periods doubled", 10i32, 6i32, MAType::TRIMA),
    ("optInFastD_MAType=KAMA", i32::MIN, i32::MIN, MAType::KAMA),
    ("optInFastD_MAType=KAMA, periods doubled", 10i32, 6i32, MAType::KAMA),
    ("optInFastD_MAType=MAMA", i32::MIN, i32::MIN, MAType::MAMA),
    ("optInFastD_MAType=MAMA, periods doubled", 10i32, 6i32, MAType::MAMA),
    ("optInFastD_MAType=T3", i32::MIN, i32::MIN, MAType::T3),
    ("optInFastD_MAType=T3, periods doubled", 10i32, 6i32, MAType::T3),
    ("optInFastD_MAType=HMA", i32::MIN, i32::MIN, MAType::HMA),
    ("optInFastD_MAType=HMA, periods doubled", 10i32, 6i32, MAType::HMA),
    ("optInFastD_MAType=DISABLED", i32::MIN, i32::MIN, MAType::DISABLED),
    ("optInFastD_MAType=DISABLED, periods doubled", 10i32, 6i32, MAType::DISABLED),
    ("optInFastD_MAType=DEFAULT", i32::MIN, i32::MIN, MAType::DEFAULT),
    ("optInFastD_MAType=DEFAULT, periods doubled", 10i32, 6i32, MAType::DEFAULT),
];

fn sub_STOCHF(r: &mut Report) {
    let core = Core::new();
    for &(label, optInFastK_Period, optInFastD_Period, optInFastD_MAType) in V_STOCHF {
        let lb = core.STOCHF_Lookback(optInFastK_Period, optInFastD_Period, optInFastD_MAType);
        if lb == usize::MAX { continue; }
        r.control("STOCHF", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outFastK: Vec<f64> = Vec::with_capacity(1);
            let mut outFastD: Vec<f64> = Vec::with_capacity(1);
            core.STOCHF(0, lb, &inHigh, &inLow, &inClose, optInFastK_Period, optInFastD_Period, optInFastD_MAType, &mut outFastK, &mut outFastD)
        }));
        if lb < 1 { r.no_quiet_range("STOCHF", label); continue; }
        r.quiet("STOCHF", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outFastK: Vec<f64> = Vec::with_capacity(1);
            let mut outFastD: Vec<f64> = Vec::with_capacity(1);
            core.STOCHF(0, lb - 1, &inHigh, &inLow, &inClose, optInFastK_Period, optInFastD_Period, optInFastD_MAType, &mut outFastK, &mut outFastD)
        }));
    }
}

fn legs_STOCHF(r: &mut Report) {
    let core = Core::new();
    let optInFastK_Period = i32::MIN;
    let optInFastD_Period = i32::MIN;
    let optInFastD_MAType = MAType::DEFAULT;
    let lb = core.STOCHF_Lookback(optInFastK_Period, optInFastD_Period, optInFastD_MAType);
    if lb == usize::MAX { r.no_legs("STOCHF"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outFastK: Vec<f64> = vec![Default::default(); 5];
        let mut outFastD: Vec<f64> = vec![Default::default(); 5];
        r.leg("STOCHF", "inHigh", 0, run(|| core.STOCHF(startIdx, endIdx, &inHigh, &inLow, &inClose, optInFastK_Period, optInFastD_Period, optInFastD_MAType, &mut outFastK, &mut outFastD)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outFastK: Vec<f64> = vec![Default::default(); 5];
        let mut outFastD: Vec<f64> = vec![Default::default(); 5];
        r.leg("STOCHF", "inLow", 1, run(|| core.STOCHF(startIdx, endIdx, &inHigh, &inLow, &inClose, optInFastK_Period, optInFastD_Period, optInFastD_MAType, &mut outFastK, &mut outFastD)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outFastK: Vec<f64> = vec![Default::default(); 5];
        let mut outFastD: Vec<f64> = vec![Default::default(); 5];
        r.leg("STOCHF", "inClose", 2, run(|| core.STOCHF(startIdx, endIdx, &inHigh, &inLow, &inClose, optInFastK_Period, optInFastD_Period, optInFastD_MAType, &mut outFastK, &mut outFastD)));
    }
    r.legs_done("STOCHF", 3);
}

const V_STOCHRSI: &[(&str, i32, i32, i32, MAType)] = &[
    ("defaults", i32::MIN, i32::MIN, i32::MIN, MAType::DEFAULT),
    ("minimums", 2i32, 1i32, 1i32, MAType::DEFAULT),
    ("optInFastD_MAType=SMA", i32::MIN, i32::MIN, i32::MIN, MAType::SMA),
    ("optInFastD_MAType=SMA, periods doubled", 28i32, 10i32, 6i32, MAType::SMA),
    ("optInFastD_MAType=EMA", i32::MIN, i32::MIN, i32::MIN, MAType::EMA),
    ("optInFastD_MAType=EMA, periods doubled", 28i32, 10i32, 6i32, MAType::EMA),
    ("optInFastD_MAType=WMA", i32::MIN, i32::MIN, i32::MIN, MAType::WMA),
    ("optInFastD_MAType=WMA, periods doubled", 28i32, 10i32, 6i32, MAType::WMA),
    ("optInFastD_MAType=DEMA", i32::MIN, i32::MIN, i32::MIN, MAType::DEMA),
    ("optInFastD_MAType=DEMA, periods doubled", 28i32, 10i32, 6i32, MAType::DEMA),
    ("optInFastD_MAType=TEMA", i32::MIN, i32::MIN, i32::MIN, MAType::TEMA),
    ("optInFastD_MAType=TEMA, periods doubled", 28i32, 10i32, 6i32, MAType::TEMA),
    ("optInFastD_MAType=TRIMA", i32::MIN, i32::MIN, i32::MIN, MAType::TRIMA),
    ("optInFastD_MAType=TRIMA, periods doubled", 28i32, 10i32, 6i32, MAType::TRIMA),
    ("optInFastD_MAType=KAMA", i32::MIN, i32::MIN, i32::MIN, MAType::KAMA),
    ("optInFastD_MAType=KAMA, periods doubled", 28i32, 10i32, 6i32, MAType::KAMA),
    ("optInFastD_MAType=MAMA", i32::MIN, i32::MIN, i32::MIN, MAType::MAMA),
    ("optInFastD_MAType=MAMA, periods doubled", 28i32, 10i32, 6i32, MAType::MAMA),
    ("optInFastD_MAType=T3", i32::MIN, i32::MIN, i32::MIN, MAType::T3),
    ("optInFastD_MAType=T3, periods doubled", 28i32, 10i32, 6i32, MAType::T3),
    ("optInFastD_MAType=HMA", i32::MIN, i32::MIN, i32::MIN, MAType::HMA),
    ("optInFastD_MAType=HMA, periods doubled", 28i32, 10i32, 6i32, MAType::HMA),
    ("optInFastD_MAType=DISABLED", i32::MIN, i32::MIN, i32::MIN, MAType::DISABLED),
    ("optInFastD_MAType=DISABLED, periods doubled", 28i32, 10i32, 6i32, MAType::DISABLED),
    ("optInFastD_MAType=DEFAULT", i32::MIN, i32::MIN, i32::MIN, MAType::DEFAULT),
    ("optInFastD_MAType=DEFAULT, periods doubled", 28i32, 10i32, 6i32, MAType::DEFAULT),
];

fn sub_STOCHRSI(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod, optInFastK_Period, optInFastD_Period, optInFastD_MAType) in V_STOCHRSI {
        let lb = core.STOCHRSI_Lookback(optInTimePeriod, optInFastK_Period, optInFastD_Period, optInFastD_MAType);
        if lb == usize::MAX { continue; }
        r.control("STOCHRSI", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outFastK: Vec<f64> = Vec::with_capacity(1);
            let mut outFastD: Vec<f64> = Vec::with_capacity(1);
            core.STOCHRSI(0, lb, &inReal, optInTimePeriod, optInFastK_Period, optInFastD_Period, optInFastD_MAType, &mut outFastK, &mut outFastD)
        }));
        if lb < 1 { r.no_quiet_range("STOCHRSI", label); continue; }
        r.quiet("STOCHRSI", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outFastK: Vec<f64> = Vec::with_capacity(1);
            let mut outFastD: Vec<f64> = Vec::with_capacity(1);
            core.STOCHRSI(0, lb - 1, &inReal, optInTimePeriod, optInFastK_Period, optInFastD_Period, optInFastD_MAType, &mut outFastK, &mut outFastD)
        }));
    }
}

fn legs_STOCHRSI(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let optInFastK_Period = i32::MIN;
    let optInFastD_Period = i32::MIN;
    let optInFastD_MAType = MAType::DEFAULT;
    let lb = core.STOCHRSI_Lookback(optInTimePeriod, optInFastK_Period, optInFastD_Period, optInFastD_MAType);
    if lb == usize::MAX { r.no_legs("STOCHRSI"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outFastK: Vec<f64> = vec![Default::default(); 5];
        let mut outFastD: Vec<f64> = vec![Default::default(); 5];
        r.leg("STOCHRSI", "inReal", 0, run(|| core.STOCHRSI(startIdx, endIdx, &inReal, optInTimePeriod, optInFastK_Period, optInFastD_Period, optInFastD_MAType, &mut outFastK, &mut outFastD)));
    }
    r.legs_done("STOCHRSI", 1);
}

const V_SUB: &[&str] = &[
    "defaults",
];

fn sub_SUB(r: &mut Report) {
    let core = Core::new();
    for &label in V_SUB {
        let lb = core.SUB_Lookback();
        if lb == usize::MAX { continue; }
        r.control("SUB", label, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SUB(0, lb, &inReal0, &inReal1, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("SUB", label); continue; }
        r.quiet("SUB", label, lb, run(|| {
            let inReal0: Vec<f64> = Vec::with_capacity(1);
            let inReal1: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SUB(0, lb - 1, &inReal0, &inReal1, &mut outReal)
        }));
    }
}

fn legs_SUB(r: &mut Report) {
    let core = Core::new();
    let lb = core.SUB_Lookback();
    if lb == usize::MAX { r.no_legs("SUB"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal0: Vec<f64> = Vec::with_capacity(1);
        let inReal1: Vec<f64> = series("real", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SUB", "inReal0", 0, run(|| core.SUB(startIdx, endIdx, &inReal0, &inReal1, &mut outReal)));
    }
    {
        let inReal0: Vec<f64> = series("real", endIdx + 1);
        let inReal1: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SUB", "inReal1", 1, run(|| core.SUB(startIdx, endIdx, &inReal0, &inReal1, &mut outReal)));
    }
    r.legs_done("SUB", 2);
}

const V_SUM: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_SUM(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_SUM {
        let lb = core.SUM_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("SUM", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SUM(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("SUM", label); continue; }
        r.quiet("SUM", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.SUM(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_SUM(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.SUM_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("SUM"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("SUM", "inReal", 0, run(|| core.SUM(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("SUM", 1);
}

const V_T3: &[(&str, i32, f64)] = &[
    ("defaults", i32::MIN, Core::REAL_DEFAULT),
    ("minimums", 1i32, 0.0f64),
];

fn sub_T3(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod, optInVFactor) in V_T3 {
        let lb = core.T3_Lookback(optInTimePeriod, optInVFactor);
        if lb == usize::MAX { continue; }
        r.control("T3", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.T3(0, lb, &inReal, optInTimePeriod, optInVFactor, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("T3", label); continue; }
        r.quiet("T3", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.T3(0, lb - 1, &inReal, optInTimePeriod, optInVFactor, &mut outReal)
        }));
    }
}

fn legs_T3(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let optInVFactor = Core::REAL_DEFAULT;
    let lb = core.T3_Lookback(optInTimePeriod, optInVFactor);
    if lb == usize::MAX { r.no_legs("T3"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("T3", "inReal", 0, run(|| core.T3(startIdx, endIdx, &inReal, optInTimePeriod, optInVFactor, &mut outReal)));
    }
    r.legs_done("T3", 1);
}

const V_TAN: &[&str] = &[
    "defaults",
];

fn sub_TAN(r: &mut Report) {
    let core = Core::new();
    for &label in V_TAN {
        let lb = core.TAN_Lookback();
        if lb == usize::MAX { continue; }
        r.control("TAN", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TAN(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("TAN", label); continue; }
        r.quiet("TAN", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TAN(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_TAN(r: &mut Report) {
    let core = Core::new();
    let lb = core.TAN_Lookback();
    if lb == usize::MAX { r.no_legs("TAN"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TAN", "inReal", 0, run(|| core.TAN(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("TAN", 1);
}

const V_TANH: &[&str] = &[
    "defaults",
];

fn sub_TANH(r: &mut Report) {
    let core = Core::new();
    for &label in V_TANH {
        let lb = core.TANH_Lookback();
        if lb == usize::MAX { continue; }
        r.control("TANH", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TANH(0, lb, &inReal, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("TANH", label); continue; }
        r.quiet("TANH", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TANH(0, lb - 1, &inReal, &mut outReal)
        }));
    }
}

fn legs_TANH(r: &mut Report) {
    let core = Core::new();
    let lb = core.TANH_Lookback();
    if lb == usize::MAX { r.no_legs("TANH"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TANH", "inReal", 0, run(|| core.TANH(startIdx, endIdx, &inReal, &mut outReal)));
    }
    r.legs_done("TANH", 1);
}

const V_TEMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_TEMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_TEMA {
        let lb = core.TEMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("TEMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TEMA(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("TEMA", label); continue; }
        r.quiet("TEMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TEMA(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_TEMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.TEMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("TEMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TEMA", "inReal", 0, run(|| core.TEMA(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("TEMA", 1);
}

const V_TRANGE: &[&str] = &[
    "defaults",
];

fn sub_TRANGE(r: &mut Report) {
    let core = Core::new();
    for &label in V_TRANGE {
        let lb = core.TRANGE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("TRANGE", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TRANGE(0, lb, &inHigh, &inLow, &inClose, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("TRANGE", label); continue; }
        r.quiet("TRANGE", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TRANGE(0, lb - 1, &inHigh, &inLow, &inClose, &mut outReal)
        }));
    }
}

fn legs_TRANGE(r: &mut Report) {
    let core = Core::new();
    let lb = core.TRANGE_Lookback();
    if lb == usize::MAX { r.no_legs("TRANGE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TRANGE", "inHigh", 0, run(|| core.TRANGE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TRANGE", "inLow", 1, run(|| core.TRANGE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TRANGE", "inClose", 2, run(|| core.TRANGE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    r.legs_done("TRANGE", 3);
}

const V_TRIMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_TRIMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_TRIMA {
        let lb = core.TRIMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("TRIMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TRIMA(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("TRIMA", label); continue; }
        r.quiet("TRIMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TRIMA(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_TRIMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.TRIMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("TRIMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TRIMA", "inReal", 0, run(|| core.TRIMA(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("TRIMA", 1);
}

const V_TRIX: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_TRIX(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_TRIX {
        let lb = core.TRIX_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("TRIX", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TRIX(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("TRIX", label); continue; }
        r.quiet("TRIX", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TRIX(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_TRIX(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.TRIX_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("TRIX"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TRIX", "inReal", 0, run(|| core.TRIX(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("TRIX", 1);
}

const V_TSF: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_TSF(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_TSF {
        let lb = core.TSF_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("TSF", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TSF(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("TSF", label); continue; }
        r.quiet("TSF", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TSF(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_TSF(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.TSF_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("TSF"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TSF", "inReal", 0, run(|| core.TSF(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("TSF", 1);
}

const V_TYPPRICE: &[&str] = &[
    "defaults",
];

fn sub_TYPPRICE(r: &mut Report) {
    let core = Core::new();
    for &label in V_TYPPRICE {
        let lb = core.TYPPRICE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("TYPPRICE", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TYPPRICE(0, lb, &inHigh, &inLow, &inClose, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("TYPPRICE", label); continue; }
        r.quiet("TYPPRICE", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.TYPPRICE(0, lb - 1, &inHigh, &inLow, &inClose, &mut outReal)
        }));
    }
}

fn legs_TYPPRICE(r: &mut Report) {
    let core = Core::new();
    let lb = core.TYPPRICE_Lookback();
    if lb == usize::MAX { r.no_legs("TYPPRICE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TYPPRICE", "inHigh", 0, run(|| core.TYPPRICE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TYPPRICE", "inLow", 1, run(|| core.TYPPRICE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("TYPPRICE", "inClose", 2, run(|| core.TYPPRICE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    r.legs_done("TYPPRICE", 3);
}

const V_ULTOSC: &[(&str, i32, i32, i32)] = &[
    ("defaults", i32::MIN, i32::MIN, i32::MIN),
    ("minimums", 1i32, 1i32, 1i32),
];

fn sub_ULTOSC(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod1, optInTimePeriod2, optInTimePeriod3) in V_ULTOSC {
        let lb = core.ULTOSC_Lookback(optInTimePeriod1, optInTimePeriod2, optInTimePeriod3);
        if lb == usize::MAX { continue; }
        r.control("ULTOSC", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ULTOSC(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod1, optInTimePeriod2, optInTimePeriod3, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("ULTOSC", label); continue; }
        r.quiet("ULTOSC", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.ULTOSC(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod1, optInTimePeriod2, optInTimePeriod3, &mut outReal)
        }));
    }
}

fn legs_ULTOSC(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod1 = i32::MIN;
    let optInTimePeriod2 = i32::MIN;
    let optInTimePeriod3 = i32::MIN;
    let lb = core.ULTOSC_Lookback(optInTimePeriod1, optInTimePeriod2, optInTimePeriod3);
    if lb == usize::MAX { r.no_legs("ULTOSC"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ULTOSC", "inHigh", 0, run(|| core.ULTOSC(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod1, optInTimePeriod2, optInTimePeriod3, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ULTOSC", "inLow", 1, run(|| core.ULTOSC(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod1, optInTimePeriod2, optInTimePeriod3, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("ULTOSC", "inClose", 2, run(|| core.ULTOSC(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod1, optInTimePeriod2, optInTimePeriod3, &mut outReal)));
    }
    r.legs_done("ULTOSC", 3);
}

const V_VAR: &[(&str, i32, f64)] = &[
    ("defaults", i32::MIN, Core::REAL_DEFAULT),
    ("minimums", 1i32, -3e37f64),
];

fn sub_VAR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod, optInNbDev) in V_VAR {
        let lb = core.VAR_Lookback(optInTimePeriod, optInNbDev);
        if lb == usize::MAX { continue; }
        r.control("VAR", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.VAR(0, lb, &inReal, optInTimePeriod, optInNbDev, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("VAR", label); continue; }
        r.quiet("VAR", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.VAR(0, lb - 1, &inReal, optInTimePeriod, optInNbDev, &mut outReal)
        }));
    }
}

fn legs_VAR(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let optInNbDev = Core::REAL_DEFAULT;
    let lb = core.VAR_Lookback(optInTimePeriod, optInNbDev);
    if lb == usize::MAX { r.no_legs("VAR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("VAR", "inReal", 0, run(|| core.VAR(startIdx, endIdx, &inReal, optInTimePeriod, optInNbDev, &mut outReal)));
    }
    r.legs_done("VAR", 1);
}

const V_VWAP: &[&str] = &[
    "defaults",
];

fn sub_VWAP(r: &mut Report) {
    let core = Core::new();
    for &label in V_VWAP {
        let lb = core.VWAP_Lookback();
        if lb == usize::MAX { continue; }
        r.control("VWAP", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.VWAP(0, lb, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("VWAP", label); continue; }
        r.quiet("VWAP", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.VWAP(0, lb - 1, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)
        }));
    }
}

fn legs_VWAP(r: &mut Report) {
    let core = Core::new();
    let lb = core.VWAP_Lookback();
    if lb == usize::MAX { r.no_legs("VWAP"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("VWAP", "inHigh", 0, run(|| core.VWAP(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("VWAP", "inLow", 1, run(|| core.VWAP(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("VWAP", "inClose", 2, run(|| core.VWAP(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("VWAP", "inVolume", 3, run(|| core.VWAP(startIdx, endIdx, &inHigh, &inLow, &inClose, &inVolume, &mut outReal)));
    }
    r.legs_done("VWAP", 4);
}

const V_VWMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_VWMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_VWMA {
        let lb = core.VWMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("VWMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.VWMA(0, lb, &inReal, &inVolume, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("VWMA", label); continue; }
        r.quiet("VWMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let inVolume: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.VWMA(0, lb - 1, &inReal, &inVolume, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_VWMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.VWMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("VWMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let inVolume: Vec<f64> = series("volume", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("VWMA", "inReal", 0, run(|| core.VWMA(startIdx, endIdx, &inReal, &inVolume, optInTimePeriod, &mut outReal)));
    }
    {
        let inReal: Vec<f64> = series("real", endIdx + 1);
        let inVolume: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("VWMA", "inVolume", 1, run(|| core.VWMA(startIdx, endIdx, &inReal, &inVolume, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("VWMA", 2);
}

const V_WAD: &[&str] = &[
    "defaults",
];

fn sub_WAD(r: &mut Report) {
    let core = Core::new();
    for &label in V_WAD {
        let lb = core.WAD_Lookback();
        if lb == usize::MAX { continue; }
        r.control("WAD", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.WAD(0, lb, &inHigh, &inLow, &inClose, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("WAD", label); continue; }
        r.quiet("WAD", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.WAD(0, lb - 1, &inHigh, &inLow, &inClose, &mut outReal)
        }));
    }
}

fn legs_WAD(r: &mut Report) {
    let core = Core::new();
    let lb = core.WAD_Lookback();
    if lb == usize::MAX { r.no_legs("WAD"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WAD", "inHigh", 0, run(|| core.WAD(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WAD", "inLow", 1, run(|| core.WAD(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WAD", "inClose", 2, run(|| core.WAD(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    r.legs_done("WAD", 3);
}

const V_WCLPRICE: &[&str] = &[
    "defaults",
];

fn sub_WCLPRICE(r: &mut Report) {
    let core = Core::new();
    for &label in V_WCLPRICE {
        let lb = core.WCLPRICE_Lookback();
        if lb == usize::MAX { continue; }
        r.control("WCLPRICE", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.WCLPRICE(0, lb, &inHigh, &inLow, &inClose, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("WCLPRICE", label); continue; }
        r.quiet("WCLPRICE", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.WCLPRICE(0, lb - 1, &inHigh, &inLow, &inClose, &mut outReal)
        }));
    }
}

fn legs_WCLPRICE(r: &mut Report) {
    let core = Core::new();
    let lb = core.WCLPRICE_Lookback();
    if lb == usize::MAX { r.no_legs("WCLPRICE"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WCLPRICE", "inHigh", 0, run(|| core.WCLPRICE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WCLPRICE", "inLow", 1, run(|| core.WCLPRICE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WCLPRICE", "inClose", 2, run(|| core.WCLPRICE(startIdx, endIdx, &inHigh, &inLow, &inClose, &mut outReal)));
    }
    r.legs_done("WCLPRICE", 3);
}

const V_WILLR: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 2i32),
];

fn sub_WILLR(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_WILLR {
        let lb = core.WILLR_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("WILLR", label, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.WILLR(0, lb, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("WILLR", label); continue; }
        r.quiet("WILLR", label, lb, run(|| {
            let inHigh: Vec<f64> = Vec::with_capacity(1);
            let inLow: Vec<f64> = Vec::with_capacity(1);
            let inClose: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.WILLR(0, lb - 1, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_WILLR(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.WILLR_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("WILLR"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inHigh: Vec<f64> = Vec::with_capacity(1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WILLR", "inHigh", 0, run(|| core.WILLR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = Vec::with_capacity(1);
        let inClose: Vec<f64> = series("close", endIdx + 1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WILLR", "inLow", 1, run(|| core.WILLR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    {
        let inHigh: Vec<f64> = series("high", endIdx + 1);
        let inLow: Vec<f64> = series("low", endIdx + 1);
        let inClose: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WILLR", "inClose", 2, run(|| core.WILLR(startIdx, endIdx, &inHigh, &inLow, &inClose, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("WILLR", 3);
}

const V_WMA: &[(&str, i32)] = &[
    ("defaults", i32::MIN),
    ("minimums", 1i32),
];

fn sub_WMA(r: &mut Report) {
    let core = Core::new();
    for &(label, optInTimePeriod) in V_WMA {
        let lb = core.WMA_Lookback(optInTimePeriod);
        if lb == usize::MAX { continue; }
        r.control("WMA", label, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.WMA(0, lb, &inReal, optInTimePeriod, &mut outReal)
        }));
        if lb < 1 { r.no_quiet_range("WMA", label); continue; }
        r.quiet("WMA", label, lb, run(|| {
            let inReal: Vec<f64> = Vec::with_capacity(1);
            let mut outReal: Vec<f64> = Vec::with_capacity(1);
            core.WMA(0, lb - 1, &inReal, optInTimePeriod, &mut outReal)
        }));
    }
}

fn legs_WMA(r: &mut Report) {
    let core = Core::new();
    let optInTimePeriod = i32::MIN;
    let lb = core.WMA_Lookback(optInTimePeriod);
    if lb == usize::MAX { r.no_legs("WMA"); return; }
    let (startIdx, endIdx) = (lb, lb + 4);
    {
        let inReal: Vec<f64> = Vec::with_capacity(1);
        let mut outReal: Vec<f64> = vec![Default::default(); 5];
        r.leg("WMA", "inReal", 0, run(|| core.WMA(startIdx, endIdx, &inReal, optInTimePeriod, &mut outReal)));
    }
    r.legs_done("WMA", 1);
}

type Probe = fn(&mut Report);

/// Every indicator in the corpus, name-sorted, with its two probes.
const PROBES: &[(&str, Probe, Probe)] = &[
    ("AC", sub_AC, legs_AC),
    ("ACCBANDS", sub_ACCBANDS, legs_ACCBANDS),
    ("ACOS", sub_ACOS, legs_ACOS),
    ("AD", sub_AD, legs_AD),
    ("ADD", sub_ADD, legs_ADD),
    ("ADOSC", sub_ADOSC, legs_ADOSC),
    ("ADX", sub_ADX, legs_ADX),
    ("ADXR", sub_ADXR, legs_ADXR),
    ("AO", sub_AO, legs_AO),
    ("APO", sub_APO, legs_APO),
    ("AROON", sub_AROON, legs_AROON),
    ("AROONOSC", sub_AROONOSC, legs_AROONOSC),
    ("ASIN", sub_ASIN, legs_ASIN),
    ("ATAN", sub_ATAN, legs_ATAN),
    ("ATR", sub_ATR, legs_ATR),
    ("AVGDEV", sub_AVGDEV, legs_AVGDEV),
    ("AVGPRICE", sub_AVGPRICE, legs_AVGPRICE),
    ("BBANDS", sub_BBANDS, legs_BBANDS),
    ("BETA", sub_BETA, legs_BETA),
    ("BOP", sub_BOP, legs_BOP),
    ("CCI", sub_CCI, legs_CCI),
    ("CDL2CROWS", sub_CDL2CROWS, legs_CDL2CROWS),
    ("CDL3BLACKCROWS", sub_CDL3BLACKCROWS, legs_CDL3BLACKCROWS),
    ("CDL3INSIDE", sub_CDL3INSIDE, legs_CDL3INSIDE),
    ("CDL3LINESTRIKE", sub_CDL3LINESTRIKE, legs_CDL3LINESTRIKE),
    ("CDL3OUTSIDE", sub_CDL3OUTSIDE, legs_CDL3OUTSIDE),
    ("CDL3STARSINSOUTH", sub_CDL3STARSINSOUTH, legs_CDL3STARSINSOUTH),
    ("CDL3WHITESOLDIERS", sub_CDL3WHITESOLDIERS, legs_CDL3WHITESOLDIERS),
    ("CDLABANDONEDBABY", sub_CDLABANDONEDBABY, legs_CDLABANDONEDBABY),
    ("CDLADVANCEBLOCK", sub_CDLADVANCEBLOCK, legs_CDLADVANCEBLOCK),
    ("CDLBELTHOLD", sub_CDLBELTHOLD, legs_CDLBELTHOLD),
    ("CDLBREAKAWAY", sub_CDLBREAKAWAY, legs_CDLBREAKAWAY),
    ("CDLCLOSINGMARUBOZU", sub_CDLCLOSINGMARUBOZU, legs_CDLCLOSINGMARUBOZU),
    ("CDLCONCEALBABYSWALL", sub_CDLCONCEALBABYSWALL, legs_CDLCONCEALBABYSWALL),
    ("CDLCOUNTERATTACK", sub_CDLCOUNTERATTACK, legs_CDLCOUNTERATTACK),
    ("CDLDARKCLOUDCOVER", sub_CDLDARKCLOUDCOVER, legs_CDLDARKCLOUDCOVER),
    ("CDLDOJI", sub_CDLDOJI, legs_CDLDOJI),
    ("CDLDOJISTAR", sub_CDLDOJISTAR, legs_CDLDOJISTAR),
    ("CDLDRAGONFLYDOJI", sub_CDLDRAGONFLYDOJI, legs_CDLDRAGONFLYDOJI),
    ("CDLENGULFING", sub_CDLENGULFING, legs_CDLENGULFING),
    ("CDLEVENINGDOJISTAR", sub_CDLEVENINGDOJISTAR, legs_CDLEVENINGDOJISTAR),
    ("CDLEVENINGSTAR", sub_CDLEVENINGSTAR, legs_CDLEVENINGSTAR),
    ("CDLGAPSIDESIDEWHITE", sub_CDLGAPSIDESIDEWHITE, legs_CDLGAPSIDESIDEWHITE),
    ("CDLGRAVESTONEDOJI", sub_CDLGRAVESTONEDOJI, legs_CDLGRAVESTONEDOJI),
    ("CDLHAMMER", sub_CDLHAMMER, legs_CDLHAMMER),
    ("CDLHANGINGMAN", sub_CDLHANGINGMAN, legs_CDLHANGINGMAN),
    ("CDLHARAMI", sub_CDLHARAMI, legs_CDLHARAMI),
    ("CDLHARAMICROSS", sub_CDLHARAMICROSS, legs_CDLHARAMICROSS),
    ("CDLHIGHWAVE", sub_CDLHIGHWAVE, legs_CDLHIGHWAVE),
    ("CDLHIKKAKE", sub_CDLHIKKAKE, legs_CDLHIKKAKE),
    ("CDLHIKKAKEMOD", sub_CDLHIKKAKEMOD, legs_CDLHIKKAKEMOD),
    ("CDLHOMINGPIGEON", sub_CDLHOMINGPIGEON, legs_CDLHOMINGPIGEON),
    ("CDLIDENTICAL3CROWS", sub_CDLIDENTICAL3CROWS, legs_CDLIDENTICAL3CROWS),
    ("CDLINNECK", sub_CDLINNECK, legs_CDLINNECK),
    ("CDLINVERTEDHAMMER", sub_CDLINVERTEDHAMMER, legs_CDLINVERTEDHAMMER),
    ("CDLKICKING", sub_CDLKICKING, legs_CDLKICKING),
    ("CDLKICKINGBYLENGTH", sub_CDLKICKINGBYLENGTH, legs_CDLKICKINGBYLENGTH),
    ("CDLLADDERBOTTOM", sub_CDLLADDERBOTTOM, legs_CDLLADDERBOTTOM),
    ("CDLLONGLEGGEDDOJI", sub_CDLLONGLEGGEDDOJI, legs_CDLLONGLEGGEDDOJI),
    ("CDLLONGLINE", sub_CDLLONGLINE, legs_CDLLONGLINE),
    ("CDLMARUBOZU", sub_CDLMARUBOZU, legs_CDLMARUBOZU),
    ("CDLMATCHINGLOW", sub_CDLMATCHINGLOW, legs_CDLMATCHINGLOW),
    ("CDLMATHOLD", sub_CDLMATHOLD, legs_CDLMATHOLD),
    ("CDLMORNINGDOJISTAR", sub_CDLMORNINGDOJISTAR, legs_CDLMORNINGDOJISTAR),
    ("CDLMORNINGSTAR", sub_CDLMORNINGSTAR, legs_CDLMORNINGSTAR),
    ("CDLONNECK", sub_CDLONNECK, legs_CDLONNECK),
    ("CDLPIERCING", sub_CDLPIERCING, legs_CDLPIERCING),
    ("CDLRICKSHAWMAN", sub_CDLRICKSHAWMAN, legs_CDLRICKSHAWMAN),
    ("CDLRISEFALL3METHODS", sub_CDLRISEFALL3METHODS, legs_CDLRISEFALL3METHODS),
    ("CDLSEPARATINGLINES", sub_CDLSEPARATINGLINES, legs_CDLSEPARATINGLINES),
    ("CDLSHOOTINGSTAR", sub_CDLSHOOTINGSTAR, legs_CDLSHOOTINGSTAR),
    ("CDLSHORTLINE", sub_CDLSHORTLINE, legs_CDLSHORTLINE),
    ("CDLSPINNINGTOP", sub_CDLSPINNINGTOP, legs_CDLSPINNINGTOP),
    ("CDLSTALLEDPATTERN", sub_CDLSTALLEDPATTERN, legs_CDLSTALLEDPATTERN),
    ("CDLSTICKSANDWICH", sub_CDLSTICKSANDWICH, legs_CDLSTICKSANDWICH),
    ("CDLTAKURI", sub_CDLTAKURI, legs_CDLTAKURI),
    ("CDLTASUKIGAP", sub_CDLTASUKIGAP, legs_CDLTASUKIGAP),
    ("CDLTHRUSTING", sub_CDLTHRUSTING, legs_CDLTHRUSTING),
    ("CDLTRISTAR", sub_CDLTRISTAR, legs_CDLTRISTAR),
    ("CDLUNIQUE3RIVER", sub_CDLUNIQUE3RIVER, legs_CDLUNIQUE3RIVER),
    ("CDLUPSIDEGAP2CROWS", sub_CDLUPSIDEGAP2CROWS, legs_CDLUPSIDEGAP2CROWS),
    ("CDLXSIDEGAP3METHODS", sub_CDLXSIDEGAP3METHODS, legs_CDLXSIDEGAP3METHODS),
    ("CEIL", sub_CEIL, legs_CEIL),
    ("CMF", sub_CMF, legs_CMF),
    ("CMO", sub_CMO, legs_CMO),
    ("CMOU", sub_CMOU, legs_CMOU),
    ("CORREL", sub_CORREL, legs_CORREL),
    ("COS", sub_COS, legs_COS),
    ("COSH", sub_COSH, legs_COSH),
    ("DEMA", sub_DEMA, legs_DEMA),
    ("DIV", sub_DIV, legs_DIV),
    ("DX", sub_DX, legs_DX),
    ("EFI", sub_EFI, legs_EFI),
    ("EMA", sub_EMA, legs_EMA),
    ("EXP", sub_EXP, legs_EXP),
    ("FLOOR", sub_FLOOR, legs_FLOOR),
    ("HMA", sub_HMA, legs_HMA),
    ("HT_DCPERIOD", sub_HT_DCPERIOD, legs_HT_DCPERIOD),
    ("HT_DCPHASE", sub_HT_DCPHASE, legs_HT_DCPHASE),
    ("HT_PHASOR", sub_HT_PHASOR, legs_HT_PHASOR),
    ("HT_SINE", sub_HT_SINE, legs_HT_SINE),
    ("HT_TRENDLINE", sub_HT_TRENDLINE, legs_HT_TRENDLINE),
    ("HT_TRENDMODE", sub_HT_TRENDMODE, legs_HT_TRENDMODE),
    ("IMI", sub_IMI, legs_IMI),
    ("KAMA", sub_KAMA, legs_KAMA),
    ("LINEARREG", sub_LINEARREG, legs_LINEARREG),
    ("LINEARREG_ANGLE", sub_LINEARREG_ANGLE, legs_LINEARREG_ANGLE),
    ("LINEARREG_INTERCEPT", sub_LINEARREG_INTERCEPT, legs_LINEARREG_INTERCEPT),
    ("LINEARREG_SLOPE", sub_LINEARREG_SLOPE, legs_LINEARREG_SLOPE),
    ("LN", sub_LN, legs_LN),
    ("LOG10", sub_LOG10, legs_LOG10),
    ("MA", sub_MA, legs_MA),
    ("MACD", sub_MACD, legs_MACD),
    ("MACDEXT", sub_MACDEXT, legs_MACDEXT),
    ("MACDFIX", sub_MACDFIX, legs_MACDFIX),
    ("MAMA", sub_MAMA, legs_MAMA),
    ("MARKETFI", sub_MARKETFI, legs_MARKETFI),
    ("MAVP", sub_MAVP, legs_MAVP),
    ("MAX", sub_MAX, legs_MAX),
    ("MAXINDEX", sub_MAXINDEX, legs_MAXINDEX),
    ("MEDPRICE", sub_MEDPRICE, legs_MEDPRICE),
    ("MFI", sub_MFI, legs_MFI),
    ("MIDPOINT", sub_MIDPOINT, legs_MIDPOINT),
    ("MIDPRICE", sub_MIDPRICE, legs_MIDPRICE),
    ("MIN", sub_MIN, legs_MIN),
    ("MININDEX", sub_MININDEX, legs_MININDEX),
    ("MINMAX", sub_MINMAX, legs_MINMAX),
    ("MINMAXINDEX", sub_MINMAXINDEX, legs_MINMAXINDEX),
    ("MINUS_DI", sub_MINUS_DI, legs_MINUS_DI),
    ("MINUS_DM", sub_MINUS_DM, legs_MINUS_DM),
    ("MOM", sub_MOM, legs_MOM),
    ("MULT", sub_MULT, legs_MULT),
    ("NATR", sub_NATR, legs_NATR),
    ("NVI", sub_NVI, legs_NVI),
    ("OBV", sub_OBV, legs_OBV),
    ("PLUS_DI", sub_PLUS_DI, legs_PLUS_DI),
    ("PLUS_DM", sub_PLUS_DM, legs_PLUS_DM),
    ("PPO", sub_PPO, legs_PPO),
    ("PVI", sub_PVI, legs_PVI),
    ("PVO", sub_PVO, legs_PVO),
    ("QSTICK", sub_QSTICK, legs_QSTICK),
    ("ROC", sub_ROC, legs_ROC),
    ("ROCP", sub_ROCP, legs_ROCP),
    ("ROCR", sub_ROCR, legs_ROCR),
    ("ROCR100", sub_ROCR100, legs_ROCR100),
    ("RSI", sub_RSI, legs_RSI),
    ("SAR", sub_SAR, legs_SAR),
    ("SAREXT", sub_SAREXT, legs_SAREXT),
    ("SIN", sub_SIN, legs_SIN),
    ("SINH", sub_SINH, legs_SINH),
    ("SMA", sub_SMA, legs_SMA),
    ("SMI", sub_SMI, legs_SMI),
    ("SQRT", sub_SQRT, legs_SQRT),
    ("STDDEV", sub_STDDEV, legs_STDDEV),
    ("STOCH", sub_STOCH, legs_STOCH),
    ("STOCHF", sub_STOCHF, legs_STOCHF),
    ("STOCHRSI", sub_STOCHRSI, legs_STOCHRSI),
    ("SUB", sub_SUB, legs_SUB),
    ("SUM", sub_SUM, legs_SUM),
    ("T3", sub_T3, legs_T3),
    ("TAN", sub_TAN, legs_TAN),
    ("TANH", sub_TANH, legs_TANH),
    ("TEMA", sub_TEMA, legs_TEMA),
    ("TRANGE", sub_TRANGE, legs_TRANGE),
    ("TRIMA", sub_TRIMA, legs_TRIMA),
    ("TRIX", sub_TRIX, legs_TRIX),
    ("TSF", sub_TSF, legs_TSF),
    ("TYPPRICE", sub_TYPPRICE, legs_TYPPRICE),
    ("ULTOSC", sub_ULTOSC, legs_ULTOSC),
    ("VAR", sub_VAR, legs_VAR),
    ("VWAP", sub_VWAP, legs_VWAP),
    ("VWMA", sub_VWMA, legs_VWMA),
    ("WAD", sub_WAD, legs_WAD),
    ("WCLPRICE", sub_WCLPRICE, legs_WCLPRICE),
    ("WILLR", sub_WILLR, legs_WILLR),
    ("WMA", sub_WMA, legs_WMA),
];

#[test]
fn no_phantom_io() {
    // One test, both sweeps, sequential: the panic hook is process-global, and
    // two tests racing to install and restore it is a flake with no bug behind it.
    let prior = std::panic::take_hook();
    std::panic::set_hook(Box::new(|_| {}));

    let mut r = Report::new();
    for (name, sub, _) in PROBES {
        r.name = name;
        sub(&mut r);
    }
    r.finish_sub_lookback();
    for (name, _, legs) in PROBES {
        r.name = name;
        legs(&mut r);
    }
    r.finish_legs();

    std::panic::set_hook(prior);

    // The corpus is the generator's, not a list kept by hand: a probe that
    // stopped being emitted is a shrinking sweep, which is the one way this
    // file can fail open.
    assert_eq!(PROBES.len(), 176, "probe count");
    assert_eq!(
        PROBES.len(),
        ta_lib::abstract_api::funcs().count(),
        "every registered function has a probe"
    );

    r.print();
    assert!(r.failures.is_empty(), "{} phantom-I/O violation(s)", r.failures.len());
}
