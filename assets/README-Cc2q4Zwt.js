import{D as e,E as t,P as n}from"./app-HG0hOr4g.js";import{t as r}from"./plugin-vue_export-helper-BDNMzG2s.js";var i=JSON.parse(`{"path":"/contribute/","title":"Contribute a TA Function","lang":"en-US","frontmatter":{"title":"Contribute a TA Function","description":"Contribute a new TA function to TA-Lib: agree the spec in the open, hand the implementation to an AI agent, prove it against golden values from an oracle.","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Contribute a TA Function\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-08-07T02:18:24.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"TA-Lib\\",\\"url\\":\\"https://ta-lib.org\\"}]}"],["meta",{"property":"og:url","content":"https://ta-lib.org/contribute/"}],["meta",{"property":"og:site_name","content":"TA-Lib.org"}],["meta",{"property":"og:title","content":"Contribute a TA Function"}],["meta",{"property":"og:description","content":"Contribute a new TA function to TA-Lib: agree the spec in the open, hand the implementation to an AI agent, prove it against golden values from an oracle."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"property":"og:updated_time","content":"2026-08-07T02:18:24.000Z"}],["meta",{"property":"article:modified_time","content":"2026-08-07T02:18:24.000Z"}],["link",{"rel":"canonical","href":"https://ta-lib.org/contribute/"}]]},"git":{"createdTime":1785787519000,"updatedTime":1786069104000,"contributors":[{"name":"mario4tier","username":"mario4tier","email":"mario4tier@users.noreply.github.com","commits":7,"url":"https://github.com/mario4tier"}]},"readingTime":{"minutes":4.48,"words":1343},"filePathRelative":"contribute/README.md"}`),a={name:`README.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="how-to-contribute-a-new-ta-function" tabindex="-1"><a class="header-anchor" href="#how-to-contribute-a-new-ta-function"><span>How to Contribute a New TA Function</span></a></h1><div class="hint-container tip"><p class="hint-container-title">Just want to request a function, not implement it?</p><p>Then add it to the <a href="https://github.com/orgs/TA-Lib/projects/1" target="_blank" rel="noopener noreferrer">New TA Functions board</a> and someone else might implement it for you.</p></div><p>TA-Lib development is <strong>human-driven specs, AI-driven code</strong>.</p><p>The implementation is meant to be written by an AI agent: hand it this page, which contains everything needed to do the job.</p><p>Humans agree, in the open, what a function computes before code is written: its name, formula, inputs, parameters and defaults.</p><p>No function is ever integrated without hardcoded golden values in the regression tests, taken from an independent oracle or a published sample. The regression tests are the final arbiter of correctness, and they are human-reviewed.</p><p>The test harness is exhaustive (verifies many edge cases) and you should not have to modify these. Any modification to the harness requires explicit human approval. Adding and registering your own function&#39;s test is expected. &quot;The harness&quot; means the shared machinery: the comparison gates, tolerances and other functions&#39; tests.</p><h2 id="what-a-contribution-looks-like" tabindex="-1"><a class="header-anchor" href="#what-a-contribution-looks-like"><span>What a contribution looks like</span></a></h2><p>TA-Lib generates all of its per-function code from a single source of truth. One function is one directory, <code>ta_codegen/input/&lt;name&gt;/</code>, holding three files:</p><table><thead><tr><th>File</th><th>Contains</th></tr></thead><tbody><tr><td><code>&lt;name&gt;.yaml</code></td><td>Metadata: inputs, outputs, parameters, defaults, group, flags. Data only, never logic.</td></tr><tr><td><code>&lt;name&gt;.c</code></td><td>The algorithm: a <code>&lt;name&gt;_lookback()</code> and a <code>&lt;name&gt;()</code> batch function, in plain portable C.</td></tr><tr><td><code>&lt;name&gt;.md</code></td><td>Documentation: summary, the formula in its original algebraic form, references.</td></tr></tbody></table><p>From these three files, the <code>ta_codegen</code> generator produces everything the project ships: the C library, native Rust, Java and .NET implementations, streaming variants, the test servers, benchmarks, and this website&#39;s function page. You never write Rust, Java or C# by hand, and you never edit generated files.</p><p>Every backend is then verified by <code>ta_regtest</code>, which drives all languages through JSON-RPC servers and cross-checks their outputs against the C reference, bit-for-bit in the strictest gates.</p><h2 id="development-setup" tabindex="-1"><a class="header-anchor" href="#development-setup"><span>Development setup</span></a></h2><p>Any of Linux, macOS, Windows or WSL2. You need:</p><ul><li>git, a C compiler (gcc/clang/MSVC), CMake ≥ 3.18</li><li>The Rust toolchain via <a href="https://rustup.rs" target="_blank" rel="noopener noreferrer">rustup</a> (the generator is written in Rust)</li><li>Recommended, for full cross-language verification: a JDK (<code>javac</code>/<code>java</code>) and the .NET SDK (<code>dotnet</code>). Without them, pass <code>--language=c,rust</code> to <code>scripts/build.py servers</code> and <code>scripts/regtest.py</code>. The C and Rust gates still verify your function, and CI runs the full matrix.</li></ul><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">git</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> clone</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> https://github.com/TA-Lib/ta-lib.git</span></span>
<span class="line"><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">cd</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> ta-lib</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">git</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> switch</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> dev</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">              # contributions branch from and merge into dev</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">scripts/build.py</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">            # build the C library + tools</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">scripts/build.py</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> test</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">       # run the C regression tests (confirms your setup works)</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">scripts/build.py</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> servers</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">    # build the generator + the per-language test servers</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>scripts/build.py</code> checks prerequisites per target and configures CMake automatically on first run.</p><h2 id="steps" tabindex="-1"><a class="header-anchor" href="#steps"><span>Steps</span></a></h2><ol><li><strong>Agree on the spec first.</strong> Claim a card on the <a href="https://github.com/orgs/TA-Lib/projects/1" target="_blank" rel="noopener noreferrer">board</a>, or open an issue if there is none. Settle the name, group, inputs, parameters with defaults, flags (does it stream?), and the formula with a citable reference before writing code. This is the human-approved part.</li><li><strong>Write the three input files</strong> in <code>ta_codegen/input/&lt;name&gt;/</code>. Start by copying a similar shipped function; <code>ta_codegen/input/cmf/</code> is a small, recent example. Keep the BSD-3-Clause license header, and add your initials and a one-line MMDDYY entry to its contributor table. The file-format references are in the repository: <a href="https://github.com/TA-Lib/ta-lib/blob/dev/docs/ta_codegen_input_yaml.md" target="_blank" rel="noopener noreferrer"><code>docs/ta_codegen_input_yaml.md</code></a>, <a href="https://github.com/TA-Lib/ta-lib/blob/dev/docs/ta_codegen_input_code.md" target="_blank" rel="noopener noreferrer"><code>ta_codegen_input_code.md</code></a> and <a href="https://github.com/TA-Lib/ta-lib/blob/dev/docs/ta_codegen_input_doc.md" target="_blank" rel="noopener noreferrer"><code>ta_codegen_input_doc.md</code></a>. When in doubt, read your checkout&#39;s copy; it tracks the generator you are running.</li><li><strong>Generate</strong> all backends: <code>scripts/build.py generate</code>, then <code>scripts/build.py servers</code> to rebuild the language servers from the regenerated sources.</li><li><strong>Verify</strong>: run <code>scripts/regtest.py</code>, the full pipeline (it also builds the frozen pre-cutover reference server that <code>ta_regtest --codegen</code> needs). A brand-new function is reported as <em>skipped</em> by the generic sweep, because it does not exist in the frozen reference; the next step is what verifies it. The first run takes ~30 minutes and checks out the frozen reference into a sibling <code>ta-lib-ref</code> worktree; <code>--no-perftest</code> skips the benchmarks a correctness change does not need.</li><li><strong>Prove the values.</strong> A new function needs a regression test with golden values from an independent source: a published reference implementation or worked examples from the literature. Document the source, its version and the tolerance at the test call site; against a double-precision oracle ~1e-12 relative is typical, and anything looser needs a written justification. See <code>src/tools/ta_regtest/ta_test_func/test_composite.c</code> for the pattern, and register the test in four places: a prototype in <code>ta_test_func.h</code>, a <code>DO_TEST</code> entry in <code>ta_regtest.c</code> whose tag names every function the group covers, and the file added to both <code>CMakeLists.txt</code> and <code>src/tools/ta_regtest/Makefile.am</code> (<code>scripts/build.py check-source-lists</code> verifies the two agree). While iterating, <code>cd bin &amp;&amp; ./ta_regtest --codegen --function=&lt;NAME&gt;</code> re-runs just your group, with every call cross-checked bitwise against all language servers.</li><li><strong>Check the diff.</strong> After regenerating, <code>git diff</code> should touch only files belonging to your function. Unrelated churn in other generated files means something is wrong.</li><li><strong>Open a pull request</strong> against the <code>dev</code> branch, citing the spec issue and the verification source.</li></ol><p>Stuck at any step? Ask on <a href="https://discord.com/invite/Erb6SwsVbH" target="_blank" rel="noopener noreferrer">Discord</a> or comment on your spec issue.</p><h2 id="notes-for-ai-assistants" tabindex="-1"><a class="header-anchor" href="#notes-for-ai-assistants"><span>Notes for AI assistants</span></a></h2><p>You are implementing a TA function for TA-Lib, following the steps above on behalf of a human contributor. Additional constraints and pointers:</p><p><strong>Ground truth is the repository, not this page.</strong> Read, in order:</p><ul><li><code>CLAUDE.md</code> at the repo root</li><li>In <code>docs/</code>: <code>ta_codegen_input_yaml.md</code>, then <code>ta_codegen_input_code.md</code>, then <code>ta_codegen_input_doc.md</code></li></ul><p>If this page and the repo disagree, the repo wins; it is versioned with the code. If you are Claude Code, the repo ships a <code>/new-ta-func</code> skill that automates this workflow; use it. It is an accelerator, not a requirement: everything it does is covered by this page and the repository docs.</p><p><strong>Invariants.</strong> Violating any of these fails review:</p><ul><li>No logic in YAML, ever. No metadata in the <code>.c</code> file.</li><li>Never edit generated output: <code>src/ta_func/</code>, <code>src/ta_abstract/</code> and everything under <code>ta_codegen/output/</code> are overwritten on the next <code>generate</code>. Change <code>ta_codegen/input/</code> and regenerate.</li><li><code>&lt;name&gt;.md</code> documents the original algebra of the indicator, never implementation artifacts: no zero-guards, epsilon comparisons or <code>period == 1</code> special cases in the formula.</li><li>In the <code>.c</code> input, call other TA functions by their bare lowercase name (<code>sma(...)</code>, <code>ema_lookback(...)</code>); the generator resolves each to the language&#39;s native symbol.</li><li>Enums, groups and other shared surfaces are generated; search the generator before hand-adding one anywhere.</li><li>If <code>generate</code> panics on a C construct, do not contort the algorithm to dodge the parser. Match the style of a shipped input file, or raise it on the spec issue: parser extensions are generator changes and need maintainer sign-off.</li></ul><p><strong>Definition of done:</strong></p><ul><li><code>scripts/regtest.py</code> passes end to end (C tests + all-language verification).</li><li>The new function has a non-vacuous test: golden values from an independent source, with source, version and tolerance documented at the call site. A test that cannot fail on a wrong formula does not count.</li><li>Regeneration is idempotent: running <code>generate</code> again leaves <code>git status</code> clean.</li><li>The generator&#39;s own suite passes: <code>cd ta_codegen/generator &amp;&amp; cargo test</code>.</li><li>The generated <code>website/src/functions/&lt;name&gt;.md</code> is correct; its sections are gated against the YAML by the generator. Optional live preview: <code>cd website &amp;&amp; pnpm install &amp;&amp; pnpm docs:dev</code> (needs Node and pnpm).</li></ul><p><strong>DO NOT:</strong></p><ul><li><strong>DO NOT</strong> invent parameter defaults or ranges. The <code>&lt;name&gt;.yaml</code> spec is the human-approved source of truth for acceptable ranges and defaults; ask the contributor.</li><li><strong>DO NOT</strong> modify the test harness or loosen a tolerance to make a gate pass. Harness changes require explicit human approval.</li><li><strong>DO NOT</strong> include unrelated generated-file churn in the pull request.</li><li><strong>DO NOT</strong> commit or push without explicit human approval.</li></ul>`,31)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};